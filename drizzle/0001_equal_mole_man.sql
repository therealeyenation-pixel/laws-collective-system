CREATE TABLE `academy_courses` (
	`id` int AUTO_INCREMENT NOT NULL,
	`houseId` int NOT NULL,
	`moduleId` int NOT NULL,
	`title` varchar(200) NOT NULL,
	`description` text,
	`scrollTitle` varchar(200),
	`level` enum('foundational','developing','mastery') NOT NULL,
	`estimatedHours` int DEFAULT 10,
	`tokensReward` int DEFAULT 100,
	`prerequisites` json,
	`learningObjectives` json,
	`status` enum('draft','active','archived') NOT NULL DEFAULT 'draft',
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `academy_courses_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `academy_houses` (
	`id` int AUTO_INCREMENT NOT NULL,
	`name` varchar(100) NOT NULL,
	`slug` varchar(50) NOT NULL,
	`description` text,
	`ageRange` varchar(20) NOT NULL,
	`gradeRange` varchar(20) NOT NULL,
	`ceremonialName` varchar(150),
	`iconPath` varchar(255),
	`colorTheme` varchar(50),
	`status` enum('active','inactive','coming_soon') NOT NULL DEFAULT 'active',
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `academy_houses_id` PRIMARY KEY(`id`),
	CONSTRAINT `academy_houses_slug_unique` UNIQUE(`slug`)
);
--> statement-breakpoint
CREATE TABLE `academy_instructors` (
	`id` int AUTO_INCREMENT NOT NULL,
	`name` varchar(200) NOT NULL,
	`email` varchar(200),
	`phone` varchar(50),
	`instructorType` enum('credentialed','sme_guest','adjunct','full_time') NOT NULL DEFAULT 'adjunct',
	`credentials` json,
	`specializations` json,
	`hourlyRate` decimal(8,2),
	`contractStatus` enum('active','inactive','pending') NOT NULL DEFAULT 'pending',
	`supervisorId` int,
	`backgroundCheckDate` timestamp,
	`backgroundCheckStatus` enum('pending','cleared','expired') DEFAULT 'pending',
	`notes` text,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `academy_instructors_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `academy_languages` (
	`id` int AUTO_INCREMENT NOT NULL,
	`name` varchar(100) NOT NULL,
	`nativeName` varchar(100),
	`slug` varchar(50) NOT NULL,
	`category` enum('indigenous','ancestral_flame','global_trade') NOT NULL,
	`description` text,
	`culturalContext` text,
	`iconEmoji` varchar(10),
	`status` enum('active','inactive','coming_soon') NOT NULL DEFAULT 'active',
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	CONSTRAINT `academy_languages_id` PRIMARY KEY(`id`),
	CONSTRAINT `academy_languages_slug_unique` UNIQUE(`slug`)
);
--> statement-breakpoint
CREATE TABLE `academy_lessons` (
	`id` int AUTO_INCREMENT NOT NULL,
	`courseId` int NOT NULL,
	`title` varchar(200) NOT NULL,
	`scrollName` varchar(200),
	`content` text,
	`contentType` enum('text','video','interactive','ceremony','practice') NOT NULL,
	`orderIndex` int NOT NULL DEFAULT 0,
	`estimatedMinutes` int DEFAULT 30,
	`tokensReward` int DEFAULT 10,
	`resources` json,
	`status` enum('draft','active','archived') NOT NULL DEFAULT 'draft',
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	CONSTRAINT `academy_lessons_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `accreditation_records` (
	`id` int AUTO_INCREMENT NOT NULL,
	`accreditingBody` varchar(200) NOT NULL,
	`accreditationType` enum('regional','national','programmatic','state') NOT NULL,
	`status` enum('researching','applying','pending','accredited','probation','revoked') NOT NULL DEFAULT 'researching',
	`applicationDate` timestamp,
	`grantedDate` timestamp,
	`expirationDate` timestamp,
	`renewalDate` timestamp,
	`requirements` json,
	`documentsSubmitted` json,
	`notes` text,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `accreditation_records_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `ach_batches` (
	`id` int AUTO_INCREMENT NOT NULL,
	`batch_number` varchar(50) NOT NULL,
	`payroll_period_id` int,
	`company_name` varchar(16) NOT NULL,
	`company_id` varchar(10) NOT NULL,
	`total_credits` decimal(18,2) NOT NULL DEFAULT '0',
	`total_debits` decimal(18,2) NOT NULL DEFAULT '0',
	`entry_count` int NOT NULL DEFAULT 0,
	`effective_date` timestamp NOT NULL,
	`file_created_at` timestamp,
	`ach_batch_status` enum('draft','generated','submitted','accepted','rejected','processed') NOT NULL DEFAULT 'draft',
	`file_name` varchar(255),
	`file_url` text,
	`created_at` timestamp NOT NULL DEFAULT (now()),
	`updated_at` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `ach_batches_id` PRIMARY KEY(`id`),
	CONSTRAINT `ach_batches_batch_number_unique` UNIQUE(`batch_number`)
);
--> statement-breakpoint
CREATE TABLE `ach_entries` (
	`id` int AUTO_INCREMENT NOT NULL,
	`batch_id` int NOT NULL,
	`payroll_run_id` int,
	`worker_id` int NOT NULL,
	`bank_account_id` int NOT NULL,
	`transaction_code` varchar(2) NOT NULL,
	`routing_number` varchar(9) NOT NULL,
	`account_number` varchar(17) NOT NULL,
	`amount` decimal(18,2) NOT NULL,
	`individual_id` varchar(15) NOT NULL,
	`individual_name` varchar(22) NOT NULL,
	`ach_entry_status` enum('pending','included','returned','settled') NOT NULL DEFAULT 'pending',
	`return_code` varchar(3),
	`return_reason` text,
	`created_at` timestamp NOT NULL DEFAULT (now()),
	`updated_at` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `ach_entries_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `achievement_blockchain_records` (
	`id` int AUTO_INCREMENT NOT NULL,
	`playerId` int NOT NULL,
	`playerAchievementId` int NOT NULL,
	`achievementId` int NOT NULL,
	`blockchainHash` varchar(255) NOT NULL,
	`previousHash` varchar(255),
	`blockNumber` int,
	`transactionHash` varchar(255),
	`recordType` enum('achievement_unlock','tier_upgrade','nft_mint','challenge_complete') NOT NULL,
	`achievementName` varchar(255) NOT NULL,
	`tier` enum('bronze','silver','gold','platinum'),
	`tokensAwarded` int DEFAULT 0,
	`signature` text,
	`verificationCode` varchar(64) NOT NULL,
	`isVerified` boolean NOT NULL DEFAULT true,
	`metadata` json,
	`recordedAt` timestamp NOT NULL DEFAULT (now()),
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	CONSTRAINT `achievement_blockchain_records_id` PRIMARY KEY(`id`),
	CONSTRAINT `achievement_blockchain_records_blockchainHash_unique` UNIQUE(`blockchainHash`),
	CONSTRAINT `achievement_blockchain_records_verificationCode_unique` UNIQUE(`verificationCode`)
);
--> statement-breakpoint
CREATE TABLE `activation_credits` (
	`id` int AUTO_INCREMENT NOT NULL,
	`houseId` int NOT NULL,
	`userId` int NOT NULL,
	`creditType` enum('course_completion','community_participation','referral','milestone_achievement','time_tenure','bonus') NOT NULL,
	`credits` int NOT NULL,
	`description` text,
	`sourceReferenceType` varchar(50),
	`sourceReferenceId` int,
	`expiresAt` timestamp,
	`status` enum('active','used','expired') NOT NULL DEFAULT 'active',
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	CONSTRAINT `activation_credits_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `activation_requirements` (
	`id` int AUTO_INCREMENT NOT NULL,
	`requirementCode` varchar(50) NOT NULL,
	`name` varchar(255) NOT NULL,
	`description` text,
	`category` enum('tenure','training','legal','financial','documentation','ceremonial') NOT NULL,
	`verificationType` enum('automatic','document_upload','manual_review','external_api','self_attestation') NOT NULL,
	`verificationConfig` json,
	`iconName` varchar(50),
	`sortOrder` int NOT NULL DEFAULT 0,
	`isActive` boolean NOT NULL DEFAULT true,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `activation_requirements_id` PRIMARY KEY(`id`),
	CONSTRAINT `activation_requirements_requirementCode_unique` UNIQUE(`requirementCode`)
);
--> statement-breakpoint
CREATE TABLE `agent_actions` (
	`id` int AUTO_INCREMENT NOT NULL,
	`agentId` int NOT NULL,
	`conversationId` int,
	`userId` int NOT NULL,
	`actionType` enum('query','create','update','delete','approve','reject','notify','analyze','generate','transfer') NOT NULL,
	`targetType` varchar(50),
	`targetId` int,
	`description` text,
	`result` json,
	`status` enum('pending','completed','failed') NOT NULL DEFAULT 'completed',
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	CONSTRAINT `agent_actions_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `agent_conversations` (
	`id` int AUTO_INCREMENT NOT NULL,
	`agentId` int NOT NULL,
	`userId` int NOT NULL,
	`title` varchar(255),
	`status` enum('active','archived','deleted') NOT NULL DEFAULT 'active',
	`metadata` json,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `agent_conversations_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `agent_messages` (
	`id` int AUTO_INCREMENT NOT NULL,
	`conversationId` int NOT NULL,
	`role` enum('user','assistant','system') NOT NULL,
	`content` text NOT NULL,
	`metadata` json,
	`tokenCount` int,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	CONSTRAINT `agent_messages_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `agents` (
	`id` int AUTO_INCREMENT NOT NULL,
	`name` varchar(100) NOT NULL,
	`type` enum('operations','support','education','analytics','guardian','finance','media','outreach','seo','engagement','hr','qaqc','purchasing','health','design','custom') NOT NULL,
	`description` text,
	`avatar` varchar(500),
	`systemPrompt` text NOT NULL,
	`capabilities` json,
	`entityId` int,
	`isActive` boolean NOT NULL DEFAULT true,
	`isPublic` boolean NOT NULL DEFAULT false,
	`createdBy` int NOT NULL,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `agents_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `allocation_pots` (
	`id` int AUTO_INCREMENT NOT NULL,
	`houseId` int NOT NULL,
	`potType` enum('root_authority_reserve','circulation_pool','house_operational','steward_compensation','commercial_operating','future_crown','ancestral_treasury') NOT NULL,
	`balance` decimal(20,8) NOT NULL DEFAULT '0',
	`targetPercentage` decimal(5,2) NOT NULL,
	`isShareable` boolean NOT NULL DEFAULT false,
	`requiresApproval` boolean NOT NULL DEFAULT true,
	`minimumBalance` decimal(20,8) NOT NULL DEFAULT '0',
	`lastSyncAt` timestamp,
	`potStatus` enum('active','frozen','depleted') NOT NULL DEFAULT 'active',
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `allocation_pots_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `allocation_transactions` (
	`id` int AUTO_INCREMENT NOT NULL,
	`sourceHouseId` int NOT NULL,
	`sourceAccountId` int,
	`grossAmount` decimal(20,8) NOT NULL,
	`houseAmount` decimal(20,8) NOT NULL,
	`treasuryAmount` decimal(20,8) NOT NULL,
	`reserveAmount` decimal(20,8) NOT NULL,
	`circulationAmount` decimal(20,8) NOT NULL,
	`allocationType` enum('income_allocation','distribution','inter_house_transfer','pot_rebalance','manual_adjustment') NOT NULL,
	`description` text,
	`referenceId` varchar(255),
	`validationStatus` enum('pending','validated','failed','manual_review') NOT NULL DEFAULT 'pending',
	`validationErrors` json,
	`blockchainHash` varchar(255),
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`processedAt` timestamp,
	CONSTRAINT `allocation_transactions_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `app_versions` (
	`id` int AUTO_INCREMENT NOT NULL,
	`version` varchar(20) NOT NULL,
	`buildNumber` int,
	`releaseNotes` text,
	`releaseDate` timestamp NOT NULL DEFAULT (now()),
	`isStable` boolean NOT NULL DEFAULT true,
	`downloadUrl` varchar(500),
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	CONSTRAINT `app_versions_id` PRIMARY KEY(`id`),
	CONSTRAINT `app_versions_version_unique` UNIQUE(`version`)
);
--> statement-breakpoint
CREATE TABLE `application_activity_log` (
	`id` int AUTO_INCREMENT NOT NULL,
	`applicationId` int NOT NULL,
	`actorId` int,
	`actorName` varchar(255),
	`action` enum('application_received','status_changed','document_uploaded','interview_scheduled','interview_completed','note_added','score_updated','offer_sent','offer_accepted','offer_rejected','hired','rejected','withdrawn') NOT NULL,
	`previousValue` text,
	`newValue` text,
	`notes` text,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	CONSTRAINT `application_activity_log_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `application_documents` (
	`id` int AUTO_INCREMENT NOT NULL,
	`applicationId` int NOT NULL,
	`documentType` enum('resume','cover_letter','portfolio','certification','reference_letter','transcript','other') NOT NULL,
	`fileName` varchar(255) NOT NULL,
	`fileKey` varchar(500) NOT NULL,
	`fileUrl` text NOT NULL,
	`fileSize` int,
	`mimeType` varchar(100),
	`uploadedAt` timestamp NOT NULL DEFAULT (now()),
	CONSTRAINT `application_documents_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `approvals` (
	`id` int AUTO_INCREMENT NOT NULL,
	`requestId` int NOT NULL,
	`approverId` int NOT NULL,
	`stage` enum('manager','finance','executive','board') NOT NULL,
	`decision` enum('pending','approve','reject','override','defer') NOT NULL DEFAULT 'pending',
	`comment` text,
	`decidedAt` timestamp,
	`signatureHash` varchar(255),
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	CONSTRAINT `approvals_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `artist_revenue_streams` (
	`id` int AUTO_INCREMENT NOT NULL,
	`artistId` int NOT NULL,
	`revenueType` enum('royalty','performance_fee','teaching_fee','commission','licensing_fee','merchandise','streaming','sync_license','nft_sale','nft_royalty','grant','sponsorship') NOT NULL,
	`productionId` int,
	`bookingId` int,
	`grossAmount` decimal(12,2) NOT NULL,
	`companyShare` decimal(12,2) NOT NULL,
	`artistShare` decimal(12,2) NOT NULL,
	`status` enum('pending','approved','paid','disputed') NOT NULL DEFAULT 'pending',
	`paidAt` timestamp,
	`paymentMethod` varchar(100),
	`paymentReference` varchar(255),
	`description` text,
	`periodStart` timestamp,
	`periodEnd` timestamp,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	CONSTRAINT `artist_revenue_streams_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `artist_training_progress` (
	`id` int AUTO_INCREMENT NOT NULL,
	`artistId` int NOT NULL,
	`programId` int NOT NULL,
	`status` enum('enrolled','in_progress','completed','dropped') NOT NULL DEFAULT 'enrolled',
	`progressPercentage` int NOT NULL DEFAULT 0,
	`moduleProgress` json,
	`enrolledAt` timestamp NOT NULL DEFAULT (now()),
	`startedAt` timestamp,
	`completedAt` timestamp,
	`finalScore` int,
	`certificateIssued` boolean NOT NULL DEFAULT false,
	`certificateUrl` text,
	`tokensAwarded` int NOT NULL DEFAULT 0,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `artist_training_progress_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `assets` (
	`id` int AUTO_INCREMENT NOT NULL,
	`assetType` enum('laptop','server','monitor','sat_phone','hotspot','vehicle','pod','license','furniture','other') NOT NULL,
	`makeModel` varchar(255),
	`serialOrVin` varchar(255),
	`ownerEntity` enum('trust','business','academy','subsidiary') NOT NULL DEFAULT 'trust',
	`ownerEntityId` int,
	`assignedToUserId` int,
	`assignedToSiteId` int,
	`assignedAt` timestamp,
	`purchaseDate` timestamp,
	`purchasePrice` decimal(20,2),
	`warrantyExpiry` timestamp,
	`maintenanceIntervalDays` int,
	`lastMaintenanceDate` timestamp,
	`assetStatus` enum('in_stock','assigned','maintenance','retired','disposed') NOT NULL DEFAULT 'in_stock',
	`ledgerRef` varchar(255),
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `assets_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `audit_events` (
	`id` int AUTO_INCREMENT NOT NULL,
	`auditType` enum('quarterly_house','annual_system','lineage_seven_year','corrective','external_witness') NOT NULL,
	`houseId` int,
	`auditPeriodStart` timestamp NOT NULL,
	`auditPeriodEnd` timestamp NOT NULL,
	`totalInflow` decimal(20,2),
	`totalOutflow` decimal(20,2),
	`giftTokenDistribution` decimal(20,2),
	`rootReserveLevel` decimal(20,2),
	`sustainabilityScore` decimal(5,2),
	`triggersActivated` json,
	`correctiveActionsRequired` boolean NOT NULL DEFAULT false,
	`correctiveActionsCompleted` boolean NOT NULL DEFAULT false,
	`flameOfAccountPerformed` boolean NOT NULL DEFAULT false,
	`custodialFlameSignature` varchar(255),
	`houseLedgerSealHash` varchar(255),
	`witnessHouseIds` json,
	`status` enum('scheduled','in_progress','completed','requires_correction') NOT NULL DEFAULT 'scheduled',
	`completedAt` timestamp,
	`blockchainHash` varchar(255),
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `audit_events_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `audit_requests` (
	`id` int AUTO_INCREMENT NOT NULL,
	`requesting_user_id` varchar(255) NOT NULL,
	`target_house_ledger_id` int NOT NULL,
	`request_reason` text NOT NULL,
	`fraud_flag_id` int,
	`audit_request_status` enum('pending','approved','denied','expired','completed') DEFAULT 'pending',
	`approved_by` varchar(255),
	`approval_notes` text,
	`access_expires_at` timestamp,
	`access_started_at` timestamp,
	`access_ended_at` timestamp,
	`created_at` timestamp DEFAULT (now()),
	`updated_at` timestamp DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `audit_requests_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `automation_rules` (
	`id` int AUTO_INCREMENT NOT NULL,
	`ruleName` varchar(255) NOT NULL,
	`ruleCode` varchar(50) NOT NULL,
	`description` text,
	`triggerEntityType` enum('house','business','property','worker','document','tax_return','restoration_case','ledger_account','payroll') NOT NULL,
	`triggerEvent` enum('created','activated','updated','stage_changed','deadline_approaching','document_uploaded','payment_received','filing_completed') NOT NULL,
	`triggerConditions` json,
	`actionType` enum('create_filing_task','send_notification','log_to_ledger','update_stage','generate_document','schedule_reminder','api_call') NOT NULL,
	`actionConfig` json,
	`priority` int DEFAULT 5,
	`isActive` boolean DEFAULT true,
	`lastTriggered` timestamp,
	`triggerCount` int DEFAULT 0,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `automation_rules_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `blockchain_networks` (
	`id` int AUTO_INCREMENT NOT NULL,
	`network_code` varchar(50) NOT NULL,
	`network_name` varchar(255) NOT NULL,
	`chain_id` varchar(100),
	`network_type` enum('mainnet','testnet','devnet','private') NOT NULL DEFAULT 'mainnet',
	`protocol_type` enum('bitcoin','ethereum','solana','cosmos','polkadot','cardano','ripple','stellar','tron','avalanche','polygon','arbitrum','optimism','base','luvchain','custom') NOT NULL,
	`rpc_endpoint` varchar(500),
	`ws_endpoint` varchar(500),
	`explorer_url` varchar(500),
	`native_currency` varchar(50) NOT NULL,
	`native_currency_decimals` int NOT NULL DEFAULT 18,
	`native_currency_symbol` varchar(20) NOT NULL,
	`average_block_time` int,
	`confirmations_required` int NOT NULL DEFAULT 1,
	`is_active` boolean NOT NULL DEFAULT true,
	`is_testnet` boolean NOT NULL DEFAULT false,
	`created_at` timestamp NOT NULL DEFAULT (now()),
	`updated_at` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `blockchain_networks_id` PRIMARY KEY(`id`),
	CONSTRAINT `blockchain_networks_network_code_unique` UNIQUE(`network_code`)
);
--> statement-breakpoint
CREATE TABLE `board_meeting_attendance` (
	`id` int AUTO_INCREMENT NOT NULL,
	`meetingId` int NOT NULL,
	`memberId` int NOT NULL,
	`status` enum('invited','confirmed','attended','absent','excused') NOT NULL DEFAULT 'invited',
	`checkedInAt` timestamp,
	`notes` text,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	CONSTRAINT `board_meeting_attendance_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `board_members` (
	`id` int AUTO_INCREMENT NOT NULL,
	`positionId` int NOT NULL,
	`userId` int,
	`name` varchar(255) NOT NULL,
	`email` varchar(320),
	`phone` varchar(20),
	`status` enum('active','pending','resigned','removed') NOT NULL DEFAULT 'active',
	`appointedAt` timestamp NOT NULL DEFAULT (now()),
	`appointedBy` int,
	`termStartDate` timestamp,
	`termEndDate` timestamp,
	`resignedAt` timestamp,
	`signatureOnFile` text,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `board_members_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `board_positions` (
	`id` int AUTO_INCREMENT NOT NULL,
	`entityId` int NOT NULL,
	`title` varchar(100) NOT NULL,
	`description` text,
	`responsibilities` json,
	`votingRights` boolean NOT NULL DEFAULT true,
	`signatureAuthority` boolean NOT NULL DEFAULT false,
	`maxSignatureAmount` decimal(15,2),
	`isOfficer` boolean NOT NULL DEFAULT false,
	`sortOrder` int NOT NULL DEFAULT 0,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `board_positions_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `board_resolution_votes` (
	`id` int AUTO_INCREMENT NOT NULL,
	`resolutionId` int NOT NULL,
	`memberId` int NOT NULL,
	`vote` enum('approve','reject','abstain') NOT NULL,
	`votedAt` timestamp NOT NULL DEFAULT (now()),
	`comments` text,
	CONSTRAINT `board_resolution_votes_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `book_annotations` (
	`id` int AUTO_INCREMENT NOT NULL,
	`userId` int NOT NULL,
	`bookId` int NOT NULL,
	`readingSessionId` int,
	`chapterNumber` int,
	`pageNumber` int,
	`startPosition` int,
	`endPosition` int,
	`annotationType` enum('highlight','note','bookmark','question') NOT NULL,
	`highlightedText` text,
	`noteContent` text,
	`highlightColor` varchar(20) DEFAULT 'yellow',
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `book_annotations_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `book_discussions` (
	`id` int AUTO_INCREMENT NOT NULL,
	`userId` int NOT NULL,
	`bookId` int NOT NULL,
	`readingSessionId` int,
	`studentProfileId` int,
	`chapterNumber` int,
	`pageNumber` int,
	`discussionType` enum('comprehension','analysis','socratic','vocabulary','free_form') NOT NULL,
	`messages` json,
	`messageCount` int DEFAULT 0,
	`discussionQuality` enum('surface','developing','proficient','advanced'),
	`qualityScore` int,
	`keyInsights` json,
	`isRequired` boolean DEFAULT false,
	`requirementMet` boolean DEFAULT false,
	`hasRecording` boolean DEFAULT false,
	`recordingUrl` text,
	`recordingDurationSeconds` int,
	`transcription` text,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `book_discussions_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `business_plan_sections` (
	`id` int AUTO_INCREMENT NOT NULL,
	`businessPlanId` int NOT NULL,
	`sectionType` enum('executive_summary','company_description','products_services','market_analysis','marketing_strategy','operations_plan','management_team','financial_projections','funding_request','appendix') NOT NULL,
	`sectionTitle` varchar(255) NOT NULL,
	`content` text,
	`isComplete` boolean DEFAULT false,
	`completedAt` timestamp,
	`sortOrder` int DEFAULT 0,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `business_plan_sections_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `business_plans` (
	`id` int AUTO_INCREMENT NOT NULL,
	`entityType` enum('llc','corporation','trust','nonprofit_508','nonprofit_501c3','collective','sole_proprietorship') NOT NULL,
	`entityName` varchar(255) NOT NULL,
	`houseId` int,
	`createdByUserId` int NOT NULL,
	`missionStatement` text,
	`visionStatement` text,
	`organizationDescription` text,
	`yearFounded` int,
	`productsServices` text,
	`uniqueValueProposition` text,
	`targetMarket` text,
	`marketSize` varchar(255),
	`competitiveAdvantage` text,
	`teamSize` int,
	`teamDescription` text,
	`keyPersonnel` json,
	`startupCosts` decimal(14,2),
	`monthlyOperatingCosts` decimal(14,2),
	`projectedRevenueYear1` decimal(14,2),
	`projectedRevenueYear2` decimal(14,2),
	`projectedRevenueYear3` decimal(14,2),
	`breakEvenTimeline` varchar(100),
	`fundingNeeded` decimal(14,2),
	`fundingPurpose` text,
	`fundingSources` json,
	`shortTermGoals` json,
	`longTermGoals` json,
	`milestones` json,
	`socialImpact` text,
	`communityBenefit` text,
	`measurableOutcomes` json,
	`status` enum('draft','in_progress','completed','approved','archived') NOT NULL DEFAULT 'draft',
	`completedAt` timestamp,
	`approvedAt` timestamp,
	`approvedBy` int,
	`version` int DEFAULT 1,
	`previousVersionId` int,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `business_plans_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `business_positions` (
	`id` int AUTO_INCREMENT NOT NULL,
	`businessEntityId` int NOT NULL,
	`houseId` int NOT NULL,
	`title` varchar(255) NOT NULL,
	`department` varchar(100),
	`description` text,
	`responsibilities` json,
	`classificationType` enum('w2_employee','w2_officer','1099_contractor','k1_member','volunteer','board_member') NOT NULL,
	`employmentType` enum('full_time','part_time','seasonal','temporary'),
	`exemptionStatus` enum('exempt','non_exempt') DEFAULT 'non_exempt',
	`compensationType` enum('salary','hourly','commission','fee','guaranteed_payment','profit_share','stipend','unpaid') NOT NULL,
	`salaryAmount` decimal(12,2),
	`hourlyRate` decimal(8,2),
	`commissionRate` decimal(5,2),
	`payFrequency` enum('weekly','biweekly','semimonthly','monthly','per_project') DEFAULT 'biweekly',
	`benefitsEligible` boolean DEFAULT false,
	`requiresBackgroundCheck` boolean DEFAULT false,
	`requiresCourseCompletion` boolean DEFAULT false,
	`requiredCourseId` int,
	`linkedSimulatorId` int,
	`status` enum('open','filled','closed') DEFAULT 'open',
	`maxHolders` int DEFAULT 1,
	`currentHolders` int DEFAULT 0,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `business_positions_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `calendar_events` (
	`id` int AUTO_INCREMENT NOT NULL,
	`title` varchar(255) NOT NULL,
	`description` text,
	`eventType` enum('team_meeting','department_meeting','all_hands','training','planning','one_on_one','external','other') NOT NULL DEFAULT 'team_meeting',
	`startTime` timestamp NOT NULL,
	`endTime` timestamp NOT NULL,
	`timezone` varchar(50) NOT NULL DEFAULT 'America/Chicago',
	`location` varchar(255),
	`meetingLink` varchar(500),
	`isRecurring` boolean NOT NULL DEFAULT false,
	`recurrenceRule` varchar(255),
	`recurrenceParentId` int,
	`departmentId` int,
	`createdBy` int NOT NULL,
	`isMandatory` boolean NOT NULL DEFAULT true,
	`status` enum('scheduled','cancelled','completed') NOT NULL DEFAULT 'scheduled',
	`notes` text,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `calendar_events_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `career_interest_submissions` (
	`id` int AUTO_INCREMENT NOT NULL,
	`firstName` varchar(100) NOT NULL,
	`lastName` varchar(100) NOT NULL,
	`email` varchar(255) NOT NULL,
	`phone` varchar(20),
	`positionId` varchar(50) NOT NULL,
	`positionTitle` varchar(100) NOT NULL,
	`entityName` varchar(255) NOT NULL,
	`currentRole` varchar(100),
	`yearsExperience` varchar(20),
	`relevantSkills` text,
	`whyInterested` text,
	`resumeUrl` varchar(500),
	`coverLetterUrl` varchar(500),
	`status` enum('new','reviewed','contacted','interviewing','hired','declined') NOT NULL DEFAULT 'new',
	`reviewedBy` int,
	`reviewedAt` timestamp,
	`notes` text,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `career_interest_submissions_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `champion_nfts` (
	`id` int AUTO_INCREMENT NOT NULL,
	`ownerId` int NOT NULL,
	`ownerWalletAddress` varchar(255),
	`tokenId` varchar(100) NOT NULL,
	`contractAddress` varchar(255) NOT NULL,
	`transactionHash` varchar(255) NOT NULL,
	`nftType` enum('platinum_achievement','leaderboard_champion','tournament_winner','special_event','founder') NOT NULL,
	`achievementId` int,
	`gameType` varchar(50),
	`leaderboardPeriod` varchar(50),
	`name` varchar(255) NOT NULL,
	`description` text,
	`imageUrl` text,
	`animationUrl` text,
	`externalUrl` text,
	`attributes` json,
	`rarity` enum('common','uncommon','rare','epic','legendary') NOT NULL DEFAULT 'rare',
	`edition` int DEFAULT 1,
	`maxEditions` int,
	`chainId` varchar(50) NOT NULL DEFAULT 'luvchain',
	`blockNumber` int,
	`mintedAt` timestamp NOT NULL DEFAULT (now()),
	`previousOwners` json,
	`lastTransferAt` timestamp,
	`nftStatus` enum('minted','transferred','burned') NOT NULL DEFAULT 'minted',
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `champion_nfts_id` PRIMARY KEY(`id`),
	CONSTRAINT `champion_nfts_tokenId_unique` UNIQUE(`tokenId`)
);
--> statement-breakpoint
CREATE TABLE `change_orders` (
	`id` int AUTO_INCREMENT NOT NULL,
	`projectId` int NOT NULL,
	`changeNumber` varchar(50) NOT NULL,
	`title` varchar(255) NOT NULL,
	`description` text NOT NULL,
	`reason` text NOT NULL,
	`impactType` varchar(50) NOT NULL,
	`scheduleImpactDays` int DEFAULT 0,
	`costImpact` decimal(15,2) DEFAULT '0',
	`status` varchar(50) NOT NULL DEFAULT 'draft',
	`requestedBy` int,
	`approvedBy` int,
	`requestedDate` timestamp DEFAULT (now()),
	`approvedDate` timestamp,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `change_orders_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `changelog_entries` (
	`id` int AUTO_INCREMENT NOT NULL,
	`version` varchar(20) NOT NULL,
	`title` varchar(255) NOT NULL,
	`description` text,
	`changeType` enum('feature','improvement','fix','security','breaking') NOT NULL DEFAULT 'feature',
	`category` varchar(100),
	`highlights` json,
	`releaseDate` timestamp NOT NULL DEFAULT (now()),
	`isPublished` boolean NOT NULL DEFAULT false,
	`isMajor` boolean NOT NULL DEFAULT false,
	`createdBy` int,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `changelog_entries_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `changelog_user_views` (
	`id` int AUTO_INCREMENT NOT NULL,
	`userId` int NOT NULL,
	`changelogId` int NOT NULL,
	`viewedAt` timestamp NOT NULL DEFAULT (now()),
	`dismissed` boolean NOT NULL DEFAULT false,
	`dontShowAgain` boolean NOT NULL DEFAULT false,
	CONSTRAINT `changelog_user_views_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `charge_code_budgets` (
	`id` int AUTO_INCREMENT NOT NULL,
	`chargeCodeId` int NOT NULL,
	`fiscalYear` int NOT NULL,
	`fiscalPeriod` int,
	`budgetedHours` decimal(10,2) NOT NULL,
	`budgetedAmount` decimal(15,2) NOT NULL,
	`actualHours` decimal(10,2) DEFAULT '0.00',
	`actualAmount` decimal(15,2) DEFAULT '0.00',
	`variance` decimal(15,2) DEFAULT '0.00',
	`lastUpdated` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `charge_code_budgets_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `charge_codes` (
	`id` int AUTO_INCREMENT NOT NULL,
	`code` varchar(50) NOT NULL,
	`name` varchar(255) NOT NULL,
	`description` text,
	`fundingSourceId` int,
	`projectId` int,
	`departmentId` int,
	`entityId` int,
	`budgetedHours` decimal(10,2),
	`hourlyRate` decimal(10,2),
	`isActive` boolean NOT NULL DEFAULT true,
	`isBillable` boolean NOT NULL DEFAULT true,
	`requiresApproval` boolean NOT NULL DEFAULT true,
	`allowOvertime` boolean NOT NULL DEFAULT false,
	`startDate` timestamp,
	`endDate` timestamp,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `charge_codes_id` PRIMARY KEY(`id`),
	CONSTRAINT `charge_codes_code_unique` UNIQUE(`code`)
);
--> statement-breakpoint
CREATE TABLE `chat_attachments` (
	`id` int AUTO_INCREMENT NOT NULL,
	`messageId` int NOT NULL,
	`chatId` int NOT NULL,
	`uploaderId` int NOT NULL,
	`fileName` varchar(255) NOT NULL,
	`fileType` varchar(100) NOT NULL,
	`fileSize` int NOT NULL,
	`mimeType` varchar(100),
	`storageKey` varchar(500) NOT NULL,
	`storageUrl` text NOT NULL,
	`thumbnailUrl` text,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	CONSTRAINT `chat_attachments_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `chat_messages` (
	`id` int AUTO_INCREMENT NOT NULL,
	`chatId` int NOT NULL,
	`senderId` int NOT NULL,
	`content` text NOT NULL,
	`contentType` enum('text','html','markdown','system') NOT NULL DEFAULT 'text',
	`replyToId` int,
	`threadId` int,
	`hasAttachments` boolean DEFAULT false,
	`isEdited` boolean DEFAULT false,
	`editedAt` timestamp,
	`isDeleted` boolean DEFAULT false,
	`deletedAt` timestamp,
	`mentions` json,
	`reactions` json,
	`metadata` json,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	CONSTRAINT `chat_messages_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `chat_participants` (
	`id` int AUTO_INCREMENT NOT NULL,
	`chatId` int NOT NULL,
	`userId` int NOT NULL,
	`role` enum('owner','admin','member') NOT NULL DEFAULT 'member',
	`isMuted` boolean DEFAULT false,
	`isPinned` boolean DEFAULT false,
	`lastReadAt` timestamp,
	`lastReadMessageId` int,
	`unreadCount` int DEFAULT 0,
	`notificationLevel` enum('all','mentions','none') DEFAULT 'all',
	`joinedAt` timestamp NOT NULL DEFAULT (now()),
	`leftAt` timestamp,
	CONSTRAINT `chat_participants_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `chats` (
	`id` int AUTO_INCREMENT NOT NULL,
	`chatType` enum('direct','group','channel','meeting') NOT NULL DEFAULT 'direct',
	`name` varchar(255),
	`description` text,
	`avatarUrl` text,
	`meetingId` int,
	`isArchived` boolean DEFAULT false,
	`isPinned` boolean DEFAULT false,
	`createdById` int NOT NULL,
	`lastMessageAt` timestamp,
	`lastMessagePreview` varchar(255),
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `chats_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `community_funds` (
	`id` int AUTO_INCREMENT NOT NULL,
	`houseId` int NOT NULL,
	`fundName` varchar(255) NOT NULL,
	`fundCode` varchar(50) NOT NULL,
	`fundType` enum('land_acquisition','education','emergency','business_development','cultural_preservation','discretionary','custom') NOT NULL,
	`description` text,
	`allocationPercentage` decimal(5,2) NOT NULL,
	`currentBalance` decimal(18,2) DEFAULT '0',
	`totalContributions` decimal(18,2) DEFAULT '0',
	`totalDisbursements` decimal(18,2) DEFAULT '0',
	`minimumBalance` decimal(18,2) DEFAULT '0',
	`requiresApproval` boolean DEFAULT true,
	`approvalThreshold` decimal(18,2),
	`isActive` boolean DEFAULT true,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `community_funds_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `company_users` (
	`id` int AUTO_INCREMENT NOT NULL,
	`companyId` int NOT NULL,
	`userId` int,
	`email` varchar(255) NOT NULL,
	`firstName` varchar(100),
	`lastName` varchar(100),
	`companyUserRole` enum('owner','admin','manager','staff','readonly','billing') NOT NULL DEFAULT 'staff',
	`permissions` json,
	`companyUserStatus` enum('invited','active','suspended','removed') NOT NULL DEFAULT 'invited',
	`invitedAt` timestamp,
	`invitedBy` int,
	`inviteToken` varchar(100),
	`inviteExpiresAt` timestamp,
	`acceptedAt` timestamp,
	`lastActiveAt` timestamp,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `company_users_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `compliance_tasks` (
	`id` int AUTO_INCREMENT NOT NULL,
	`businessEntityId` int,
	`houseId` int NOT NULL,
	`positionHolderId` int,
	`taskType` enum('payroll_tax_deposit','quarterly_941','annual_940','w2_filing','1099_filing','k1_filing','state_tax_deposit','state_quarterly','annual_report','business_license','i9_reverification','workers_comp_audit','benefits_enrollment','performance_review','custom') NOT NULL,
	`taskName` varchar(255) NOT NULL,
	`description` text,
	`dueDate` timestamp NOT NULL,
	`reminderDate` timestamp,
	`isRecurring` boolean DEFAULT false,
	`recurrencePattern` enum('weekly','biweekly','monthly','quarterly','annually'),
	`assignedToUserId` int,
	`completedAt` timestamp,
	`completedByUserId` int,
	`completionNotes` text,
	`status` enum('upcoming','due_soon','overdue','completed','skipped') DEFAULT 'upcoming',
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `compliance_tasks_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `comprehension_quizzes` (
	`id` int AUTO_INCREMENT NOT NULL,
	`bookId` int NOT NULL,
	`title` varchar(255) NOT NULL,
	`chapterNumber` int,
	`quizType` enum('chapter','midpoint','final','vocabulary') NOT NULL,
	`questions` json,
	`questionCount` int DEFAULT 0,
	`passingScore` int DEFAULT 70,
	`quizDifficulty` enum('basic','intermediate','advanced') DEFAULT 'intermediate',
	`isActive` boolean NOT NULL DEFAULT true,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `comprehension_quizzes_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `contact_submissions` (
	`id` int AUTO_INCREMENT NOT NULL,
	`name` varchar(100) NOT NULL,
	`email` varchar(255) NOT NULL,
	`phone` varchar(20),
	`subject` varchar(200),
	`message` text NOT NULL,
	`source` varchar(50) DEFAULT 'landing_page',
	`status` enum('new','read','replied','archived') NOT NULL DEFAULT 'new',
	`repliedAt` timestamp,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	CONSTRAINT `contact_submissions_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `contract_amendments` (
	`id` int AUTO_INCREMENT NOT NULL,
	`amendmentNumber` varchar(50) NOT NULL,
	`contractId` int NOT NULL,
	`sowId` int,
	`title` varchar(500) NOT NULL,
	`description` text,
	`changeType` enum('scope_change','rate_change','term_extension','termination','other') NOT NULL,
	`previousValue` text,
	`newValue` text,
	`effectiveDate` timestamp,
	`approvedBy` int,
	`approvedAt` timestamp,
	`status` enum('draft','pending_approval','approved','rejected') NOT NULL DEFAULT 'draft',
	`createdBy` int NOT NULL,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `contract_amendments_id` PRIMARY KEY(`id`),
	CONSTRAINT `contract_amendments_amendmentNumber_unique` UNIQUE(`amendmentNumber`)
);
--> statement-breakpoint
CREATE TABLE `contract_milestones` (
	`id` int AUTO_INCREMENT NOT NULL,
	`contractId` int NOT NULL,
	`title` varchar(255) NOT NULL,
	`description` text,
	`milestoneType` enum('signing','effective_date','payment_due','deliverable_due','review_date','renewal_notice','expiration','termination','amendment','custom') NOT NULL,
	`dueDate` timestamp NOT NULL,
	`completedDate` timestamp,
	`status` enum('pending','completed','overdue','cancelled') NOT NULL DEFAULT 'pending',
	`reminderDays` int DEFAULT 7,
	`reminderSent` boolean NOT NULL DEFAULT false,
	`luvLedgerRecorded` boolean NOT NULL DEFAULT false,
	`luvLedgerTransactionId` int,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `contract_milestones_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `contract_negotiations` (
	`id` int AUTO_INCREMENT NOT NULL,
	`userId` int NOT NULL,
	`contractTitle` varchar(255) NOT NULL,
	`contractType` enum('employment','service','vendor','partnership','lease','nda','licensing','consulting','other') NOT NULL,
	`ourParty` varchar(255) NOT NULL,
	`counterparty` varchar(255) NOT NULL,
	`counterpartyContact` varchar(255),
	`contractValue` decimal(15,2),
	`contractDuration` varchar(100),
	`startDate` timestamp,
	`endDate` timestamp,
	`status` enum('initial_review','counter_proposed','under_negotiation','final_review','accepted','rejected','withdrawn','expired') NOT NULL DEFAULT 'initial_review',
	`keyTerms` json,
	`aiAnalysisId` varchar(100),
	`riskScore` int,
	`leverageScore` int,
	`walkAwayThreshold` text,
	`outcome` enum('won','lost','compromised','pending') DEFAULT 'pending',
	`outcomeNotes` text,
	`finalContractUrl` varchar(500),
	`negotiationStartedAt` timestamp NOT NULL DEFAULT (now()),
	`lastActivityAt` timestamp NOT NULL DEFAULT (now()),
	`completedAt` timestamp,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `contract_negotiations_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `contract_templates` (
	`id` int AUTO_INCREMENT NOT NULL,
	`name` varchar(255) NOT NULL,
	`description` text,
	`contractType` enum('vendor_agreement','partnership_agreement','service_agreement','licensing_agreement','lease_agreement','employment_contract','contractor_agreement','nda','operating_agreement','trust_affiliation','distribution_agreement','franchise_agreement','joint_venture','loan_agreement','insurance_policy','other') NOT NULL,
	`templateUrl` text,
	`templateContent` text,
	`variables` json,
	`usageCount` int NOT NULL DEFAULT 0,
	`lastUsedAt` timestamp,
	`isActive` boolean NOT NULL DEFAULT true,
	`createdBy` int NOT NULL,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `contract_templates_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `contractor_businesses` (
	`id` int AUTO_INCREMENT NOT NULL,
	`transitionId` int NOT NULL,
	`contractorId` int NOT NULL,
	`contractorName` varchar(255) NOT NULL,
	`businessName` varchar(255) NOT NULL,
	`businessType` varchar(100),
	`ein` varchar(20),
	`stateOfFormation` varchar(50),
	`formationDate` timestamp,
	`status` enum('active','inactive','dissolved') NOT NULL DEFAULT 'active',
	`annualRevenue` decimal(15,2),
	`revenueYear` int,
	`employeesHired` int DEFAULT 0,
	`platformSubscription` enum('none','basic','professional','enterprise') NOT NULL DEFAULT 'none',
	`platformActiveDate` timestamp,
	`activeContractWithLaws` boolean NOT NULL DEFAULT false,
	`lastContractDate` timestamp,
	`totalContractValue` decimal(15,2),
	`notes` text,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `contractor_businesses_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `contractor_invoices` (
	`id` int AUTO_INCREMENT NOT NULL,
	`workerId` int NOT NULL,
	`invoiceNumber` varchar(50) NOT NULL,
	`periodStart` timestamp NOT NULL,
	`periodEnd` timestamp NOT NULL,
	`totalHours` decimal(8,2) NOT NULL,
	`totalAmount` decimal(15,2) NOT NULL,
	`status` enum('draft','submitted','under_review','approved','paid','disputed') NOT NULL DEFAULT 'draft',
	`submittedAt` timestamp,
	`approvedBy` int,
	`approvedAt` timestamp,
	`paidAt` timestamp,
	`paymentReference` varchar(100),
	`notes` text,
	`attachmentUrl` text,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `contractor_invoices_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `contractor_transitions` (
	`id` int AUTO_INCREMENT NOT NULL,
	`employeeId` int NOT NULL,
	`employeeName` varchar(255) NOT NULL,
	`initiatedBy` int NOT NULL,
	`initiatedByName` varchar(255),
	`phase` enum('initiated','training_assigned','training_in_progress','training_completed','entity_formation','entity_verified','contract_pending','contract_signed','completed','cancelled') NOT NULL DEFAULT 'initiated',
	`trainingModuleId` int,
	`trainingStartDate` timestamp,
	`trainingCompletionDate` timestamp,
	`trainingScore` int,
	`certificationIssued` boolean NOT NULL DEFAULT false,
	`businessEntityId` int,
	`entityName` varchar(255),
	`entityType` varchar(100),
	`entityFormationDate` timestamp,
	`einObtained` boolean NOT NULL DEFAULT false,
	`businessBankSetup` boolean NOT NULL DEFAULT false,
	`contractId` int,
	`contractSignedDate` timestamp,
	`contractTerms` json,
	`status` enum('active','completed','cancelled','on_hold') NOT NULL DEFAULT 'active',
	`completedDate` timestamp,
	`cancellationReason` text,
	`notes` text,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `contractor_transitions_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `contracts` (
	`id` int AUTO_INCREMENT NOT NULL,
	`contractNumber` varchar(50) NOT NULL,
	`contractType` enum('msa','sow','nda','employment','consulting','vendor','partnership','other') NOT NULL,
	`title` varchar(500) NOT NULL,
	`description` text,
	`contractorId` int,
	`contractorBusinessId` int,
	`clientEntityId` int,
	`effectiveDate` timestamp,
	`expirationDate` timestamp,
	`autoRenew` boolean NOT NULL DEFAULT false,
	`renewalTermMonths` int DEFAULT 12,
	`totalValue` decimal(15,2),
	`paymentTerms` varchar(100),
	`billingFrequency` enum('one_time','weekly','bi_weekly','monthly','quarterly','annually') DEFAULT 'monthly',
	`retainerAmount` decimal(15,2),
	`hourlyRate` decimal(10,2),
	`terminationNoticeDays` int DEFAULT 30,
	`nonCompeteMonths` int DEFAULT 0,
	`ipAssignment` boolean DEFAULT true,
	`confidentialityRequired` boolean DEFAULT true,
	`insuranceRequired` boolean DEFAULT false,
	`insuranceMinimum` decimal(15,2),
	`signedByContractor` boolean DEFAULT false,
	`signedByContractorAt` timestamp,
	`signedByClient` boolean DEFAULT false,
	`signedByClientAt` timestamp,
	`signedByClientName` varchar(255),
	`status` enum('draft','pending_review','pending_signature','active','expired','terminated','renewed') NOT NULL DEFAULT 'draft',
	`notes` text,
	`createdBy` int NOT NULL,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `contracts_id` PRIMARY KEY(`id`),
	CONSTRAINT `contracts_contractNumber_unique` UNIQUE(`contractNumber`)
);
--> statement-breakpoint
CREATE TABLE `course_completion_certificates` (
	`id` int AUTO_INCREMENT NOT NULL,
	`userId` int NOT NULL,
	`walletId` int NOT NULL,
	`courseType` enum('business_setup','business_plan','grant_writing','financial_literacy','trust_formation','contracts','blockchain_crypto','operations') NOT NULL,
	`certificateHash` varchar(66) NOT NULL,
	`transactionHash` varchar(66) NOT NULL,
	`tokenId` int NOT NULL,
	`title` varchar(255) NOT NULL,
	`description` text,
	`issuerName` varchar(255) NOT NULL DEFAULT 'L.A.W.S. Collective',
	`recipientName` varchar(255) NOT NULL,
	`completionDate` timestamp NOT NULL,
	`tokensEarned` int NOT NULL DEFAULT 0,
	`courseData` json,
	`metadata` json,
	`imageUrl` varchar(500),
	`verificationUrl` varchar(500),
	`isRevoked` boolean NOT NULL DEFAULT false,
	`revokedAt` timestamp,
	`revokedReason` text,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	CONSTRAINT `course_completion_certificates_id` PRIMARY KEY(`id`),
	CONSTRAINT `course_completion_certificates_certificateHash_unique` UNIQUE(`certificateHash`)
);
--> statement-breakpoint
CREATE TABLE `course_progress` (
	`id` int AUTO_INCREMENT NOT NULL,
	`userId` int NOT NULL,
	`courseType` enum('business_setup','business_plan','grant_writing','financial_literacy','trust_formation','contracts','blockchain_crypto','operations') NOT NULL,
	`currentModule` int NOT NULL DEFAULT 0,
	`totalModules` int NOT NULL,
	`completedModules` json,
	`worksheetData` json,
	`quizScores` json,
	`tokensEarned` int NOT NULL DEFAULT 0,
	`status` enum('not_started','in_progress','completed') NOT NULL DEFAULT 'not_started',
	`startedAt` timestamp,
	`completedAt` timestamp,
	`certificateId` int,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `course_progress_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `course_recommendations` (
	`id` int AUTO_INCREMENT NOT NULL,
	`assessmentId` int NOT NULL,
	`studentProfileId` int NOT NULL,
	`courseId` int NOT NULL,
	`priority` int NOT NULL DEFAULT 1,
	`recommendationReason` text,
	`alignedStrengths` json,
	`addressedGrowthAreas` json,
	`confidenceScore` int DEFAULT 80,
	`parentDecision` enum('pending','approved','rejected','modified') NOT NULL DEFAULT 'pending',
	`parentDecisionAt` timestamp,
	`parentNotes` text,
	`alternativeCourseId` int,
	`enrolled` boolean NOT NULL DEFAULT false,
	`enrolledAt` timestamp,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `course_recommendations_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `creative_artists` (
	`id` int AUTO_INCREMENT NOT NULL,
	`fullName` varchar(255) NOT NULL,
	`stageName` varchar(255),
	`email` varchar(320),
	`phone` varchar(50),
	`artistType` enum('performer','producer','designer','animator','hybrid') NOT NULL,
	`primaryEntity` enum('real_eye_nation','laws_collective','both') NOT NULL DEFAULT 'real_eye_nation',
	`specializations` json,
	`familyMemberId` int,
	`houseId` varchar(50),
	`specialistTrackId` int,
	`status` enum('applicant','trainee','active','senior','master','emeritus','inactive') NOT NULL DEFAULT 'applicant',
	`experienceLevel` enum('entry','intermediate','advanced','expert','master') NOT NULL DEFAULT 'entry',
	`businessFundamentalsCompleted` boolean NOT NULL DEFAULT false,
	`financialLiteracyCompleted` boolean NOT NULL DEFAULT false,
	`ipLicensingTrainingCompleted` boolean NOT NULL DEFAULT false,
	`portfolioUrl` text,
	`demoReelUrl` text,
	`bio` text,
	`contractType` enum('employee','contractor','freelance','intern') NOT NULL DEFAULT 'contractor',
	`minimumGuarantee` decimal(10,2),
	`revenueSharePercentage` decimal(5,2) DEFAULT '70.00',
	`tokenBalance` int NOT NULL DEFAULT 0,
	`lifetimeTokensEarned` int NOT NULL DEFAULT 0,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `creative_artists_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `creative_bookings` (
	`id` int AUTO_INCREMENT NOT NULL,
	`bookingType` enum('live_performance','recording_session','photo_shoot','video_shoot','workshop','teaching','consultation','design_session','event_appearance') NOT NULL,
	`primaryArtistId` int NOT NULL,
	`additionalArtists` json,
	`eventName` varchar(255) NOT NULL,
	`eventDescription` text,
	`locationType` enum('in_person','virtual','hybrid') NOT NULL DEFAULT 'in_person',
	`venue` varchar(255),
	`address` text,
	`virtualLink` text,
	`startDateTime` timestamp NOT NULL,
	`endDateTime` timestamp NOT NULL,
	`setupTime` int,
	`clientType` enum('internal','external') NOT NULL DEFAULT 'internal',
	`clientEntityId` int,
	`clientName` varchar(255),
	`clientContact` varchar(255),
	`bookingFee` decimal(10,2),
	`depositAmount` decimal(10,2),
	`depositPaid` boolean NOT NULL DEFAULT false,
	`finalPaymentPaid` boolean NOT NULL DEFAULT false,
	`status` enum('inquiry','pending','confirmed','in_progress','completed','cancelled') NOT NULL DEFAULT 'inquiry',
	`technicalRequirements` text,
	`specialRequests` text,
	`productionId` int,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `creative_bookings_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `creative_productions` (
	`id` int AUTO_INCREMENT NOT NULL,
	`title` varchar(255) NOT NULL,
	`productionType` enum('film','video','music_track','album','podcast','live_performance','theater_production','dance_piece','spoken_word','documentary','graphic_design','brand_identity','ui_ux_design','3d_model','animation','motion_graphics','nft','digital_art','illustration','music_video','promotional_content','educational_content') NOT NULL,
	`owningEntity` enum('real_eye_nation','laws_collective','joint') NOT NULL,
	`description` text,
	`genre` varchar(100),
	`duration` int,
	`releaseDate` timestamp,
	`ipOwnership` enum('artist_full','company_full','shared_majority_artist','shared_majority_company','work_for_hire') NOT NULL DEFAULT 'shared_majority_artist',
	`copyrightRegistered` boolean NOT NULL DEFAULT false,
	`copyrightNumber` varchar(100),
	`licensingAvailable` boolean NOT NULL DEFAULT true,
	`licensingTypes` json,
	`status` enum('in_development','in_production','post_production','completed','released','archived') NOT NULL DEFAULT 'in_development',
	`productionBudget` decimal(12,2),
	`totalRevenue` decimal(12,2) DEFAULT '0.00',
	`totalRoyaltiesPaid` decimal(12,2) DEFAULT '0.00',
	`primaryAssetUrl` text,
	`thumbnailUrl` text,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `creative_productions_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `creative_training_programs` (
	`id` int AUTO_INCREMENT NOT NULL,
	`programName` varchar(255) NOT NULL,
	`programType` enum('acting','voice_acting','music_performance','music_production','dance','spoken_word','theater','film_production','graphic_design','ui_ux','3d_animation','motion_graphics','brand_development','digital_art','nft_creation','business_fundamentals','financial_literacy','ip_licensing','marketing_self','contract_negotiation') NOT NULL,
	`owningEntity` enum('real_eye_nation','laws_collective','academy') NOT NULL,
	`description` text,
	`learningObjectives` json,
	`prerequisitePrograms` json,
	`isBusinessFundamental` boolean NOT NULL DEFAULT false,
	`durationWeeks` int,
	`hoursPerWeek` int,
	`modules` json,
	`completionRequirements` json,
	`certificateAwarded` boolean NOT NULL DEFAULT true,
	`completionTokenReward` int NOT NULL DEFAULT 100,
	`status` enum('draft','active','archived') NOT NULL DEFAULT 'draft',
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `creative_training_programs_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `credential_achievements` (
	`id` int AUTO_INCREMENT NOT NULL,
	`credentialId` int NOT NULL,
	`userId` int NOT NULL,
	`achievementType` enum('game_sovereignty','academy_certification','direct_onboarding','employment_start','legacy_transfer','realm_mastery','community_contribution','mentor_status') NOT NULL,
	`achievementName` varchar(255) NOT NULL,
	`description` text,
	`pointsEarned` int NOT NULL DEFAULT 0,
	`verified` boolean NOT NULL DEFAULT false,
	`verifiedBy` int,
	`verifiedAt` timestamp,
	`metadata` json,
	`earnedAt` timestamp NOT NULL DEFAULT (now()),
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	CONSTRAINT `credential_achievements_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `curriculum_projects` (
	`id` int AUTO_INCREMENT NOT NULL,
	`title` varchar(200) NOT NULL,
	`description` text,
	`projectType` enum('course_development','assessment_design','instructor_guide','accreditation_docs','lms_setup','full_curriculum') NOT NULL,
	`scope` text,
	`deliverables` json,
	`contractorId` int,
	`contractorName` varchar(200),
	`contractorCompany` varchar(200),
	`budget` decimal(10,2),
	`amountPaid` decimal(10,2) DEFAULT '0',
	`startDate` timestamp,
	`targetEndDate` timestamp,
	`actualEndDate` timestamp,
	`status` enum('planning','contracted','in_progress','review','completed','cancelled') NOT NULL DEFAULT 'planning',
	`grantFunded` boolean NOT NULL DEFAULT false,
	`grantId` int,
	`notes` text,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `curriculum_projects_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `customer_subscriptions` (
	`id` int AUTO_INCREMENT NOT NULL,
	`customerId` int NOT NULL,
	`customerHouseId` int,
	`planId` int NOT NULL,
	`billingCycle` enum('monthly','annual') NOT NULL DEFAULT 'monthly',
	`currentPrice` decimal(10,2) NOT NULL,
	`nextBillingDate` timestamp,
	`status` enum('active','past_due','cancelled','paused','trial') NOT NULL DEFAULT 'active',
	`startedAt` timestamp NOT NULL DEFAULT (now()),
	`cancelledAt` timestamp,
	`pausedAt` timestamp,
	`trialEndsAt` timestamp,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `customer_subscriptions_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `deliverable_quality_scores` (
	`id` int AUTO_INCREMENT NOT NULL,
	`progressionId` int NOT NULL,
	`serviceOrderId` int,
	`projectId` int,
	`deliverableType` varchar(100),
	`deliverableDescription` text,
	`standardId` int NOT NULL,
	`score` decimal(10,2) NOT NULL,
	`assessedBy` int,
	`assessedAt` timestamp NOT NULL DEFAULT (now()),
	`assessmentMethod` enum('self_assessment','peer_review','supervisor_review','client_feedback','automated') NOT NULL,
	`feedback` text,
	`improvementAreas` json,
	`blockchainHash` varchar(255),
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	CONSTRAINT `deliverable_quality_scores_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `demo_mode_sessions` (
	`id` int AUTO_INCREMENT NOT NULL,
	`userId` int NOT NULL,
	`sessionName` varchar(255),
	`scenarioType` enum('house_activation','business_creation','property_acquisition','worker_onboarding','tax_filing','restoration_case','full_lifecycle') NOT NULL,
	`currentStep` int DEFAULT 0,
	`totalSteps` int,
	`stepData` json,
	`showLedgerEntries` boolean DEFAULT true,
	`showFilingProgress` boolean DEFAULT true,
	`animationSpeed` enum('slow','normal','fast') DEFAULT 'normal',
	`status` enum('active','paused','completed','cancelled') DEFAULT 'active',
	`startedAt` timestamp NOT NULL DEFAULT (now()),
	`completedAt` timestamp,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	CONSTRAINT `demo_mode_sessions_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `department_simulator_progress` (
	`id` int AUTO_INCREMENT NOT NULL,
	`userId` int NOT NULL,
	`simulatorId` varchar(50) NOT NULL,
	`moduleId` int NOT NULL,
	`questionsAnswered` int NOT NULL DEFAULT 0,
	`correctAnswers` int NOT NULL DEFAULT 0,
	`isCompleted` boolean NOT NULL DEFAULT false,
	`tokensEarned` int NOT NULL DEFAULT 0,
	`startedAt` timestamp NOT NULL DEFAULT (now()),
	`completedAt` timestamp,
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `department_simulator_progress_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `design_assets` (
	`id` int AUTO_INCREMENT NOT NULL,
	`assetName` varchar(255) NOT NULL,
	`assetType` enum('logo','icon','illustration','photo','video','animation','3d_model','font','color_palette','template','mockup','source_file','export','nft') NOT NULL,
	`projectId` int,
	`productionId` int,
	`creatorId` int,
	`fileUrl` text NOT NULL,
	`thumbnailUrl` text,
	`fileFormat` varchar(50),
	`fileSizeBytes` int,
	`dimensions` varchar(50),
	`description` text,
	`tags` json,
	`colorPalette` json,
	`licenseType` enum('internal_only','client_exclusive','royalty_free','rights_managed','creative_commons','nft_owned') NOT NULL DEFAULT 'internal_only',
	`isNft` boolean NOT NULL DEFAULT false,
	`nftContractAddress` varchar(255),
	`nftTokenId` varchar(100),
	`nftChain` varchar(50),
	`version` int NOT NULL DEFAULT 1,
	`parentAssetId` int,
	`status` enum('draft','review','approved','archived') NOT NULL DEFAULT 'draft',
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `design_assets_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `design_projects` (
	`id` int AUTO_INCREMENT NOT NULL,
	`projectName` varchar(255) NOT NULL,
	`projectType` enum('brand_identity','logo_design','marketing_materials','ui_ux_design','web_design','app_design','3d_modeling','animation','motion_graphics','video_editing','photo_editing','illustration','infographic','presentation','packaging','merchandise_design','nft_collection','ai_assisted_design') NOT NULL,
	`clientType` enum('internal','external') NOT NULL DEFAULT 'internal',
	`clientEntityId` int,
	`clientName` varchar(255),
	`leadDesignerId` int,
	`description` text,
	`requirements` text,
	`deliverables` json,
	`startDate` timestamp,
	`dueDate` timestamp,
	`completedDate` timestamp,
	`status` enum('briefing','in_progress','review','revision','approved','delivered','archived') NOT NULL DEFAULT 'briefing',
	`projectBudget` decimal(10,2),
	`billingType` enum('fixed','hourly','internal') NOT NULL DEFAULT 'internal',
	`hourlyRate` decimal(8,2),
	`hoursLogged` decimal(8,2) DEFAULT '0.00',
	`totalBilled` decimal(10,2) DEFAULT '0.00',
	`aiToolsUsed` json,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `design_projects_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `digital_avatars` (
	`id` int AUTO_INCREMENT NOT NULL,
	`user_id` int NOT NULL,
	`avatar_name` varchar(255) NOT NULL,
	`model_url` varchar(500),
	`thumbnail_url` varchar(500),
	`customization` json,
	`nft_token_id` varchar(255),
	`nft_contract_address` varchar(255),
	`nft_network_id` int,
	`is_default` boolean NOT NULL DEFAULT false,
	`is_active` boolean NOT NULL DEFAULT true,
	`created_at` timestamp NOT NULL DEFAULT (now()),
	`updated_at` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `digital_avatars_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `distribution_events` (
	`id` int AUTO_INCREMENT NOT NULL,
	`incomeEventId` int NOT NULL,
	`distributionType` enum('inter_house','intra_house','root_treasury','ancestral_treasury') NOT NULL,
	`fromHouseId` int NOT NULL,
	`toHouseId` int,
	`allocationCategory` enum('operations','inheritance','network','root_authority_reserve','circulation_pool','ancestral_treasury'),
	`amount` decimal(20,2) NOT NULL,
	`percentage` decimal(5,2) NOT NULL,
	`description` text,
	`status` enum('pending','executed','verified') NOT NULL DEFAULT 'pending',
	`executedAt` timestamp,
	`blockchainHash` varchar(255),
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	CONSTRAINT `distribution_events_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `distribution_rules` (
	`id` int AUTO_INCREMENT NOT NULL,
	`houseId` int NOT NULL,
	`ruleName` varchar(255) NOT NULL,
	`ruleType` enum('threshold','schedule','event','conditional') NOT NULL,
	`triggerCondition` json NOT NULL,
	`distributionAction` json NOT NULL,
	`priority` int NOT NULL DEFAULT 0,
	`isActive` boolean NOT NULL DEFAULT true,
	`lastTriggered` timestamp,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `distribution_rules_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `divine_stem_modules` (
	`id` int AUTO_INCREMENT NOT NULL,
	`name` varchar(150) NOT NULL,
	`slug` varchar(100) NOT NULL,
	`description` text,
	`ceremonialTitle` varchar(200),
	`iconEmoji` varchar(10),
	`category` enum('stem','ceremonial','entrepreneurial','creative','language') NOT NULL,
	`orderIndex` int NOT NULL DEFAULT 0,
	`status` enum('active','inactive','coming_soon') NOT NULL DEFAULT 'active',
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	CONSTRAINT `divine_stem_modules_id` PRIMARY KEY(`id`),
	CONSTRAINT `divine_stem_modules_slug_unique` UNIQUE(`slug`)
);
--> statement-breakpoint
CREATE TABLE `document_access` (
	`id` int AUTO_INCREMENT NOT NULL,
	`documentId` int NOT NULL,
	`userId` int,
	`entityId` int,
	`role` enum('viewer','editor','admin') NOT NULL DEFAULT 'viewer',
	`canDownload` boolean DEFAULT true,
	`canShare` boolean DEFAULT false,
	`expiresAt` timestamp,
	`grantedBy` int NOT NULL,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	CONSTRAINT `document_access_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `document_access_log` (
	`id` int AUTO_INCREMENT NOT NULL,
	`documentId` int NOT NULL,
	`userId` int NOT NULL,
	`action` enum('view','download','edit','share','delete','restore') NOT NULL,
	`ipAddress` varchar(50),
	`userAgent` varchar(500),
	`metadata` json,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	CONSTRAINT `document_access_log_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `document_field_definitions` (
	`id` int AUTO_INCREMENT NOT NULL,
	`templateId` int NOT NULL,
	`fieldCode` varchar(100) NOT NULL,
	`fieldLabel` varchar(255) NOT NULL,
	`fieldType` enum('text','number','date','checkbox','radio','select','ssn','ein','phone','email','address','currency','signature') NOT NULL,
	`pageNumber` int DEFAULT 1,
	`positionX` decimal(6,2),
	`positionY` decimal(6,2),
	`width` decimal(6,2),
	`height` decimal(6,2),
	`fontSize` int,
	`fontWeight` varchar(20),
	`textAlign` enum('left','center','right') DEFAULT 'left',
	`maxLength` int,
	`dataSource` varchar(255),
	`defaultValue` varchar(500),
	`isRequired` boolean DEFAULT false,
	`validationPattern` varchar(500),
	`validationMessage` varchar(255),
	`options` json,
	`displayOrder` int DEFAULT 0,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	CONSTRAINT `document_field_definitions_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `document_folders` (
	`id` int AUTO_INCREMENT NOT NULL,
	`ownerId` int NOT NULL,
	`entityId` int,
	`parentFolderId` int,
	`name` varchar(100) NOT NULL,
	`description` varchar(500),
	`color` varchar(20),
	`icon` varchar(50),
	`accessLevel` enum('owner_only','entity_members','authorized_users') NOT NULL DEFAULT 'owner_only',
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `document_folders_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `document_requirements` (
	`id` int AUTO_INCREMENT NOT NULL,
	`grantId` int,
	`grantType` varchar(100),
	`reqDocCategory` enum('budget','staffing','equipment','letters_of_support','legal','financial_statements','program_narrative','evaluation_plan','timeline','certificates','other') NOT NULL,
	`description` text NOT NULL,
	`isRequired` boolean NOT NULL DEFAULT true,
	`maxFileSize` int,
	`allowedFormats` json,
	`validityPeriod` int,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	CONSTRAINT `document_requirements_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `document_templates` (
	`id` int AUTO_INCREMENT NOT NULL,
	`templateCode` varchar(50) NOT NULL,
	`templateName` varchar(255) NOT NULL,
	`description` text,
	`category` enum('state_business','federal_business','federal_tax','state_tax','employment','property','trust','trademark','general_legal') NOT NULL,
	`subcategory` varchar(100),
	`jurisdiction` varchar(100),
	`jurisdictionName` varchar(255),
	`formNumber` varchar(50),
	`formRevision` varchar(20),
	`ombNumber` varchar(20),
	`pageCount` int DEFAULT 1,
	`pageSize` enum('letter','legal','a4') DEFAULT 'letter',
	`orientation` enum('portrait','landscape') DEFAULT 'portrait',
	`marginTop` decimal(4,2) DEFAULT '0.5',
	`marginBottom` decimal(4,2) DEFAULT '0.5',
	`marginLeft` decimal(4,2) DEFAULT '0.75',
	`marginRight` decimal(4,2) DEFAULT '0.75',
	`primaryFont` varchar(100) DEFAULT 'Courier',
	`fontSize` int DEFAULT 10,
	`templateHtml` text,
	`templateCss` text,
	`fieldMappings` json,
	`validationRules` json,
	`requiredFields` json,
	`filingInstructions` text,
	`filingAddress` text,
	`filingUrl` varchar(500),
	`filingFee` decimal(10,2),
	`isActive` boolean DEFAULT true,
	`effectiveDate` timestamp,
	`expirationDate` timestamp,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `document_templates_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `document_versions` (
	`id` int AUTO_INCREMENT NOT NULL,
	`documentId` int NOT NULL,
	`version` int NOT NULL,
	`content` text,
	`fileUrl` varchar(500),
	`changeNotes` text,
	`createdBy` int NOT NULL,
	`blockchainHash` varchar(100),
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	CONSTRAINT `document_versions_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `document_workflow` (
	`id` int AUTO_INCREMENT NOT NULL,
	`documentId` int NOT NULL,
	`documentType` varchar(100) NOT NULL,
	`status` enum('draft','review','pending_signature','approved','official','expired','superseded') NOT NULL DEFAULT 'draft',
	`version` int NOT NULL DEFAULT 1,
	`createdBy` int NOT NULL,
	`reviewedBy` int,
	`approvedBy` int,
	`reviewedAt` timestamp,
	`approvedAt` timestamp,
	`officialAt` timestamp,
	`expiresAt` timestamp,
	`notes` text,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `document_workflow_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `economic_health_indicators` (
	`id` int AUTO_INCREMENT NOT NULL,
	`houseId` int,
	`indicatorType` enum('liquidity_ratio','allocation_accuracy','sync_success_rate','transaction_volume','pot_balance_health','gift_sale_compliance','error_rate') NOT NULL,
	`currentValue` decimal(20,8) NOT NULL,
	`targetValue` decimal(20,8),
	`thresholdMin` decimal(20,8),
	`thresholdMax` decimal(20,8),
	`healthStatus` enum('healthy','warning','critical','unknown') NOT NULL DEFAULT 'unknown',
	`measurementPeriod` enum('hourly','daily','weekly','monthly') NOT NULL,
	`measuredAt` timestamp NOT NULL DEFAULT (now()),
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	CONSTRAINT `economic_health_indicators_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `electronic_signatures` (
	`id` int AUTO_INCREMENT NOT NULL,
	`signer_id` int NOT NULL,
	`signer_name` varchar(255) NOT NULL,
	`signer_email` varchar(255),
	`document_type` varchar(100) NOT NULL,
	`document_id` int NOT NULL,
	`document_title` varchar(500),
	`signature_data` text NOT NULL,
	`signature_hash` varchar(64) NOT NULL,
	`ip_address` varchar(45),
	`user_agent` text,
	`signed_at` timestamp NOT NULL DEFAULT (now()),
	`verification_code` varchar(32) NOT NULL,
	`is_verified` boolean NOT NULL DEFAULT true,
	`expires_at` timestamp,
	`requires_re_acknowledgment` boolean NOT NULL DEFAULT false,
	`created_at` timestamp NOT NULL DEFAULT (now()),
	CONSTRAINT `electronic_signatures_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `email_sends` (
	`id` int AUTO_INCREMENT NOT NULL,
	`userId` int NOT NULL,
	`templateId` int,
	`agentId` int,
	`recipientEmail` varchar(255) NOT NULL,
	`recipientName` varchar(100),
	`subject` varchar(200) NOT NULL,
	`status` enum('pending','sent','delivered','opened','clicked','bounced','failed') NOT NULL DEFAULT 'pending',
	`externalId` varchar(100),
	`openedAt` timestamp,
	`clickedAt` timestamp,
	`errorMessage` text,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	CONSTRAINT `email_sends_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `email_templates` (
	`id` int AUTO_INCREMENT NOT NULL,
	`userId` int NOT NULL,
	`name` varchar(100) NOT NULL,
	`subject` varchar(200) NOT NULL,
	`htmlContent` text NOT NULL,
	`textContent` text,
	`category` enum('notification','marketing','transactional','newsletter') NOT NULL DEFAULT 'notification',
	`variables` json,
	`isActive` boolean NOT NULL DEFAULT true,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `email_templates_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `employee_game_sessions` (
	`id` int AUTO_INCREMENT NOT NULL,
	`userId` int NOT NULL,
	`gameId` int,
	`gameSlug` varchar(100) NOT NULL,
	`gameName` varchar(255) NOT NULL,
	`startTime` timestamp NOT NULL,
	`endTime` timestamp,
	`durationMinutes` int NOT NULL DEFAULT 0,
	`sessionType` enum('solo_practice','team_battle','house_championship','laws_tournament','training') NOT NULL DEFAULT 'solo_practice',
	`won` boolean DEFAULT false,
	`score` int DEFAULT 0,
	`difficulty` varchar(20),
	`countsTowardRequirement` boolean DEFAULT true,
	`weekNumber` int NOT NULL,
	`weekYear` int NOT NULL,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	CONSTRAINT `employee_game_sessions_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `employee_onboarding` (
	`id` int AUTO_INCREMENT NOT NULL,
	`employeeId` int NOT NULL,
	`applicationId` int,
	`checklistId` int NOT NULL,
	`status` enum('not_started','in_progress','completed','on_hold') NOT NULL DEFAULT 'not_started',
	`startDate` timestamp,
	`targetCompletionDate` timestamp,
	`actualCompletionDate` timestamp,
	`assignedHrId` int,
	`assignedManagerId` int,
	`notes` text,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `employee_onboarding_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `employees` (
	`id` int AUTO_INCREMENT NOT NULL,
	`firstName` varchar(100) NOT NULL,
	`lastName` varchar(100) NOT NULL,
	`preferredName` varchar(100),
	`email` varchar(255) NOT NULL,
	`phone` varchar(50),
	`entityId` int NOT NULL,
	`department` varchar(100) NOT NULL,
	`jobTitle` varchar(255) NOT NULL,
	`positionLevel` enum('executive','manager','lead','coordinator','specialist','intern') NOT NULL,
	`reportsTo` int,
	`employmentType` enum('full_time','part_time','contract','intern') NOT NULL DEFAULT 'full_time',
	`workLocation` enum('remote','hybrid','on_site') NOT NULL DEFAULT 'remote',
	`startDate` timestamp NOT NULL,
	`endDate` timestamp,
	`workerType` enum('employee','contractor','volunteer') NOT NULL DEFAULT 'employee',
	`contractStartDate` timestamp,
	`contractEndDate` timestamp,
	`hourlyRate` decimal(10,2),
	`is1099` boolean DEFAULT false,
	`contractTerms` text,
	`bio` text,
	`avatarUrl` varchar(500),
	`linkedinUrl` varchar(255),
	`status` enum('active','on_leave','terminated','pending') NOT NULL DEFAULT 'active',
	`userId` int,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `employees_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `employer_tax_forms` (
	`id` int AUTO_INCREMENT NOT NULL,
	`positionHolderId` int NOT NULL,
	`businessEntityId` int NOT NULL,
	`houseId` int NOT NULL,
	`taxYear` int NOT NULL,
	`documentType` enum('w2','1099_nec','1099_misc','k1_1065','k1_1120s') NOT NULL,
	`recipientName` varchar(255) NOT NULL,
	`recipientSSN` varchar(11),
	`recipientAddress` text,
	`payerName` varchar(255) NOT NULL,
	`payerEIN` varchar(10) NOT NULL,
	`payerAddress` text,
	`wagesBox1` decimal(12,2),
	`federalWithheldBox2` decimal(12,2),
	`socialSecurityWagesBox3` decimal(12,2),
	`socialSecurityWithheldBox4` decimal(12,2),
	`medicareWagesBox5` decimal(12,2),
	`medicareWithheldBox6` decimal(12,2),
	`stateWagesBox16` decimal(12,2),
	`stateWithheldBox17` decimal(12,2),
	`stateCode` varchar(2),
	`nonemployeeCompensation` decimal(12,2),
	`ordinaryIncome` decimal(12,2),
	`guaranteedPayments` decimal(12,2),
	`capitalGains` decimal(12,2),
	`distributionsReceived` decimal(12,2),
	`vaultDocumentId` int,
	`s3Url` varchar(500),
	`filedWithIRS` boolean DEFAULT false,
	`filedAt` timestamp,
	`irsConfirmationNumber` varchar(50),
	`deliveredToRecipient` boolean DEFAULT false,
	`deliveredAt` timestamp,
	`deliveryMethod` enum('mail','email','portal'),
	`status` enum('draft','generated','filed','corrected','voided') DEFAULT 'draft',
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `employer_tax_forms_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `employment_documents` (
	`id` int AUTO_INCREMENT NOT NULL,
	`positionHolderId` int NOT NULL,
	`positionId` int NOT NULL,
	`houseId` int NOT NULL,
	`documentType` enum('offer_letter','employment_agreement','contractor_agreement','w4_form','i9_form','w9_form','job_description','direct_deposit_form','handbook_acknowledgment','confidentiality_agreement','non_compete_agreement','operating_agreement_amendment','membership_certificate','board_resolution','background_check_authorization','termination_letter','resignation_letter') NOT NULL,
	`documentName` varchar(255) NOT NULL,
	`documentVersion` int DEFAULT 1,
	`vaultDocumentId` int,
	`s3Url` varchar(500),
	`requiresSignature` boolean DEFAULT true,
	`signedAt` timestamp,
	`signedByUserId` int,
	`signatureHash` varchar(64),
	`witnessRequired` boolean DEFAULT false,
	`witnessName` varchar(255),
	`witnessSignedAt` timestamp,
	`effectiveDate` timestamp,
	`expirationDate` timestamp,
	`status` enum('draft','pending_signature','signed','expired','superseded','voided') DEFAULT 'draft',
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `employment_documents_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `entity_lifecycle_stages` (
	`id` int AUTO_INCREMENT NOT NULL,
	`entityType` enum('house','business','property','worker','document','tax_return','restoration_case') NOT NULL,
	`entityId` int NOT NULL,
	`houseId` int,
	`currentStage` enum('draft','pending_creation','created','pending_activation','active','operating','on_hold','under_review','in_compliance','pending_transfer','transferring','pending_sale','selling','pending_dissolution','dissolving','dissolved','archived','deleted') NOT NULL,
	`previousStage` varchar(50),
	`stageEnteredAt` timestamp NOT NULL DEFAULT (now()),
	`complianceStatus` enum('compliant','pending_review','action_required','non_compliant','exempt') DEFAULT 'compliant',
	`nextComplianceDate` timestamp,
	`automatedTransitions` boolean DEFAULT true,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `entity_lifecycle_stages_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `estimated_taxes` (
	`id` int AUTO_INCREMENT NOT NULL,
	`tax_year_id` int NOT NULL,
	`house_id` int NOT NULL,
	`tax_quarter` enum('q1','q2','q3','q4') NOT NULL,
	`due_date` timestamp NOT NULL,
	`estimated_amount` decimal(18,2) NOT NULL,
	`paid_amount` decimal(18,2) NOT NULL DEFAULT '0',
	`paid_at` timestamp,
	`payment_method` varchar(100),
	`confirmation_number` varchar(100),
	`luv_ledger_transaction_id` int,
	`estimated_tax_status` enum('pending','paid','partial','overdue') NOT NULL DEFAULT 'pending',
	`created_at` timestamp NOT NULL DEFAULT (now()),
	`updated_at` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `estimated_taxes_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `event_invitees` (
	`id` int AUTO_INCREMENT NOT NULL,
	`eventId` int NOT NULL,
	`userId` int,
	`departmentId` int,
	`roleLevel` varchar(50),
	`isRequired` boolean NOT NULL DEFAULT true,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	CONSTRAINT `event_invitees_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `excellence_badges` (
	`id` int AUTO_INCREMENT NOT NULL,
	`name` varchar(255) NOT NULL,
	`code` varchar(50) NOT NULL,
	`description` text,
	`category` enum('quality','timeliness','client_satisfaction','revenue','mentorship','innovation','leadership','community') NOT NULL,
	`requirementType` varchar(100) NOT NULL,
	`requirementValue` varchar(255) NOT NULL,
	`requirementPeriod` enum('one_time','monthly','quarterly','annual') NOT NULL DEFAULT 'one_time',
	`badgeUrl` text,
	`badgeColor` varchar(20),
	`tier` enum('bronze','silver','gold','platinum') NOT NULL DEFAULT 'bronze',
	`tokenReward` int DEFAULT 0,
	`premiumTierBoost` boolean NOT NULL DEFAULT false,
	`isActive` boolean NOT NULL DEFAULT true,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `excellence_badges_id` PRIMARY KEY(`id`),
	CONSTRAINT `excellence_badges_code_unique` UNIQUE(`code`)
);
--> statement-breakpoint
CREATE TABLE `exchange_rates` (
	`id` int AUTO_INCREMENT NOT NULL,
	`base_currency` varchar(20) NOT NULL,
	`quote_currency` varchar(20) NOT NULL,
	`rate` decimal(30,15) NOT NULL,
	`source` varchar(100) NOT NULL,
	`recorded_at` timestamp NOT NULL DEFAULT (now()),
	CONSTRAINT `exchange_rates_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `external_businesses` (
	`id` int AUTO_INCREMENT NOT NULL,
	`userId` varchar(255) NOT NULL,
	`name` varchar(255) NOT NULL,
	`type` enum('vendor','partner','contractor','client','supplier','consultant','agency','other') NOT NULL DEFAULT 'vendor',
	`industry` varchar(255),
	`contact_name` varchar(255),
	`contact_email` varchar(255),
	`contact_phone` varchar(50),
	`website` varchar(500),
	`address` text,
	`city` varchar(100),
	`state` varchar(50),
	`zip_code` varchar(20),
	`country` varchar(100) DEFAULT 'USA',
	`ein` varchar(20),
	`duns_number` varchar(20),
	`notes` text,
	`status` enum('active','inactive','pending','blacklisted') NOT NULL DEFAULT 'active',
	`rating` int DEFAULT 0,
	`total_contracts` int DEFAULT 0,
	`total_spend` decimal(15,2) DEFAULT '0.00',
	`created_at` timestamp DEFAULT (now()),
	`updated_at` timestamp DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `external_businesses_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `external_companies` (
	`id` int AUTO_INCREMENT NOT NULL,
	`companyName` varchar(255) NOT NULL,
	`legalName` varchar(255),
	`ein` varchar(20),
	`stateOfFormation` varchar(50),
	`entityType` enum('llc','corporation','s_corp','partnership','sole_proprietor','nonprofit','trust','other') NOT NULL,
	`primaryContactName` varchar(255) NOT NULL,
	`primaryContactEmail` varchar(255) NOT NULL,
	`primaryContactPhone` varchar(50),
	`streetAddress` varchar(255),
	`city` varchar(100),
	`state` varchar(50),
	`zipCode` varchar(20),
	`country` varchar(100) DEFAULT 'United States',
	`industry` varchar(100),
	`employeeCount` enum('1','2_10','11_50','51_200','201_500','500_plus'),
	`annualRevenue` enum('under_100k','100k_500k','500k_1m','1m_5m','5m_10m','10m_plus'),
	`yearFounded` int,
	`website` varchar(255),
	`subscriptionTier` enum('standalone','connected','full_suite') NOT NULL DEFAULT 'standalone',
	`externalCompanyStatus` enum('pending_verification','onboarding','active','suspended','cancelled') NOT NULL DEFAULT 'pending_verification',
	`onboardingCompletedAt` timestamp,
	`onboardingStep` int DEFAULT 1,
	`referredBy` int,
	`referralCode` varchar(50),
	`internalNotes` text,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `external_companies_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `facility_plans` (
	`id` int AUTO_INCREMENT NOT NULL,
	`name` varchar(200) NOT NULL,
	`facilityType` enum('education_center','training_facility','mixed_use','satellite') NOT NULL DEFAULT 'education_center',
	`description` text,
	`plannedCapacity` int,
	`estimatedCost` decimal(12,2),
	`landRequirements` text,
	`buildingRequirements` json,
	`status` enum('concept','planning','land_search','land_acquired','design','construction','operational') NOT NULL DEFAULT 'concept',
	`targetOpenDate` timestamp,
	`linkedLandAcquisitionId` int,
	`notes` text,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `facility_plans_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `family_resumes` (
	`id` int AUTO_INCREMENT NOT NULL,
	`familyMemberId` varchar(100) NOT NULL,
	`fullName` varchar(255) NOT NULL,
	`title` varchar(255),
	`email` varchar(320),
	`phone` varchar(50),
	`location` varchar(255),
	`summary` text,
	`qualificationType` enum('traditional','demonstrated','hybrid') NOT NULL DEFAULT 'demonstrated',
	`education` json,
	`certifications` json,
	`competencyEvidence` json,
	`skills` json,
	`references` json,
	`developmentPlan` text,
	`resumeStatus` enum('draft','complete','approved') NOT NULL DEFAULT 'draft',
	`createdBy` int,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `family_resumes_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `feature_flags` (
	`id` int AUTO_INCREMENT NOT NULL,
	`flag_key` varchar(100) NOT NULL,
	`flag_name` varchar(255) NOT NULL,
	`description` text,
	`is_global` boolean NOT NULL DEFAULT true,
	`enabled_for_users` json,
	`enabled_for_houses` json,
	`enabled_percentage` int NOT NULL DEFAULT 100,
	`is_enabled` boolean NOT NULL DEFAULT false,
	`category` varchar(100),
	`created_at` timestamp NOT NULL DEFAULT (now()),
	`updated_at` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `feature_flags_id` PRIMARY KEY(`id`),
	CONSTRAINT `feature_flags_flag_key_unique` UNIQUE(`flag_key`)
);
--> statement-breakpoint
CREATE TABLE `feedback_requests` (
	`id` int AUTO_INCREMENT NOT NULL,
	`reviewId` int NOT NULL,
	`requesterId` int NOT NULL,
	`requesterName` varchar(255) NOT NULL,
	`employeeName` varchar(255) NOT NULL,
	`dueDate` timestamp,
	`remindersSent` int NOT NULL DEFAULT 0,
	`lastReminderAt` timestamp,
	`totalRequested` int NOT NULL DEFAULT 0,
	`totalSubmitted` int NOT NULL DEFAULT 0,
	`status` enum('open','closed','cancelled') NOT NULL DEFAULT 'open',
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `feedback_requests_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `filing_reminders` (
	`id` int AUTO_INCREMENT NOT NULL,
	`filingTaskId` int NOT NULL,
	`houseId` int NOT NULL,
	`userId` int NOT NULL,
	`reminderType` enum('upcoming_deadline','overdue','document_needed','action_required','status_update') NOT NULL,
	`message` text NOT NULL,
	`scheduledFor` timestamp NOT NULL,
	`sentAt` timestamp,
	`deliveryMethod` enum('email','sms','in_app','all') DEFAULT 'in_app',
	`delivered` boolean DEFAULT false,
	`acknowledged` boolean DEFAULT false,
	`acknowledgedAt` timestamp,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	CONSTRAINT `filing_reminders_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `filing_tasks` (
	`id` int AUTO_INCREMENT NOT NULL,
	`houseId` int NOT NULL,
	`userId` int NOT NULL,
	`workflowId` int NOT NULL,
	`entityType` enum('business','property','worker','tax_return','trademark','trust') NOT NULL,
	`entityId` int,
	`taskName` varchar(255) NOT NULL,
	`description` text,
	`status` enum('pending','in_progress','awaiting_documents','submitted','under_review','approved','rejected','completed','cancelled') NOT NULL DEFAULT 'pending',
	`dueDate` timestamp,
	`submittedAt` timestamp,
	`completedAt` timestamp,
	`confirmationNumber` varchar(100),
	`filingReference` varchar(255),
	`rejectionReason` text,
	`attachedDocuments` json,
	`generatedDocuments` json,
	`feePaid` decimal(10,2),
	`feeTransactionId` int,
	`automationStatus` enum('not_started','preparing','ready_to_submit','auto_submitted','manual_required') DEFAULT 'not_started',
	`automationLog` json,
	`ledgerTransactionId` int,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `filing_tasks_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `filing_workflows` (
	`id` int AUTO_INCREMENT NOT NULL,
	`workflowName` varchar(255) NOT NULL,
	`workflowCode` varchar(50) NOT NULL,
	`description` text,
	`filingType` enum('state_business','federal_business','state_tax','federal_tax','property','employment','nonprofit','trust','trademark','patent') NOT NULL,
	`jurisdiction` varchar(100),
	`requiredDocuments` json,
	`prerequisites` json,
	`estimatedDays` int,
	`filingFee` decimal(10,2),
	`automationLevel` enum('full','partial','manual') DEFAULT 'partial',
	`apiEndpoint` varchar(500),
	`isActive` boolean DEFAULT true,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `filing_workflows_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `financial_errors` (
	`id` int AUTO_INCREMENT NOT NULL,
	`errorCode` varchar(20) NOT NULL,
	`errorCategory` enum('luvledger','allocation','gift_sale','sync','validation','system') NOT NULL,
	`errorSeverity` enum('info','warning','error','critical') NOT NULL,
	`houseId` int,
	`transactionId` int,
	`syncCycleId` int,
	`message` text NOT NULL,
	`details` json,
	`stackTrace` text,
	`errorStatus` enum('open','acknowledged','investigating','resolved','ignored') NOT NULL DEFAULT 'open',
	`resolvedById` int,
	`resolvedAt` timestamp,
	`resolution` text,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `financial_errors_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `flame_lock_codes` (
	`id` int AUTO_INCREMENT NOT NULL,
	`entityType` enum('house','ai_system','business','scroll') NOT NULL,
	`entityId` int NOT NULL,
	`flameLockCode` varchar(64) NOT NULL,
	`lockHash` varchar(255) NOT NULL,
	`issuedByUserId` int NOT NULL,
	`status` enum('active','revoked','expired') NOT NULL DEFAULT 'active',
	`expiresAt` timestamp,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `flame_lock_codes_id` PRIMARY KEY(`id`),
	CONSTRAINT `flame_lock_codes_flameLockCode_unique` UNIQUE(`flameLockCode`)
);
--> statement-breakpoint
CREATE TABLE `foreign_bank_accounts` (
	`id` int AUTO_INCREMENT NOT NULL,
	`houseId` int NOT NULL,
	`userId` int NOT NULL,
	`foreignEntityId` int,
	`bankName` varchar(255) NOT NULL,
	`bankAddress` text,
	`countryCode` varchar(3) NOT NULL,
	`accountNumber` varchar(100) NOT NULL,
	`accountType` enum('checking','savings','investment','securities','other') NOT NULL,
	`currency` varchar(3) NOT NULL,
	`swiftCode` varchar(20),
	`iban` varchar(50),
	`routingNumber` varchar(50),
	`maxBalanceThisYear` decimal(18,2),
	`currentBalance` decimal(18,2),
	`lastBalanceUpdate` timestamp,
	`fbarReportable` boolean DEFAULT false,
	`lastFbarYear` int,
	`status` enum('active','closed','frozen') NOT NULL DEFAULT 'active',
	`openedDate` timestamp,
	`closedDate` timestamp,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `foreign_bank_accounts_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `foreign_entities` (
	`id` int AUTO_INCREMENT NOT NULL,
	`houseId` int NOT NULL,
	`parentEntityId` int,
	`entityName` varchar(255) NOT NULL,
	`tradingName` varchar(255),
	`registrationNumber` varchar(100),
	`taxId` varchar(100),
	`vatNumber` varchar(100),
	`jurisdictionId` int NOT NULL,
	`countryCode` varchar(3) NOT NULL,
	`entityType` enum('uk_ltd','uk_plc','uk_llp','uk_cic','eu_gmbh','eu_ag','eu_bv','eu_sarl','eu_sas','nevis_llc','cook_islands_trust','cayman_exempt','bvi_bc','panama_sa','seychelles_ibc','hk_limited','singapore_pte','dubai_fze','foreign_nonprofit','foreign_branch','representative_office') NOT NULL,
	`status` enum('pending_formation','active','dormant','dissolved','struck_off') NOT NULL DEFAULT 'pending_formation',
	`incorporationDate` timestamp,
	`financialYearEnd` varchar(10),
	`registeredAddress` text,
	`businessAddress` text,
	`directors` json,
	`shareholders` json,
	`secretary` varchar(255),
	`bankName` varchar(255),
	`bankAccountNumber` varchar(100),
	`bankSwiftCode` varchar(20),
	`bankIban` varchar(50),
	`annualReturnDue` timestamp,
	`lastAnnualReturn` timestamp,
	`auditRequired` boolean DEFAULT false,
	`luvLedgerAccountId` int,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `foreign_entities_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `founder_consulting_engagements` (
	`id` int AUTO_INCREMENT NOT NULL,
	`clientEntity` varchar(255) NOT NULL,
	`serviceType` enum('strategic_advisory','training_delivery','grant_consulting','community_engagement','curriculum_development','other') NOT NULL,
	`serviceDescription` text,
	`billingType` enum('hourly','project','retainer','per_event') NOT NULL,
	`hourlyRate` decimal(10,2),
	`projectFee` decimal(10,2),
	`hoursWorked` decimal(8,2),
	`invoiceNumber` varchar(50),
	`invoiceDate` date,
	`dueDate` date,
	`totalAmount` decimal(18,2) NOT NULL,
	`status` enum('draft','invoiced','paid','overdue','cancelled') NOT NULL DEFAULT 'draft',
	`paidAt` timestamp,
	`incomeStreamId` int,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `founder_consulting_engagements_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `founder_income_streams` (
	`id` int AUTO_INCREMENT NOT NULL,
	`incomeType` enum('trust_distribution','llc_distribution_laws_collective','llc_distribution_laws_llc','llc_distribution_real_eye','token_earnings','consulting_fees','ip_royalties','grant_stipend') NOT NULL,
	`sourceEntity` varchar(255) NOT NULL,
	`description` text,
	`amount` decimal(18,2) NOT NULL,
	`currency` varchar(3) NOT NULL DEFAULT 'USD',
	`periodStart` date,
	`periodEnd` date,
	`taxCategory` enum('trust_income','pass_through_k1','self_employment','royalty_income','w2_wages','1099_contractor'),
	`taxYear` int,
	`documentationRef` varchar(255),
	`invoiceNumber` varchar(50),
	`status` enum('projected','pending','received','reconciled') NOT NULL DEFAULT 'pending',
	`receivedAt` timestamp,
	`ledgerTransactionId` int,
	`blockchainHash` varchar(255),
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `founder_income_streams_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `founder_income_summary` (
	`id` int AUTO_INCREMENT NOT NULL,
	`year` int NOT NULL,
	`quarter` int,
	`month` int,
	`trustDistributions` decimal(18,2) DEFAULT '0',
	`llcDistributions` decimal(18,2) DEFAULT '0',
	`tokenEarnings` decimal(18,2) DEFAULT '0',
	`consultingFees` decimal(18,2) DEFAULT '0',
	`ipRoyalties` decimal(18,2) DEFAULT '0',
	`grantStipends` decimal(18,2) DEFAULT '0',
	`totalIncome` decimal(18,2) DEFAULT '0',
	`estimatedSelfEmploymentTax` decimal(18,2),
	`estimatedIncomeTax` decimal(18,2),
	`status` enum('projected','actual','reconciled') NOT NULL DEFAULT 'projected',
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `founder_income_summary_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `founder_ip_licenses` (
	`id` int AUTO_INCREMENT NOT NULL,
	`ipType` enum('curriculum','brand_trademark','systems_processes','content','other') NOT NULL,
	`ipName` varchar(255) NOT NULL,
	`ipDescription` text,
	`licenseeEntity` varchar(255) NOT NULL,
	`licenseType` enum('exclusive','non_exclusive','limited') NOT NULL,
	`startDate` date NOT NULL,
	`endDate` date,
	`autoRenew` boolean DEFAULT false,
	`royaltyType` enum('flat_annual','per_use','percentage','per_student') NOT NULL,
	`royaltyRate` decimal(10,4),
	`flatFee` decimal(18,2),
	`status` enum('active','expired','terminated','pending') NOT NULL DEFAULT 'pending',
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `founder_ip_licenses_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `founder_token_earnings` (
	`id` int AUTO_INCREMENT NOT NULL,
	`tokenType` enum('MIRROR','GIFT','SPARK','HOUSE','CROWN') NOT NULL,
	`amount` int NOT NULL,
	`activityType` varchar(100) NOT NULL,
	`activityDescription` text,
	`converted` boolean NOT NULL DEFAULT false,
	`conversionDate` timestamp,
	`cashValue` decimal(18,2),
	`conversionRate` decimal(10,4),
	`incomeStreamId` int,
	`blockchainHash` varchar(255),
	`earnedAt` timestamp NOT NULL DEFAULT (now()),
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	CONSTRAINT `founder_token_earnings_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `founding_members` (
	`id` int AUTO_INCREMENT NOT NULL,
	`userId` int,
	`houseId` varchar(100),
	`fullName` varchar(255) NOT NULL,
	`foundingDate` timestamp NOT NULL,
	`foundingRole` enum('primary_founder','co_founder','charter_member','founding_investor') NOT NULL,
	`entityId` varchar(100),
	`entityName` varchar(255),
	`heirEducationBenefit` boolean NOT NULL DEFAULT true,
	`benefitGenerations` int NOT NULL DEFAULT 3,
	`verificationStatus` enum('verified','pending','disputed') NOT NULL DEFAULT 'verified',
	`verifiedBy` varchar(255),
	`verifiedAt` timestamp,
	`foundingMemberStatus` enum('active','deceased','inactive') NOT NULL DEFAULT 'active',
	`notes` text,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `founding_members_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `fraud_flags` (
	`id` int AUTO_INCREMENT NOT NULL,
	`house_ledger_id` int NOT NULL,
	`fraud_flag_type` enum('unusual_transaction_volume','balance_discrepancy','unauthorized_access_attempt','hash_mismatch','duplicate_transaction','suspicious_pattern','manual_report') NOT NULL,
	`fraud_severity` enum('low','medium','high','critical') DEFAULT 'medium',
	`description` text,
	`detected_at` timestamp DEFAULT (now()),
	`fraud_investigation_status` enum('pending','under_review','resolved_valid','resolved_fraud','dismissed') DEFAULT 'pending',
	`investigated_by` varchar(255),
	`resolution_notes` text,
	`resolved_at` timestamp,
	`created_at` timestamp DEFAULT (now()),
	CONSTRAINT `fraud_flags_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `fund_contributions` (
	`id` int AUTO_INCREMENT NOT NULL,
	`fundId` int NOT NULL,
	`houseId` int NOT NULL,
	`sourceType` enum('revenue_share','direct_donation','grant_allocation','investment_return','transfer_in') NOT NULL,
	`sourceEntityType` varchar(50),
	`sourceEntityId` int,
	`sourceEntityName` varchar(255),
	`grossAmount` decimal(18,2) NOT NULL,
	`netAmount` decimal(18,2) NOT NULL,
	`currency` varchar(10) DEFAULT 'USD',
	`parentTransactionId` int,
	`splitPercentage` decimal(5,2),
	`calculationNotes` text,
	`ledgerTransactionId` int,
	`blockchainHash` varchar(128),
	`status` enum('pending','confirmed','reversed') DEFAULT 'confirmed',
	`contributedAt` timestamp NOT NULL DEFAULT (now()),
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	CONSTRAINT `fund_contributions_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `fund_disbursements` (
	`id` int AUTO_INCREMENT NOT NULL,
	`fundId` int NOT NULL,
	`houseId` int NOT NULL,
	`purposeType` enum('property_purchase','scholarship','tuition_assistance','emergency_grant','business_loan','business_grant','cultural_event','cultural_project','community_vote','administrative','transfer_out') NOT NULL,
	`description` text NOT NULL,
	`recipientType` enum('house_member','house','external_entity','vendor','institution','fund') NOT NULL,
	`recipientId` int,
	`recipientName` varchar(255) NOT NULL,
	`requestedAmount` decimal(18,2) NOT NULL,
	`approvedAmount` decimal(18,2),
	`disbursedAmount` decimal(18,2),
	`currency` varchar(10) DEFAULT 'USD',
	`status` enum('draft','pending_approval','approved','rejected','processing','disbursed','cancelled','refunded') NOT NULL DEFAULT 'draft',
	`requestedBy` int NOT NULL,
	`approvedBy` int,
	`approvalNotes` text,
	`rejectionReason` text,
	`supportingDocuments` json,
	`receiptDocuments` json,
	`ledgerTransactionId` int,
	`blockchainHash` varchar(128),
	`requestedAt` timestamp NOT NULL DEFAULT (now()),
	`approvedAt` timestamp,
	`disbursedAt` timestamp,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `fund_disbursements_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `funding_sources` (
	`id` int AUTO_INCREMENT NOT NULL,
	`name` varchar(255) NOT NULL,
	`code` varchar(50) NOT NULL,
	`type` enum('grant','contract','internal','donation','revenue') NOT NULL,
	`description` text,
	`funderName` varchar(255),
	`totalBudget` decimal(15,2),
	`laborBudget` decimal(15,2),
	`startDate` timestamp,
	`endDate` timestamp,
	`status` enum('active','pending','closed','expired') NOT NULL DEFAULT 'active',
	`requiresTimeTracking` boolean NOT NULL DEFAULT true,
	`reportingFrequency` enum('weekly','biweekly','monthly','quarterly','annually') DEFAULT 'monthly',
	`entityId` int,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `funding_sources_id` PRIMARY KEY(`id`),
	CONSTRAINT `funding_sources_code_unique` UNIQUE(`code`)
);
--> statement-breakpoint
CREATE TABLE `game_achievements` (
	`id` int AUTO_INCREMENT NOT NULL,
	`name` varchar(100) NOT NULL,
	`description` text,
	`gameId` int,
	`achievementType` enum('milestone','streak','skill','tournament','special') NOT NULL,
	`requirement` json,
	`tokenReward` int DEFAULT 0,
	`badgeIcon` varchar(100),
	`hasTiers` boolean NOT NULL DEFAULT false,
	`tierRequirements` json,
	`tierRewards` json,
	`isActive` boolean NOT NULL DEFAULT true,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	CONSTRAINT `game_achievements_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `game_center_games` (
	`id` int AUTO_INCREMENT NOT NULL,
	`name` varchar(100) NOT NULL,
	`slug` varchar(50) NOT NULL,
	`description` text,
	`gameType` enum('strategy','puzzle','word','card','board','trivia','mystery','educational') NOT NULL,
	`ageGroup` enum('k_5','6_8','9_12','adult','all_ages') NOT NULL DEFAULT 'all_ages',
	`minPlayers` int NOT NULL DEFAULT 1,
	`maxPlayers` int NOT NULL DEFAULT 2,
	`estimatedDuration` varchar(50),
	`skillsTargeted` json,
	`tokenRewardBase` int DEFAULT 10,
	`isActive` boolean NOT NULL DEFAULT true,
	`icon` varchar(50),
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `game_center_games_id` PRIMARY KEY(`id`),
	CONSTRAINT `game_center_games_slug_unique` UNIQUE(`slug`)
);
--> statement-breakpoint
CREATE TABLE `game_challenges` (
	`id` int AUTO_INCREMENT NOT NULL,
	`name` varchar(100) NOT NULL,
	`description` text,
	`challengeType` enum('daily','weekly') NOT NULL,
	`gameType` varchar(50),
	`requirement` json NOT NULL,
	`tokenReward` int NOT NULL DEFAULT 25,
	`badgeIcon` varchar(100),
	`isActive` boolean NOT NULL DEFAULT true,
	`rotationSlot` int,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	CONSTRAINT `game_challenges_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `game_matches` (
	`id` int AUTO_INCREMENT NOT NULL,
	`gameId` int NOT NULL,
	`matchType` enum('solo','vs_ai','vs_player','tournament') NOT NULL DEFAULT 'solo',
	`status` enum('waiting','in_progress','completed','abandoned') NOT NULL DEFAULT 'waiting',
	`player1Id` int,
	`player2Id` int,
	`winnerId` int,
	`player1Score` int DEFAULT 0,
	`player2Score` int DEFAULT 0,
	`gameState` json,
	`startedAt` timestamp,
	`completedAt` timestamp,
	`tokensAwarded` int DEFAULT 0,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	CONSTRAINT `game_matches_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `game_player_achievements` (
	`id` int AUTO_INCREMENT NOT NULL,
	`playerId` int NOT NULL,
	`achievementId` int NOT NULL,
	`earnedAt` timestamp NOT NULL DEFAULT (now()),
	`tokensAwarded` int DEFAULT 0,
	`currentTier` enum('bronze','silver','gold','platinum') DEFAULT 'bronze',
	`progressCount` int NOT NULL DEFAULT 1,
	`tierUpgradedAt` timestamp,
	`shareCode` varchar(32),
	`timesShared` int DEFAULT 0,
	`blockchainHash` varchar(255),
	`blockchainRecordedAt` timestamp,
	`nftTokenId` varchar(100),
	`nftContractAddress` varchar(255),
	`nftMintedAt` timestamp,
	`nftTransactionHash` varchar(255),
	CONSTRAINT `game_player_achievements_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `game_player_challenges` (
	`id` int AUTO_INCREMENT NOT NULL,
	`playerId` int NOT NULL,
	`challengeId` int NOT NULL,
	`periodStart` timestamp NOT NULL,
	`periodEnd` timestamp NOT NULL,
	`currentProgress` int NOT NULL DEFAULT 0,
	`targetProgress` int NOT NULL,
	`isCompleted` boolean NOT NULL DEFAULT false,
	`completedAt` timestamp,
	`tokensAwarded` int DEFAULT 0,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	CONSTRAINT `game_player_challenges_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `game_player_stats` (
	`id` int AUTO_INCREMENT NOT NULL,
	`playerId` int NOT NULL,
	`gameId` int NOT NULL,
	`gamesPlayed` int NOT NULL DEFAULT 0,
	`gamesWon` int NOT NULL DEFAULT 0,
	`gamesLost` int NOT NULL DEFAULT 0,
	`gamesTied` int NOT NULL DEFAULT 0,
	`totalScore` bigint NOT NULL DEFAULT 0,
	`highScore` int DEFAULT 0,
	`currentStreak` int DEFAULT 0,
	`bestStreak` int DEFAULT 0,
	`rating` int DEFAULT 1000,
	`tokensEarned` int DEFAULT 0,
	`lastPlayedAt` timestamp,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `game_player_stats_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `game_save_states` (
	`id` int AUTO_INCREMENT NOT NULL,
	`playerId` int NOT NULL,
	`gameId` int NOT NULL,
	`matchId` int,
	`saveName` varchar(100),
	`saveType` enum('manual','auto','checkpoint') NOT NULL DEFAULT 'auto',
	`gameState` json NOT NULL,
	`score` int DEFAULT 0,
	`progress` int DEFAULT 0,
	`playTime` int DEFAULT 0,
	`difficulty` enum('easy','medium','hard') DEFAULT 'medium',
	`isActive` boolean NOT NULL DEFAULT true,
	`savedAt` timestamp NOT NULL DEFAULT (now()),
	`expiresAt` timestamp,
	CONSTRAINT `game_save_states_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `game_scores` (
	`id` int AUTO_INCREMENT NOT NULL,
	`userId` int NOT NULL,
	`gameType` varchar(50) NOT NULL,
	`score` int NOT NULL,
	`difficulty` varchar(20) NOT NULL,
	`correctAnswers` int NOT NULL,
	`totalQuestions` int NOT NULL,
	`maxStreak` int NOT NULL DEFAULT 0,
	`tokensEarned` int NOT NULL DEFAULT 0,
	`completedAt` timestamp NOT NULL DEFAULT (now()),
	CONSTRAINT `game_scores_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `game_tournaments` (
	`id` int AUTO_INCREMENT NOT NULL,
	`gameId` int NOT NULL,
	`name` varchar(200) NOT NULL,
	`description` text,
	`tournamentType` enum('single_elimination','double_elimination','round_robin','swiss') NOT NULL DEFAULT 'single_elimination',
	`ageGroup` enum('k_5','6_8','9_12','adult','all_ages','family') NOT NULL DEFAULT 'all_ages',
	`maxParticipants` int DEFAULT 16,
	`currentParticipants` int DEFAULT 0,
	`entryFee` int DEFAULT 0,
	`prizePool` int DEFAULT 0,
	`status` enum('registration','in_progress','completed','cancelled') NOT NULL DEFAULT 'registration',
	`bracketData` json,
	`registrationDeadline` timestamp,
	`startDate` timestamp,
	`endDate` timestamp,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `game_tournaments_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `gaming_compliance_reports` (
	`id` int AUTO_INCREMENT NOT NULL,
	`reportType` enum('individual','department','house','entity','system') NOT NULL,
	`departmentId` int,
	`houseId` int,
	`entityId` int,
	`periodType` enum('weekly','monthly','quarterly') NOT NULL,
	`periodStart` timestamp NOT NULL,
	`periodEnd` timestamp NOT NULL,
	`totalEmployees` int DEFAULT 0,
	`compliantEmployees` int DEFAULT 0,
	`nonCompliantEmployees` int DEFAULT 0,
	`excusedEmployees` int DEFAULT 0,
	`complianceRate` decimal(5,2),
	`totalMinutesPlayed` int DEFAULT 0,
	`averageMinutesPerEmployee` int DEFAULT 0,
	`totalTeamEventsHeld` int DEFAULT 0,
	`averageEventAttendance` decimal(5,2),
	`topPerformers` json,
	`reportFileUrl` varchar(500),
	`generatedBy` int,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	CONSTRAINT `gaming_compliance_reports_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `gaming_leaderboards` (
	`id` int AUTO_INCREMENT NOT NULL,
	`leaderboardType` enum('individual','team','house','entity') NOT NULL,
	`period` enum('weekly','monthly','quarterly','yearly','all_time') NOT NULL,
	`periodStart` timestamp NOT NULL,
	`periodEnd` timestamp,
	`userId` int,
	`departmentId` int,
	`houseId` int,
	`entityId` int,
	`gameId` int,
	`gameSlug` varchar(100),
	`rank` int NOT NULL,
	`previousRank` int,
	`rankChange` int DEFAULT 0,
	`totalMinutesPlayed` int DEFAULT 0,
	`totalGamesPlayed` int DEFAULT 0,
	`totalWins` int DEFAULT 0,
	`totalScore` int DEFAULT 0,
	`winRate` decimal(5,2),
	`complianceRate` decimal(5,2),
	`streakWeeks` int DEFAULT 0,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `gaming_leaderboards_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `generated_documents` (
	`id` int AUTO_INCREMENT NOT NULL,
	`houseId` int NOT NULL,
	`userId` int NOT NULL,
	`templateId` int NOT NULL,
	`templateCode` varchar(50) NOT NULL,
	`entityType` enum('business','property','worker','tax_return','trust','trademark') NOT NULL,
	`entityId` int,
	`documentName` varchar(255) NOT NULL,
	`documentNumber` varchar(100),
	`formData` json NOT NULL,
	`pdfUrl` varchar(500),
	`pdfKey` varchar(255),
	`htmlContent` text,
	`status` enum('draft','generated','reviewed','signed','filed','accepted','rejected') NOT NULL DEFAULT 'draft',
	`signatureRequired` boolean DEFAULT false,
	`signedAt` timestamp,
	`signedBy` varchar(255),
	`signatureData` text,
	`filedAt` timestamp,
	`filingMethod` enum('mail','online','in_person','fax'),
	`confirmationNumber` varchar(100),
	`acceptedAt` timestamp,
	`rejectionReason` text,
	`ledgerTransactionId` int,
	`generatedAt` timestamp NOT NULL DEFAULT (now()),
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `generated_documents_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `gift_activation_attempts` (
	`id` int AUTO_INCREMENT NOT NULL,
	`giftId` int NOT NULL,
	`userId` varchar(255) NOT NULL,
	`giftAttemptStatus` enum('approved','denied','pending') NOT NULL,
	`anniversaryMet` boolean,
	`scrollsComplete` boolean,
	`lineageVerified` boolean,
	`lockExpired` boolean,
	`resultMessage` text,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	CONSTRAINT `gift_activation_attempts_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `gift_sale_ratios` (
	`id` int AUTO_INCREMENT NOT NULL,
	`houseId` int NOT NULL,
	`totalGiftsIssued` int NOT NULL DEFAULT 0,
	`totalSalesCompleted` int NOT NULL DEFAULT 0,
	`currentRatio` decimal(10,4) NOT NULL DEFAULT '0',
	`globalRatioTarget` decimal(5,2) NOT NULL DEFAULT '3.00',
	`houseRatioTarget` decimal(5,2) NOT NULL DEFAULT '2.00',
	`isCompliant` boolean NOT NULL DEFAULT true,
	`lastViolationAt` timestamp,
	`violationCount` int NOT NULL DEFAULT 0,
	`giftingBlocked` boolean NOT NULL DEFAULT false,
	`blockReason` text,
	`lastUpdatedAt` timestamp NOT NULL DEFAULT (now()),
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	CONSTRAINT `gift_sale_ratios_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `gift_tokens` (
	`id` int AUTO_INCREMENT NOT NULL,
	`sourceUserId` varchar(255) NOT NULL,
	`sourceHouseId` int NOT NULL,
	`targetUserId` varchar(255),
	`targetHouseId` int,
	`targetEmail` varchar(255),
	`targetName` varchar(255),
	`giftType` enum('mirror','adaptive','locked') NOT NULL,
	`giftValue` decimal(20,8),
	`giftDescription` text,
	`giftMessage` text,
	`requiresAnniversary` boolean DEFAULT false,
	`anniversaryDate` timestamp,
	`requiresStewardshipScrolls` boolean DEFAULT false,
	`requiredScrolls` json,
	`lockDurationDays` int,
	`lockExpiresAt` timestamp,
	`requiresLineageVerification` boolean DEFAULT false,
	`lineageVerified` boolean DEFAULT false,
	`lineageVerifiedAt` timestamp,
	`lineageVerifiedBy` varchar(255),
	`giftStatus` enum('pending','awaiting_activation','activated','claimed','expired','revoked') NOT NULL DEFAULT 'pending',
	`activatedAt` timestamp,
	`claimedAt` timestamp,
	`revokedAt` timestamp,
	`revokeReason` text,
	`giftHash` varchar(64),
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `gift_tokens_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `government_action_notifications` (
	`id` int AUTO_INCREMENT NOT NULL,
	`governmentActionId` int NOT NULL,
	`notificationType` enum('deadline_reminder','new_action','status_change','compliance_alert') NOT NULL,
	`daysBefore` int,
	`recipientUserId` int,
	`recipientEmail` varchar(255),
	`recipientDepartment` varchar(100),
	`scheduledFor` timestamp,
	`sentAt` timestamp,
	`status` enum('pending','sent','failed','cancelled') NOT NULL DEFAULT 'pending',
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	CONSTRAINT `government_action_notifications_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `government_action_tasks` (
	`id` int AUTO_INCREMENT NOT NULL,
	`governmentActionId` int NOT NULL,
	`title` varchar(500) NOT NULL,
	`description` text,
	`assignedTo` int,
	`assignedDepartment` varchar(100),
	`dueDate` timestamp,
	`completedAt` timestamp,
	`status` enum('pending','in_progress','completed','blocked','cancelled') NOT NULL DEFAULT 'pending',
	`priority` enum('urgent','high','medium','low') NOT NULL DEFAULT 'medium',
	`notes` text,
	`documentationUrl` varchar(1000),
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()),
	CONSTRAINT `government_action_tasks_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `government_actions` (
	`id` int AUTO_INCREMENT NOT NULL,
	`title` varchar(500) NOT NULL,
	`description` text,
	`agencyId` int NOT NULL,
	`sourceUrl` varchar(1000),
	`referenceNumber` varchar(100),
	`actionType` enum('regulatory_change','grant_announcement','tax_update','licensing_requirement','labor_law','nonprofit_compliance','filing_deadline','policy_change','enforcement_action','guidance_update') NOT NULL,
	`announcedDate` timestamp,
	`effectiveDate` timestamp,
	`deadline` timestamp,
	`expirationDate` timestamp,
	`impactLevel` enum('critical','high','medium','low','informational') NOT NULL DEFAULT 'medium',
	`affectedEntities` json,
	`affectedDepartments` json,
	`estimatedCost` decimal(12,2),
	`estimatedTimeHours` int,
	`swotCategory` enum('strength','weakness','opportunity','threat'),
	`swotNotes` text,
	`complianceStatus` enum('pending','in_progress','compliant','non_compliant','not_applicable') NOT NULL DEFAULT 'pending',
	`complianceNotes` text,
	`assignedTo` int,
	`relatedTaskId` int,
	`relatedResourceLinkId` int,
	`showInTicker` boolean NOT NULL DEFAULT true,
	`tickerPriority` enum('urgent','high','normal','low') NOT NULL DEFAULT 'normal',
	`isAgentIdentified` boolean NOT NULL DEFAULT false,
	`agentConfidence` decimal(3,2),
	`agentSource` varchar(255),
	`status` enum('active','archived','superseded') NOT NULL DEFAULT 'active',
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()),
	`createdBy` int,
	CONSTRAINT `government_actions_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `government_agencies` (
	`id` int AUTO_INCREMENT NOT NULL,
	`code` varchar(20) NOT NULL,
	`name` varchar(255) NOT NULL,
	`fullName` varchar(500),
	`level` enum('federal','state','local','international') NOT NULL DEFAULT 'federal',
	`state` varchar(2),
	`website` varchar(500),
	`contactEmail` varchar(255),
	`contactPhone` varchar(50),
	`relevantDepartments` json,
	`relevantEntities` json,
	`isActive` boolean NOT NULL DEFAULT true,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()),
	CONSTRAINT `government_agencies_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `grant_application_documents` (
	`id` int AUTO_INCREMENT NOT NULL,
	`applicationId` int NOT NULL,
	`documentId` int NOT NULL,
	`isRequired` boolean NOT NULL DEFAULT false,
	`isSubmitted` boolean NOT NULL DEFAULT false,
	`notes` text,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	CONSTRAINT `grant_application_documents_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `grant_application_history` (
	`id` int AUTO_INCREMENT NOT NULL,
	`applicationId` int NOT NULL,
	`action` enum('created','status_changed','updated','submitted','attachment_added','note_added','assigned','feedback_received') NOT NULL,
	`previousStatus` varchar(50),
	`newStatus` varchar(50),
	`description` text,
	`metadata` json,
	`performedBy` int,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	CONSTRAINT `grant_application_history_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `grant_applications` (
	`id` int AUTO_INCREMENT NOT NULL,
	`grantOpportunityId` int NOT NULL,
	`applyingEntityId` int,
	`applyingEntityName` varchar(255) NOT NULL,
	`requestedAmount` decimal(12,2) NOT NULL,
	`projectTitle` varchar(255),
	`projectDescription` text,
	`status` enum('draft','in_review','submitted','under_review','additional_info','approved','denied','withdrawn') NOT NULL DEFAULT 'draft',
	`startedAt` timestamp NOT NULL DEFAULT (now()),
	`submittedAt` timestamp,
	`decisionDate` timestamp,
	`awardedAmount` decimal(12,2),
	`awardDate` timestamp,
	`proposalDocumentId` int,
	`budgetDocumentId` int,
	`supportingDocuments` json,
	`funderFeedback` text,
	`lessonsLearned` text,
	`assignedTo` int,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `grant_applications_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `grant_documents` (
	`id` int AUTO_INCREMENT NOT NULL,
	`entityId` varchar(100) NOT NULL,
	`entityName` varchar(255) NOT NULL,
	`fileName` varchar(500) NOT NULL,
	`originalFileName` varchar(500) NOT NULL,
	`fileSize` int NOT NULL,
	`mimeType` varchar(100) NOT NULL,
	`s3Key` varchar(1000) NOT NULL,
	`s3Url` varchar(2000) NOT NULL,
	`grantDocCategory` enum('budget','staffing','equipment','letters_of_support','legal','financial_statements','program_narrative','evaluation_plan','timeline','certificates','other') NOT NULL,
	`description` text,
	`version` int NOT NULL DEFAULT 1,
	`expiresAt` timestamp,
	`isExpired` boolean NOT NULL DEFAULT false,
	`grantDocStatus` enum('active','archived','replaced','deleted') NOT NULL DEFAULT 'active',
	`uploadedBy` varchar(255),
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `grant_documents_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `grant_funders` (
	`id` int AUTO_INCREMENT NOT NULL,
	`name` varchar(255) NOT NULL,
	`type` enum('federal','state','local','foundation','corporate','religious','community','other') NOT NULL,
	`website` text,
	`contactName` varchar(255),
	`contactEmail` varchar(255),
	`contactPhone` varchar(50),
	`focusAreas` json,
	`typicalGrantRange` varchar(100),
	`applicationCycle` varchar(100),
	`notes` text,
	`relationshipStatus` enum('researching','contacted','applied','funded','declined','inactive') DEFAULT 'researching',
	`totalApplied` int DEFAULT 0,
	`totalAwarded` int DEFAULT 0,
	`totalFundingReceived` decimal(14,2) DEFAULT '0',
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `grant_funders_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `grant_opportunities` (
	`id` int AUTO_INCREMENT NOT NULL,
	`name` varchar(255) NOT NULL,
	`funderName` varchar(255) NOT NULL,
	`funderType` enum('federal','state','local','foundation','corporate','religious','community','other') NOT NULL,
	`minAmount` decimal(12,2),
	`maxAmount` decimal(12,2),
	`typicalAmount` decimal(12,2),
	`eligibleEntityTypes` json,
	`eligibilityRequirements` text,
	`geographicRestrictions` varchar(255),
	`focusAreas` json,
	`description` text,
	`applicationDeadline` timestamp,
	`awardAnnouncementDate` timestamp,
	`fundingPeriodStart` timestamp,
	`fundingPeriodEnd` timestamp,
	`isRolling` boolean DEFAULT false,
	`applicationUrl` text,
	`applicationRequirements` text,
	`matchingRequired` boolean DEFAULT false,
	`matchingPercentage` decimal(5,2),
	`status` enum('researching','eligible','not_eligible','applying','submitted','archived') NOT NULL DEFAULT 'researching',
	`priority` enum('high','medium','low') DEFAULT 'medium',
	`notes` text,
	`addedBy` int NOT NULL,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `grant_opportunities_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `grant_tasks` (
	`id` int AUTO_INCREMENT NOT NULL,
	`grantApplicationId` int NOT NULL,
	`title` varchar(255) NOT NULL,
	`description` text,
	`dueDate` timestamp,
	`status` enum('pending','in_progress','completed','blocked') NOT NULL DEFAULT 'pending',
	`priority` enum('high','medium','low') DEFAULT 'medium',
	`assignedTo` int,
	`completedAt` timestamp,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `grant_tasks_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `guardian_access` (
	`id` int AUTO_INCREMENT NOT NULL,
	`guardianUserId` int NOT NULL,
	`studentProfileId` int NOT NULL,
	`accessLevel` enum('view_only','manage','full_control') NOT NULL DEFAULT 'manage',
	`notifications` json,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	CONSTRAINT `guardian_access_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `heir_accumulation_accounts` (
	`id` int AUTO_INCREMENT NOT NULL,
	`heirId` int NOT NULL,
	`houseId` int NOT NULL,
	`currentBalance` decimal(18,2) DEFAULT '0',
	`totalDeposits` decimal(18,2) DEFAULT '0',
	`totalWithdrawals` decimal(18,2) DEFAULT '0',
	`totalInterestEarned` decimal(18,2) DEFAULT '0',
	`interestRate` decimal(5,4) DEFAULT '0',
	`compoundingFrequency` enum('daily','monthly','quarterly','annually') DEFAULT 'monthly',
	`lastInterestDate` timestamp,
	`minimumAge` int DEFAULT 18,
	`releaseSchedule` enum('lump_sum','graduated','monthly','annual') DEFAULT 'graduated',
	`spendthriftProtected` boolean DEFAULT true,
	`status` enum('active','frozen','closed') DEFAULT 'active',
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `heir_accumulation_accounts_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `heir_distribution_locks` (
	`id` int AUTO_INCREMENT NOT NULL,
	`houseId` int NOT NULL,
	`isLocked` boolean DEFAULT false,
	`lockedAt` timestamp,
	`lockedBy` int,
	`lockReason` text,
	`lockDocumentId` int,
	`totalAllocatedPercentage` decimal(5,2),
	`canUnlock` boolean DEFAULT false,
	`unlockConditions` text,
	`lastModifiedAt` timestamp,
	`lastModifiedBy` int,
	`modificationHistory` json,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `heir_distribution_locks_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `heir_distributions` (
	`id` int AUTO_INCREMENT NOT NULL,
	`heirId` int NOT NULL,
	`houseId` int NOT NULL,
	`sourceType` enum('community_share','direct_gift','vesting_release','accumulation_release') NOT NULL,
	`sourceEventId` int,
	`grossAmount` decimal(18,2) NOT NULL,
	`heirPercentage` decimal(5,2) NOT NULL,
	`vestedPercentage` decimal(5,2) NOT NULL,
	`netAmount` decimal(18,2) NOT NULL,
	`distributedAmount` decimal(18,2) DEFAULT '0',
	`accumulatedAmount` decimal(18,2) DEFAULT '0',
	`calculationNotes` text,
	`ledgerTransactionId` int,
	`blockchainHash` varchar(128),
	`status` enum('pending','processed','held','released','reversed') DEFAULT 'processed',
	`processedAt` timestamp NOT NULL DEFAULT (now()),
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	CONSTRAINT `heir_distributions_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `heir_education_benefits` (
	`id` int AUTO_INCREMENT NOT NULL,
	`heirUserId` int,
	`heirHouseId` varchar(100),
	`heirFullName` varchar(255) NOT NULL,
	`foundingMemberId` int NOT NULL,
	`generationFromFounder` int NOT NULL,
	`lineagePath` text,
	`benefitType` enum('full_tuition','partial_tuition','materials_only','priority_enrollment') NOT NULL DEFAULT 'full_tuition',
	`coveragePercentage` int NOT NULL DEFAULT 100,
	`academyEnrollmentId` int,
	`enrollmentDate` timestamp,
	`expectedGraduation` timestamp,
	`tuitionValue` decimal(10,2),
	`totalBenefitUsed` decimal(10,2) DEFAULT '0',
	`heirBenefitStatus` enum('eligible','enrolled','graduated','expired','revoked') NOT NULL DEFAULT 'eligible',
	`lineageVerified` boolean NOT NULL DEFAULT false,
	`verifiedBy` varchar(255),
	`verifiedAt` timestamp,
	`notes` text,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `heir_education_benefits_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `heir_vesting_schedules` (
	`id` int AUTO_INCREMENT NOT NULL,
	`heirId` int NOT NULL,
	`houseId` int NOT NULL,
	`milestoneType` enum('age','education','house_participation','business_completion','time_based','custom') NOT NULL,
	`milestoneName` varchar(255) NOT NULL,
	`milestoneDescription` text,
	`targetAge` int,
	`educationLevel` enum('high_school','associates','bachelors','masters','doctorate','trade_certification'),
	`vestingMonths` int,
	`vestingPercentage` decimal(5,2) NOT NULL,
	`status` enum('pending','achieved','waived','expired') DEFAULT 'pending',
	`achievedAt` timestamp,
	`verifiedBy` int,
	`verificationNotes` text,
	`milestoneOrder` int DEFAULT 1,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	CONSTRAINT `heir_vesting_schedules_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `homeschool_portfolios` (
	`id` int AUTO_INCREMENT NOT NULL,
	`studentProfileId` int NOT NULL,
	`guardianUserId` int NOT NULL,
	`academicYear` varchar(10) NOT NULL,
	`stateOfResidence` varchar(50),
	`complianceRequirements` json,
	`assessmentRecords` json,
	`courseCompletions` json,
	`certificatesEarned` json,
	`workSamples` json,
	`totalInstructionalHours` int DEFAULT 0,
	`hoursBreakdown` json,
	`progressSummary` text,
	`strengthsNarrative` text,
	`goalsNarrative` text,
	`transcriptGenerated` boolean NOT NULL DEFAULT false,
	`transcriptUrl` text,
	`gpaEquivalent` decimal(3,2),
	`portfolioStatus` enum('active','archived','submitted_for_review') NOT NULL DEFAULT 'active',
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `homeschool_portfolios_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `house_activation_events` (
	`id` int AUTO_INCREMENT NOT NULL,
	`houseId` int NOT NULL,
	`eventType` enum('template_selected','requirement_started','requirement_completed','document_uploaded','verification_requested','verification_approved','verification_rejected','activation_initiated','activation_completed','activation_ceremony','status_changed') NOT NULL,
	`requirementId` int,
	`previousStatus` varchar(50),
	`newStatus` varchar(50),
	`description` text,
	`metadata` json,
	`actorUserId` int,
	`actorType` enum('system','user','admin') NOT NULL DEFAULT 'user',
	`blockchainHash` varchar(255),
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	CONSTRAINT `house_activation_events_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `house_activation_progress` (
	`id` int AUTO_INCREMENT NOT NULL,
	`houseId` int NOT NULL,
	`requirementId` int NOT NULL,
	`status` enum('not_started','in_progress','pending_verification','verified','failed','waived') NOT NULL DEFAULT 'not_started',
	`progressPercentage` int NOT NULL DEFAULT 0,
	`progressData` json,
	`verifiedAt` timestamp,
	`verifiedByUserId` int,
	`verificationNotes` text,
	`documentUrls` json,
	`waivedAt` timestamp,
	`waivedByUserId` int,
	`waiverReason` text,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `house_activation_progress_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `house_businesses` (
	`id` int AUTO_INCREMENT NOT NULL,
	`houseId` int NOT NULL,
	`businessEntityId` int NOT NULL,
	`ownershipPercentage` decimal(5,2) NOT NULL DEFAULT '100.00',
	`incomeContributionRate` decimal(5,2) NOT NULL DEFAULT '100.00',
	`status` enum('active','inactive') NOT NULL DEFAULT 'active',
	`linkedAt` timestamp NOT NULL DEFAULT (now()),
	CONSTRAINT `house_businesses_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `house_contracts` (
	`id` int AUTO_INCREMENT NOT NULL,
	`houseId` int NOT NULL,
	`houseName` varchar(255) NOT NULL,
	`contractNumber` varchar(100) NOT NULL,
	`title` varchar(500) NOT NULL,
	`description` text,
	`contractType` enum('vendor_agreement','partnership_agreement','service_agreement','licensing_agreement','lease_agreement','employment_contract','contractor_agreement','nda','operating_agreement','trust_affiliation','distribution_agreement','franchise_agreement','joint_venture','loan_agreement','insurance_policy','other') NOT NULL,
	`counterpartyName` varchar(255) NOT NULL,
	`counterpartyType` enum('individual','business','government','nonprofit','trust','other') NOT NULL DEFAULT 'business',
	`counterpartyContact` varchar(255),
	`counterpartyEmail` varchar(255),
	`counterpartyPhone` varchar(50),
	`contractValue` decimal(15,2),
	`paymentTerms` varchar(255),
	`paymentFrequency` enum('one_time','weekly','biweekly','monthly','quarterly','annually','as_invoiced'),
	`effectiveDate` timestamp NOT NULL,
	`expirationDate` timestamp,
	`renewalDate` timestamp,
	`terminationDate` timestamp,
	`autoRenew` boolean NOT NULL DEFAULT false,
	`renewalTermMonths` int,
	`noticePeriodDays` int DEFAULT 30,
	`documentUrl` text,
	`documentHash` varchar(64),
	`signatureStatus` enum('draft','pending_internal','pending_counterparty','partially_signed','fully_executed','expired') NOT NULL DEFAULT 'draft',
	`internalSignedBy` int,
	`internalSignedAt` timestamp,
	`counterpartySignedAt` timestamp,
	`status` enum('draft','pending_approval','active','suspended','expired','terminated','renewed') NOT NULL DEFAULT 'draft',
	`complianceAlertDays` int DEFAULT 30,
	`lastComplianceCheck` timestamp,
	`complianceStatus` enum('compliant','attention_needed','non_compliant','not_applicable') NOT NULL DEFAULT 'compliant',
	`luvLedgerRecorded` boolean NOT NULL DEFAULT false,
	`luvLedgerTransactionId` int,
	`luvLedgerHash` varchar(255),
	`tags` json,
	`notes` text,
	`createdBy` int NOT NULL,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `house_contracts_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `house_document_vaults` (
	`id` int AUTO_INCREMENT NOT NULL,
	`house_id` int NOT NULL,
	`vault_name` varchar(255) NOT NULL,
	`vault_hash` varchar(64) NOT NULL,
	`total_documents` int NOT NULL DEFAULT 0,
	`total_storage_bytes` bigint NOT NULL DEFAULT 0,
	`storage_quota_bytes` bigint NOT NULL DEFAULT 10737418240,
	`encryption_enabled` boolean NOT NULL DEFAULT true,
	`encryption_key_hash` varchar(64),
	`vault_status` enum('active','locked','archived') NOT NULL DEFAULT 'active',
	`created_at` timestamp NOT NULL DEFAULT (now()),
	`updated_at` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `house_document_vaults_id` PRIMARY KEY(`id`),
	CONSTRAINT `house_document_vaults_house_id_unique` UNIQUE(`house_id`)
);
--> statement-breakpoint
CREATE TABLE `house_heirs` (
	`id` int AUTO_INCREMENT NOT NULL,
	`houseId` int NOT NULL,
	`userId` int,
	`fullName` varchar(255) NOT NULL,
	`relationship` enum('child','grandchild','great_grandchild','spouse','sibling','niece_nephew','cousin','adopted','guardian_ward','other') NOT NULL,
	`dateOfBirth` timestamp,
	`email` varchar(255),
	`phone` varchar(50),
	`address` text,
	`distributionPercentage` decimal(5,2) NOT NULL,
	`percentageLocked` boolean DEFAULT false,
	`percentageLockedAt` timestamp,
	`percentageLockedBy` int,
	`vestingStatus` enum('not_started','partial','fully_vested','forfeited') DEFAULT 'not_started',
	`vestedPercentage` decimal(5,2) DEFAULT '0',
	`distributionMethod` enum('immediate','accumulate','hybrid') DEFAULT 'accumulate',
	`accumulationPercentage` decimal(5,2) DEFAULT '100',
	`spendthriftEnabled` boolean DEFAULT true,
	`status` enum('active','suspended','removed','deceased') DEFAULT 'active',
	`designatedAt` timestamp NOT NULL DEFAULT (now()),
	`designatedBy` int NOT NULL,
	`designationDocumentId` int,
	`totalDistributed` decimal(18,2) DEFAULT '0',
	`totalAccumulated` decimal(18,2) DEFAULT '0',
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `house_heirs_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `house_ledger_transactions` (
	`id` int AUTO_INCREMENT NOT NULL,
	`house_ledger_id` int NOT NULL,
	`hl_transaction_type` enum('inflow','outflow','transfer','allocation','distribution','treasury_contribution','reserve_deposit','circulation_withdrawal') NOT NULL,
	`amount` decimal(18,2) NOT NULL,
	`from_account` varchar(255),
	`to_account` varchar(255),
	`description` text,
	`transaction_hash` varchar(64) NOT NULL,
	`previous_hash` varchar(64),
	`verified` boolean DEFAULT false,
	`created_at` timestamp DEFAULT (now()),
	CONSTRAINT `house_ledger_transactions_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `house_ledgers` (
	`id` int AUTO_INCREMENT NOT NULL,
	`house_id` int NOT NULL,
	`ledger_name` varchar(255) NOT NULL,
	`ledger_hash` varchar(64) NOT NULL,
	`ledger_status` enum('active','suspended','archived','under_audit') DEFAULT 'active',
	`total_balance` decimal(18,2) DEFAULT '0.00',
	`reserve_balance` decimal(18,2) DEFAULT '0.00',
	`circulation_balance` decimal(18,2) DEFAULT '0.00',
	`treasury_contribution` decimal(18,2) DEFAULT '0.00',
	`house_retained` decimal(18,2) DEFAULT '0.00',
	`transaction_count` int DEFAULT 0,
	`last_sync_at` timestamp,
	`created_at` timestamp DEFAULT (now()),
	`updated_at` timestamp DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `house_ledgers_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `house_members` (
	`id` int AUTO_INCREMENT NOT NULL,
	`houseId` int NOT NULL,
	`userId` int NOT NULL,
	`memberType` enum('bloodline','non_bloodline') NOT NULL DEFAULT 'bloodline',
	`lineageStatus` enum('source_flame','direct_descendant','aligned_member') DEFAULT 'direct_descendant',
	`publicAlias` varchar(100),
	`realNameProtected` boolean NOT NULL DEFAULT true,
	`imageProtected` boolean NOT NULL DEFAULT true,
	`locationProtected` boolean NOT NULL DEFAULT true,
	`voiceLikenessProtected` boolean NOT NULL DEFAULT true,
	`role` enum('trustee','beneficiary','successor_trustee','advisor','custodial_flame') NOT NULL,
	`ownershipPercentage` decimal(5,2) NOT NULL DEFAULT '0',
	`votingRights` boolean NOT NULL DEFAULT false,
	`distributionEligible` boolean NOT NULL DEFAULT true,
	`canTransferTokens` boolean NOT NULL DEFAULT false,
	`canInitiateHouse` boolean NOT NULL DEFAULT false,
	`successionEligible` boolean NOT NULL DEFAULT false,
	`status` enum('active','inactive','pending') NOT NULL DEFAULT 'pending',
	`addedAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `house_members_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `house_projected_distributions` (
	`id` int AUTO_INCREMENT NOT NULL,
	`houseId` int NOT NULL,
	`periodStart` date NOT NULL,
	`periodEnd` date NOT NULL,
	`projectedGrossIncome` decimal(20,2) NOT NULL DEFAULT '0',
	`projectedNetDistribution` decimal(20,2) NOT NULL DEFAULT '0',
	`projectedOperationsAllocation` decimal(20,2) NOT NULL DEFAULT '0',
	`projectedInheritanceAllocation` decimal(20,2) NOT NULL DEFAULT '0',
	`calculationMethod` enum('average_house','tier_based','custom_projection','historical') NOT NULL DEFAULT 'average_house',
	`calculationNotes` text,
	`isDisplayed` boolean NOT NULL DEFAULT true,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `house_projected_distributions_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `house_referrals` (
	`id` int AUTO_INCREMENT NOT NULL,
	`referringHouseId` int NOT NULL,
	`referredHouseId` int NOT NULL,
	`referringUserId` int NOT NULL,
	`referralCode` varchar(50),
	`referralType` enum('direct','link','event','partner') NOT NULL DEFAULT 'direct',
	`commissionRate` decimal(5,2) DEFAULT '10.00',
	`commissionDuration` int DEFAULT 12,
	`totalCommissionEarned` decimal(20,2) DEFAULT '0.00',
	`status` enum('pending','active','expired','cancelled') NOT NULL DEFAULT 'pending',
	`activatedAt` timestamp,
	`expiresAt` timestamp,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `house_referrals_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `house_service_permissions` (
	`id` int AUTO_INCREMENT NOT NULL,
	`houseId` int NOT NULL,
	`permissionType` enum('founding_house','heir','licensed','consumer_only') NOT NULL,
	`canOfferProducts` boolean NOT NULL DEFAULT false,
	`canOfferServices` boolean NOT NULL DEFAULT false,
	`canOfferTraining` boolean NOT NULL DEFAULT false,
	`canResell` boolean NOT NULL DEFAULT false,
	`canWhiteLabel` boolean NOT NULL DEFAULT false,
	`revenueSharePercentage` decimal(5,2) DEFAULT '0',
	`restrictedCategories` json,
	`grantedBy` int,
	`grantedAt` timestamp,
	`expiresAt` timestamp,
	`status` enum('active','suspended','revoked','expired') NOT NULL DEFAULT 'active',
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `house_service_permissions_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `house_sub_entities` (
	`id` int AUTO_INCREMENT NOT NULL,
	`houseId` int NOT NULL,
	`entityType` enum('operating_llc','family_trust','education_trust','charitable_trust','investment_entity','holding_company') NOT NULL,
	`name` varchar(255) NOT NULL,
	`description` text,
	`ein` varchar(20),
	`stateOfFormation` varchar(50),
	`formationDate` date,
	`allocationPercentage` decimal(5,2) NOT NULL DEFAULT '0',
	`formationDocumentUrl` text,
	`operatingAgreementUrl` text,
	`status` enum('forming','active','suspended','dissolved') NOT NULL DEFAULT 'forming',
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `house_sub_entities_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `house_succession_designations` (
	`id` int AUTO_INCREMENT NOT NULL,
	`houseId` int NOT NULL,
	`successorUserId` int NOT NULL,
	`priority` int NOT NULL,
	`designationType` enum('explicit','automatic_spouse','automatic_eldest','automatic_trust') NOT NULL,
	`conditions` json,
	`acceptanceStatus` enum('pending','accepted','declined','revoked') NOT NULL DEFAULT 'pending',
	`acceptedAt` timestamp,
	`designatedByUserId` int NOT NULL,
	`designatedAt` timestamp NOT NULL DEFAULT (now()),
	`effectiveDate` date,
	`expiresAt` timestamp,
	`notes` text,
	`blockchainHash` varchar(255),
	`status` enum('active','superseded','revoked','executed') NOT NULL DEFAULT 'active',
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `house_succession_designations_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `house_templates` (
	`id` int AUTO_INCREMENT NOT NULL,
	`templateCode` varchar(50) NOT NULL,
	`name` varchar(255) NOT NULL,
	`description` text,
	`targetAudience` enum('employee_transition','external_partner','community_member','family_branch','business_entity') NOT NULL,
	`defaultTrustType` enum('living','revocable','irrevocable','dynasty') DEFAULT 'living',
	`defaultHouseType` enum('bloodline','mirrored','adaptive') DEFAULT 'adaptive',
	`defaultInterHouseSplit` decimal(5,2) NOT NULL DEFAULT '60.00',
	`defaultInterHouseDistribution` decimal(5,2) NOT NULL DEFAULT '40.00',
	`defaultIntraHouseOperations` decimal(5,2) NOT NULL DEFAULT '70.00',
	`defaultIntraHouseInheritance` decimal(5,2) NOT NULL DEFAULT '30.00',
	`activationRequirements` json,
	`educationalModules` json,
	`estimatedActivationTime` varchar(50),
	`isActive` boolean NOT NULL DEFAULT true,
	`sortOrder` int NOT NULL DEFAULT 0,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `house_templates_id` PRIMARY KEY(`id`),
	CONSTRAINT `house_templates_templateCode_unique` UNIQUE(`templateCode`)
);
--> statement-breakpoint
CREATE TABLE `houses` (
	`id` int AUTO_INCREMENT NOT NULL,
	`name` varchar(255) NOT NULL,
	`publicAlias` varchar(255),
	`houseType` enum('root','bloodline','mirrored','adaptive') NOT NULL,
	`legacyHouseType` enum('root','family','business','community'),
	`parentHouseId` int,
	`ownerUserId` int NOT NULL,
	`trustName` varchar(255),
	`trustType` enum('living','revocable','irrevocable','dynasty') DEFAULT 'living',
	`trustEIN` varchar(20),
	`interHouseSplit` decimal(5,2) NOT NULL DEFAULT '60.00',
	`interHouseDistribution` decimal(5,2) NOT NULL DEFAULT '40.00',
	`intraHouseOperations` decimal(5,2) NOT NULL DEFAULT '70.00',
	`intraHouseInheritance` decimal(5,2) NOT NULL DEFAULT '30.00',
	`totalAssets` decimal(20,2) NOT NULL DEFAULT '0',
	`totalIncome` decimal(20,2) NOT NULL DEFAULT '0',
	`totalDistributed` decimal(20,2) NOT NULL DEFAULT '0',
	`operationsBalance` decimal(20,2) NOT NULL DEFAULT '0',
	`inheritanceReserve` decimal(20,2) NOT NULL DEFAULT '0',
	`rootAuthorityReserve` decimal(20,2) NOT NULL DEFAULT '0',
	`circulationPool` decimal(20,2) NOT NULL DEFAULT '0',
	`ancestralTreasury` decimal(20,2) NOT NULL DEFAULT '0',
	`successionStatus` enum('stable','interim','pending_confirmation') DEFAULT 'stable',
	`interimCustodianId` int,
	`successionStartedAt` timestamp,
	`status` enum('template','forming','pending_activation','active','suspended','dissolved') NOT NULL DEFAULT 'template',
	`activationPathway` enum('employee_transition','external_partner','business_first','community_member','family_branch'),
	`linkedBusinessEntityId` int,
	`businessVerificationStatus` enum('not_applicable','pending_verification','verified','rejected') DEFAULT 'not_applicable',
	`businessVerifiedAt` timestamp,
	`businessVerifiedByUserId` int,
	`voluntaryContributionRate` decimal(5,2) DEFAULT '0.00',
	`contributionFrequency` enum('none','monthly','quarterly','annually') DEFAULT 'none',
	`lastContributionDate` timestamp,
	`totalContributed` decimal(20,2) DEFAULT '0.00',
	`distributionEligible` boolean DEFAULT false,
	`distributionTier` enum('observer','participant','contributor','partner') DEFAULT 'observer',
	`platformSubscriptionTier` enum('free','basic','professional','enterprise') DEFAULT 'free',
	`monthlySubscriptionFee` decimal(10,2) DEFAULT '0.00',
	`totalPlatformFeesGenerated` decimal(20,2) DEFAULT '0.00',
	`totalToolUsageFees` decimal(20,2) DEFAULT '0.00',
	`totalReferralCommissions` decimal(20,2) DEFAULT '0.00',
	`totalMarketplaceFees` decimal(20,2) DEFAULT '0.00',
	`referredHousesCount` int DEFAULT 0,
	`lastPlatformActivityAt` timestamp,
	`trainingCompletionStatus` enum('not_started','in_progress','completed') DEFAULT 'not_started',
	`trainingCompletedAt` timestamp,
	`requiredCoursesCompleted` int DEFAULT 0,
	`totalRequiredCourses` int DEFAULT 8,
	`templateId` int,
	`generation` int NOT NULL DEFAULT 1,
	`isGenesis` boolean DEFAULT false,
	`genesisRIN` varchar(50),
	`genesisHash` varchar(255),
	`statementOfPurpose` text,
	`flameLightingTimestamp` timestamp,
	`genesisDeclarationDocId` int,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `houses_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `impact_metrics` (
	`id` int AUTO_INCREMENT NOT NULL,
	`metricType` enum('employees_trained','employees_transitioned','businesses_formed','contractor_retention','contractor_revenue','jobs_created','community_members_served','scholarships_awarded','volunteer_hours') NOT NULL,
	`periodType` enum('monthly','quarterly','annual') NOT NULL,
	`periodStart` timestamp NOT NULL,
	`periodEnd` timestamp NOT NULL,
	`targetValue` decimal(15,2),
	`actualValue` decimal(15,2) NOT NULL,
	`previousPeriodValue` decimal(15,2),
	`entityId` int,
	`entityName` varchar(255),
	`programName` varchar(255),
	`grantId` varchar(100),
	`notes` text,
	`dataSource` varchar(255),
	`verifiedBy` int,
	`verifiedAt` timestamp,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `impact_metrics_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `incidents` (
	`id` int AUTO_INCREMENT NOT NULL,
	`title` varchar(255) NOT NULL,
	`description` text NOT NULL,
	`incidentType` enum('security_breach','data_loss','system_outage','compliance_violation','financial_irregularity','safety_incident','other') NOT NULL,
	`incidentSeverity` enum('low','medium','high','critical') NOT NULL,
	`reportedById` int NOT NULL,
	`reportedAt` timestamp NOT NULL DEFAULT (now()),
	`assignedToId` int,
	`rootCause` text,
	`resolution` text,
	`resolvedAt` timestamp,
	`preventiveMeasures` text,
	`incidentStatus` enum('reported','investigating','resolved','closed') NOT NULL DEFAULT 'reported',
	`ledgerRef` varchar(255),
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `incidents_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `income_events` (
	`id` int AUTO_INCREMENT NOT NULL,
	`sourceType` enum('business','investment','grant','donation','other') NOT NULL,
	`sourceId` int,
	`houseId` int NOT NULL,
	`grossAmount` decimal(20,2) NOT NULL,
	`netAmount` decimal(20,2) NOT NULL,
	`description` text,
	`incomeDate` timestamp NOT NULL DEFAULT (now()),
	`status` enum('pending','processed','distributed') NOT NULL DEFAULT 'pending',
	`blockchainHash` varchar(255),
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	CONSTRAINT `income_events_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `inflow_capture` (
	`id` int AUTO_INCREMENT NOT NULL,
	`houseId` int NOT NULL,
	`sourceType` enum('sale','gift_received','investment_return','grant','donation','royalty','interest','other') NOT NULL,
	`grossAmount` decimal(20,8) NOT NULL,
	`currency` varchar(10) NOT NULL DEFAULT 'USD',
	`sourceDescription` text,
	`externalReference` varchar(255),
	`inflowStatus` enum('pending','validated','allocated','failed','manual_review') NOT NULL DEFAULT 'pending',
	`allocationTransactionId` int,
	`receivedAt` timestamp NOT NULL DEFAULT (now()),
	`processedAt` timestamp,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	CONSTRAINT `inflow_capture_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `inheritance_queue` (
	`id` int AUTO_INCREMENT NOT NULL,
	`houseId` int NOT NULL,
	`beneficiaryUserId` int NOT NULL,
	`amount` decimal(20,2) NOT NULL,
	`vestingDate` timestamp,
	`status` enum('accumulating','vested','distributed','forfeited') NOT NULL DEFAULT 'accumulating',
	`distributionEventId` int,
	`notes` text,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `inheritance_queue_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `integrity_triggers` (
	`id` int AUTO_INCREMENT NOT NULL,
	`triggerType` enum('gift_to_sale_disproportion','treasury_depletion','unauthorized_transfer','misalignment_detection','continuity_beacon') NOT NULL,
	`houseId` int NOT NULL,
	`severity` enum('warning','critical','emergency') NOT NULL DEFAULT 'warning',
	`thresholdValue` decimal(20,2),
	`actualValue` decimal(20,2),
	`description` text,
	`autoFreezeActivated` boolean NOT NULL DEFAULT false,
	`ceremonialWarningIssued` boolean NOT NULL DEFAULT false,
	`realignmentRequired` boolean NOT NULL DEFAULT false,
	`resolvedAt` timestamp,
	`resolvedByUserId` int,
	`resolutionNotes` text,
	`status` enum('active','acknowledged','resolved','escalated') NOT NULL DEFAULT 'active',
	`blockchainHash` varchar(255),
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `integrity_triggers_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `international_compliance_filings` (
	`id` int AUTO_INCREMENT NOT NULL,
	`houseId` int NOT NULL,
	`entityId` int,
	`entityType` enum('foreign_entity','international_trust','foreign_account') NOT NULL,
	`filingType` enum('fbar','fatca_8938','form_3520','form_3520a','form_5471','form_5472','form_8865','form_8858','crs_report') NOT NULL,
	`taxYear` int NOT NULL,
	`dueDate` timestamp NOT NULL,
	`extendedDueDate` timestamp,
	`status` enum('not_started','in_progress','ready_to_file','filed','accepted','rejected') NOT NULL DEFAULT 'not_started',
	`filedDate` timestamp,
	`confirmationNumber` varchar(100),
	`documentId` int,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `international_compliance_filings_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `international_jurisdictions` (
	`id` int AUTO_INCREMENT NOT NULL,
	`countryCode` varchar(3) NOT NULL,
	`countryName` varchar(255) NOT NULL,
	`region` enum('north_america','south_america','europe','africa','asia','oceania','caribbean','middle_east') NOT NULL,
	`legalSystem` enum('common_law','civil_law','religious_law','customary_law','mixed') NOT NULL,
	`currency` varchar(3) NOT NULL,
	`currencyName` varchar(100),
	`corporateTaxRate` decimal(5,2),
	`vatRate` decimal(5,2),
	`withholdingTaxRate` decimal(5,2),
	`hasTaxTreatyWithUS` boolean DEFAULT false,
	`taxTreatyDetails` text,
	`nonprofitRecognition` boolean DEFAULT false,
	`charityRegistrationRequired` boolean DEFAULT false,
	`charityRegistrationAuthority` varchar(255),
	`swiftRequired` boolean DEFAULT true,
	`ibanRequired` boolean DEFAULT false,
	`fatcaParticipant` boolean DEFAULT false,
	`crsParticipant` boolean DEFAULT false,
	`amlRequirements` text,
	`kycRequirements` text,
	`availableEntityTypes` json,
	`isActive` boolean DEFAULT true,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `international_jurisdictions_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `international_trusts` (
	`id` int AUTO_INCREMENT NOT NULL,
	`houseId` int NOT NULL,
	`trustName` varchar(255) NOT NULL,
	`trustType` enum('asset_protection','dynasty','charitable','spendthrift','discretionary','fixed_interest','purpose','hybrid') NOT NULL,
	`jurisdictionId` int NOT NULL,
	`countryCode` varchar(3) NOT NULL,
	`governingLaw` varchar(255),
	`settlor` json,
	`trustees` json,
	`protector` json,
	`beneficiaries` json,
	`trustDeed` text,
	`letterOfWishes` text,
	`initialSettlement` decimal(18,2),
	`currentValue` decimal(18,2),
	`assets` json,
	`status` enum('draft','established','active','frozen','terminated') NOT NULL DEFAULT 'draft',
	`establishedDate` timestamp,
	`terminationDate` timestamp,
	`usReportingRequired` boolean DEFAULT true,
	`form3520Filed` boolean DEFAULT false,
	`form3520ADue` timestamp,
	`luvLedgerAccountId` int,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `international_trusts_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `invoice_line_items` (
	`id` int AUTO_INCREMENT NOT NULL,
	`invoiceId` int NOT NULL,
	`chargeCodeId` int NOT NULL,
	`description` text,
	`hours` decimal(6,2) NOT NULL,
	`rate` decimal(10,2) NOT NULL,
	`amount` decimal(15,2) NOT NULL,
	CONSTRAINT `invoice_line_items_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `job_applications` (
	`id` int AUTO_INCREMENT NOT NULL,
	`positionId` varchar(100) NOT NULL,
	`positionTitle` varchar(255) NOT NULL,
	`entity` varchar(255) NOT NULL,
	`firstName` varchar(100) NOT NULL,
	`lastName` varchar(100) NOT NULL,
	`email` varchar(320) NOT NULL,
	`phone` varchar(50),
	`currentRole` varchar(255),
	`yearsExperience` varchar(50),
	`relevantSkills` text,
	`whyInterested` text,
	`coverLetter` text,
	`resumeUrl` text,
	`resumeFileName` varchar(255),
	`resumeFileKey` varchar(500),
	`status` enum('received','screening','phone_screen','interview_scheduled','interview_completed','reference_check','offer_extended','offer_accepted','hired','rejected','withdrawn') NOT NULL DEFAULT 'received',
	`statusNotes` text,
	`interviewDate` timestamp,
	`interviewType` enum('phone','video','in_person'),
	`interviewNotes` text,
	`interviewScore` int,
	`panelReviewers` json,
	`panelScores` json,
	`panelNotes` json,
	`decisionMadeBy` int,
	`decisionDate` timestamp,
	`decisionReason` text,
	`offeredSalary` decimal(10,2),
	`offeredStartDate` date,
	`offerExpiresAt` timestamp,
	`appliedAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `job_applications_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `jurisdictions` (
	`id` int AUTO_INCREMENT NOT NULL,
	`jurisdiction_code` varchar(20) NOT NULL,
	`jurisdiction_name` varchar(255) NOT NULL,
	`jurisdiction_type` enum('country','state','province','territory','region','city','special_zone') NOT NULL,
	`parent_jurisdiction_id` int,
	`crypto_legal_status` enum('legal','restricted','prohibited','unregulated','unknown') NOT NULL DEFAULT 'unknown',
	`kyc_required` boolean NOT NULL DEFAULT false,
	`aml_required` boolean NOT NULL DEFAULT false,
	`tax_treaty_countries` json,
	`vat_rate` decimal(5,2),
	`default_timezone` varchar(100),
	`is_operational` boolean NOT NULL DEFAULT true,
	`restriction_notes` text,
	`created_at` timestamp NOT NULL DEFAULT (now()),
	`updated_at` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `jurisdictions_id` PRIMARY KEY(`id`),
	CONSTRAINT `jurisdictions_jurisdiction_code_unique` UNIQUE(`jurisdiction_code`)
);
--> statement-breakpoint
CREATE TABLE `language_lessons` (
	`id` int AUTO_INCREMENT NOT NULL,
	`languageId` int NOT NULL,
	`title` varchar(200) NOT NULL,
	`level` enum('beginner','intermediate','advanced') NOT NULL,
	`lessonType` enum('vocabulary','pronunciation','conversation','ceremony','story','chant') NOT NULL,
	`content` json,
	`audioUrl` varchar(500),
	`orderIndex` int NOT NULL DEFAULT 0,
	`tokensReward` int DEFAULT 15,
	`status` enum('draft','active','archived') NOT NULL DEFAULT 'draft',
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	CONSTRAINT `language_lessons_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `learning_pathways` (
	`id` int AUTO_INCREMENT NOT NULL,
	`name` varchar(255) NOT NULL,
	`code` varchar(50) NOT NULL,
	`description` text,
	`targetStage` enum('senior_employee','contractor','certified_contractor','business_owner','house_member') NOT NULL,
	`targetDepartment` varchar(50),
	`targetCertification` int,
	`steps` json,
	`estimatedDurationWeeks` int,
	`aiGenerated` boolean NOT NULL DEFAULT false,
	`aiGeneratedAt` timestamp,
	`isActive` boolean NOT NULL DEFAULT true,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `learning_pathways_id` PRIMARY KEY(`id`),
	CONSTRAINT `learning_pathways_code_unique` UNIQUE(`code`)
);
--> statement-breakpoint
CREATE TABLE `ledger_access_logs` (
	`id` int AUTO_INCREMENT NOT NULL,
	`house_ledger_id` int NOT NULL,
	`accessed_by_user_id` varchar(255) NOT NULL,
	`ledger_access_type` enum('view','export','audit','fraud_investigation') NOT NULL,
	`access_reason` text,
	`access_approved_by` varchar(255),
	`ip_address` varchar(45),
	`access_granted` boolean DEFAULT false,
	`created_at` timestamp DEFAULT (now()),
	CONSTRAINT `ledger_access_logs_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `library_books` (
	`id` int AUTO_INCREMENT NOT NULL,
	`title` varchar(500) NOT NULL,
	`author` varchar(255) NOT NULL,
	`isbn` varchar(20),
	`coverImageUrl` text,
	`genre` varchar(100),
	`subGenre` varchar(100),
	`lawsPillar` enum('land','air','water','self'),
	`readingLevel` enum('k_2','3_5','6_8','9_12','adult') NOT NULL,
	`lexileScore` int,
	`pageCount` int,
	`estimatedReadingMinutes` int,
	`description` text,
	`contentUrl` text,
	`contentType` enum('pdf','epub','html','text') DEFAULT 'text',
	`chapters` json,
	`publisher` varchar(255),
	`publicationYear` int,
	`language` varchar(50) DEFAULT 'English',
	`hasAudioVersion` boolean DEFAULT false,
	`audioUrl` text,
	`hasQuizzes` boolean DEFAULT false,
	`hasDiscussionGuide` boolean DEFAULT false,
	`isActive` boolean NOT NULL DEFAULT true,
	`isFeatured` boolean DEFAULT false,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `library_books_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `lifecycle_events` (
	`id` int AUTO_INCREMENT NOT NULL,
	`entityType` enum('house','business','property','worker','document','tax_return','restoration_case','ledger_account','payroll') NOT NULL,
	`entityId` int NOT NULL,
	`entityName` varchar(255),
	`eventType` enum('created','activated','updated','deactivated','archived','filed','approved','rejected','submitted','completed','transferred','dissolved','acquired','sold','hired','terminated') NOT NULL,
	`eventDescription` text,
	`houseId` int,
	`userId` int,
	`luvLedgerTransactionId` int,
	`blockchainHash` varchar(128),
	`financialImpact` decimal(18,2),
	`impactType` enum('income','expense','asset','liability','neutral'),
	`metadata` json,
	`previousState` json,
	`newState` json,
	`eventTimestamp` timestamp NOT NULL DEFAULT (now()),
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	CONSTRAINT `lifecycle_events_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `living_scrolls` (
	`id` int AUTO_INCREMENT NOT NULL,
	`studentProfileId` int NOT NULL,
	`scrollType` enum('voice_scroll','house_lexicon','translation_book','mastery_scroll') NOT NULL,
	`title` varchar(200) NOT NULL,
	`languageId` int,
	`content` json,
	`entriesCount` int DEFAULT 0,
	`isPublic` boolean DEFAULT false,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `living_scrolls_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `luvchain_blocks` (
	`id` int AUTO_INCREMENT NOT NULL,
	`blockNumber` int NOT NULL,
	`blockHash` varchar(66) NOT NULL,
	`previousHash` varchar(66) NOT NULL,
	`timestamp` timestamp NOT NULL DEFAULT (now()),
	`nonce` int NOT NULL DEFAULT 0,
	`difficulty` int NOT NULL DEFAULT 1,
	`merkleRoot` varchar(66),
	`transactionCount` int NOT NULL DEFAULT 0,
	`size` int NOT NULL DEFAULT 0,
	`validator` varchar(255),
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	CONSTRAINT `luvchain_blocks_id` PRIMARY KEY(`id`),
	CONSTRAINT `luvchain_blocks_blockNumber_unique` UNIQUE(`blockNumber`),
	CONSTRAINT `luvchain_blocks_blockHash_unique` UNIQUE(`blockHash`)
);
--> statement-breakpoint
CREATE TABLE `luvchain_smart_contracts` (
	`id` int AUTO_INCREMENT NOT NULL,
	`contractAddress` varchar(66) NOT NULL,
	`creatorWalletId` int NOT NULL,
	`contractType` enum('certificate','token_transfer','trust_distribution','grant_allocation','entity_creation','escrow','subscription','custom') NOT NULL,
	`name` varchar(255) NOT NULL,
	`description` text,
	`code` text,
	`abi` json,
	`state` json,
	`isActive` boolean NOT NULL DEFAULT true,
	`deployedAt` timestamp NOT NULL DEFAULT (now()),
	`lastExecutedAt` timestamp,
	`executionCount` int NOT NULL DEFAULT 0,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	CONSTRAINT `luvchain_smart_contracts_id` PRIMARY KEY(`id`),
	CONSTRAINT `luvchain_smart_contracts_contractAddress_unique` UNIQUE(`contractAddress`)
);
--> statement-breakpoint
CREATE TABLE `luvchain_transactions` (
	`id` int AUTO_INCREMENT NOT NULL,
	`transactionHash` varchar(66) NOT NULL,
	`blockId` int,
	`fromWalletId` int NOT NULL,
	`toWalletId` int,
	`contractId` int,
	`transactionType` enum('transfer','contract_deploy','contract_call','certificate_mint','entity_register','trust_setup','grant_disburse','reward') NOT NULL,
	`amount` decimal(20,8) NOT NULL DEFAULT '0',
	`gasUsed` int NOT NULL DEFAULT 0,
	`gasFee` decimal(20,8) NOT NULL DEFAULT '0',
	`data` json,
	`status` enum('pending','confirmed','failed') NOT NULL DEFAULT 'pending',
	`confirmations` int NOT NULL DEFAULT 0,
	`errorMessage` text,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`confirmedAt` timestamp,
	CONSTRAINT `luvchain_transactions_id` PRIMARY KEY(`id`),
	CONSTRAINT `luvchain_transactions_transactionHash_unique` UNIQUE(`transactionHash`)
);
--> statement-breakpoint
CREATE TABLE `main_house_ledger` (
	`id` int AUTO_INCREMENT NOT NULL,
	`ledger_name` varchar(255) DEFAULT 'Root Authority Ledger',
	`total_treasury_balance` decimal(18,2) DEFAULT '0.00',
	`total_houses_connected` int DEFAULT 0,
	`total_transactions_processed` int DEFAULT 0,
	`last_aggregation_at` timestamp,
	`aggregation_hash` varchar(64),
	`created_at` timestamp DEFAULT (now()),
	`updated_at` timestamp DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `main_house_ledger_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `mastery_certificates` (
	`id` int AUTO_INCREMENT NOT NULL,
	`studentProfileId` int NOT NULL,
	`certificateType` enum('course_completion','house_graduation','language_mastery','stem_mastery','sovereign_diploma') NOT NULL,
	`title` varchar(255) NOT NULL,
	`description` text,
	`courseId` int,
	`houseId` int,
	`languageId` int,
	`level` varchar(50),
	`blockchainHash` varchar(255),
	`verificationCode` varchar(100),
	`issuedAt` timestamp NOT NULL DEFAULT (now()),
	`metadata` json,
	CONSTRAINT `mastery_certificates_id` PRIMARY KEY(`id`),
	CONSTRAINT `mastery_certificates_verificationCode_unique` UNIQUE(`verificationCode`)
);
--> statement-breakpoint
CREATE TABLE `matchmaking_queue` (
	`id` int AUTO_INCREMENT NOT NULL,
	`playerId` int NOT NULL,
	`gameId` int NOT NULL,
	`matchType` enum('casual','ranked','tournament') NOT NULL DEFAULT 'casual',
	`playerRating` int DEFAULT 1000,
	`ratingRange` int DEFAULT 200,
	`queuedAt` timestamp NOT NULL DEFAULT (now()),
	`expiresAt` timestamp,
	`status` enum('waiting','matched','expired','cancelled') NOT NULL DEFAULT 'waiting',
	`matchedWithId` int,
	`matchedAt` timestamp,
	CONSTRAINT `matchmaking_queue_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `meeting_attendance` (
	`id` int AUTO_INCREMENT NOT NULL,
	`eventId` int NOT NULL,
	`userId` int NOT NULL,
	`status` enum('invited','confirmed','declined','attended','absent','excused') NOT NULL DEFAULT 'invited',
	`responseTime` timestamp,
	`checkInTime` timestamp,
	`checkOutTime` timestamp,
	`excuseReason` text,
	`approvedBy` int,
	`notes` text,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `meeting_attendance_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `meeting_chats` (
	`id` int AUTO_INCREMENT NOT NULL,
	`meetingId` int NOT NULL,
	`chatId` int NOT NULL,
	`persistAfterMeeting` boolean DEFAULT true,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	CONSTRAINT `meeting_chats_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `meeting_participants` (
	`id` int AUTO_INCREMENT NOT NULL,
	`meetingId` int NOT NULL,
	`userId` int,
	`guestEmail` varchar(320),
	`guestName` varchar(255),
	`role` enum('host','co_host','presenter','attendee') NOT NULL DEFAULT 'attendee',
	`inviteStatus` enum('pending','accepted','declined','tentative') NOT NULL DEFAULT 'pending',
	`joinedAt` timestamp,
	`leftAt` timestamp,
	`totalDuration` int,
	`canShareScreen` boolean DEFAULT true,
	`canUnmute` boolean DEFAULT true,
	`canChat` boolean DEFAULT true,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	CONSTRAINT `meeting_participants_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `meeting_reminders` (
	`id` int AUTO_INCREMENT NOT NULL,
	`meetingId` int NOT NULL,
	`reminderType` enum('15_min','1_hour','24_hour') NOT NULL,
	`sentAt` timestamp NOT NULL DEFAULT (now()),
	`recipientCount` int DEFAULT 0,
	CONSTRAINT `meeting_reminders_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `meetings` (
	`id` int AUTO_INCREMENT NOT NULL,
	`title` varchar(255) NOT NULL,
	`description` text,
	`hostId` int NOT NULL,
	`scheduledAt` timestamp NOT NULL,
	`duration` int NOT NULL DEFAULT 60,
	`timezone` varchar(50) DEFAULT 'America/New_York',
	`status` enum('scheduled','in_progress','completed','cancelled') NOT NULL DEFAULT 'scheduled',
	`provider` enum('daily','teams','custom') NOT NULL DEFAULT 'daily',
	`roomUrl` text,
	`roomName` varchar(255),
	`providerMeetingId` varchar(255),
	`isRecorded` boolean DEFAULT false,
	`recordingUrl` text,
	`waitingRoomEnabled` boolean DEFAULT false,
	`maxParticipants` int DEFAULT 100,
	`isRecurring` boolean DEFAULT false,
	`recurrenceRule` varchar(255),
	`parentMeetingId` int,
	`metadata` json,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	`startedAt` timestamp,
	`endedAt` timestamp,
	CONSTRAINT `meetings_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `member_credentials` (
	`id` int AUTO_INCREMENT NOT NULL,
	`userId` int NOT NULL,
	`houseId` int,
	`credentialId` varchar(50) NOT NULL,
	`verificationCode` varchar(20) NOT NULL,
	`entryPath` enum('game','academy','direct','employment','legacy') NOT NULL,
	`accessLevel` enum('basic','standard','premium','elite','sovereign') NOT NULL DEFAULT 'basic',
	`status` enum('active','suspended','revoked','expired') NOT NULL DEFAULT 'active',
	`issuedAt` timestamp NOT NULL DEFAULT (now()),
	`expiresAt` timestamp,
	`lastVerifiedAt` timestamp,
	`metadata` json,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()),
	CONSTRAINT `member_credentials_id` PRIMARY KEY(`id`),
	CONSTRAINT `member_credentials_credentialId_unique` UNIQUE(`credentialId`)
);
--> statement-breakpoint
CREATE TABLE `metrics` (
	`id` int AUTO_INCREMENT NOT NULL,
	`name` varchar(255) NOT NULL,
	`description` text,
	`metricCategory` enum('financial','operational','program','hr','compliance','impact') NOT NULL,
	`targetValue` decimal(20,4),
	`actualValue` decimal(20,4),
	`unit` varchar(50),
	`periodStart` timestamp NOT NULL,
	`periodEnd` timestamp NOT NULL,
	`departmentId` int,
	`programId` int,
	`metricStatus` enum('on_track','at_risk','off_track','achieved') NOT NULL DEFAULT 'on_track',
	`notes` text,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `metrics_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `mirror_tokens` (
	`id` int AUTO_INCREMENT NOT NULL,
	`tokenCode` varchar(64) NOT NULL,
	`ownerUserId` int NOT NULL,
	`houseId` int,
	`status` enum('available','locked','used','transferred') NOT NULL DEFAULT 'available',
	`lockExpiresAt` timestamp,
	`usedForHouseId` int,
	`usedAt` timestamp,
	`previousOwnerId` int,
	`transferredAt` timestamp,
	`transferBlockchainHash` varchar(255),
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `mirror_tokens_id` PRIMARY KEY(`id`),
	CONSTRAINT `mirror_tokens_tokenCode_unique` UNIQUE(`tokenCode`)
);
--> statement-breakpoint
CREATE TABLE `multi_chain_crypto_transactions` (
	`id` int AUTO_INCREMENT NOT NULL,
	`transaction_hash` varchar(255) NOT NULL,
	`network_id` int NOT NULL,
	`token_id` int NOT NULL,
	`crypto_tx_type` enum('incoming','outgoing','internal','swap','bridge','stake','unstake') NOT NULL,
	`from_wallet_id` int,
	`to_wallet_id` int,
	`from_address` varchar(255) NOT NULL,
	`to_address` varchar(255) NOT NULL,
	`amount` decimal(36,18) NOT NULL,
	`amount_usd` decimal(20,2),
	`exchange_rate_at_time` decimal(20,8),
	`gas_fee` decimal(36,18),
	`gas_fee_usd` decimal(20,2),
	`block_number` varchar(100),
	`block_timestamp` timestamp,
	`confirmations` int NOT NULL DEFAULT 0,
	`crypto_tx_status` enum('pending','confirming','confirmed','failed','cancelled','replaced') NOT NULL DEFAULT 'pending',
	`memo` text,
	`metadata` json,
	`luv_ledger_transaction_id` int,
	`created_at` timestamp NOT NULL DEFAULT (now()),
	`updated_at` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `multi_chain_crypto_transactions_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `multi_chain_wallets` (
	`id` int AUTO_INCREMENT NOT NULL,
	`user_id` int,
	`house_id` int,
	`business_entity_id` int,
	`network_id` int NOT NULL,
	`wallet_address` varchar(255) NOT NULL,
	`wallet_label` varchar(255),
	`wallet_type` enum('hot','cold','custodial','non_custodial','multisig','smart_contract') NOT NULL DEFAULT 'hot',
	`public_key` text,
	`encrypted_private_key` text,
	`key_derivation_path` varchar(100),
	`is_multisig` boolean NOT NULL DEFAULT false,
	`required_signatures` int,
	`total_signers` int,
	`signer_addresses` json,
	`wallet_status` enum('active','frozen','archived','compromised') NOT NULL DEFAULT 'active',
	`is_verified` boolean NOT NULL DEFAULT false,
	`verified_at` timestamp,
	`created_at` timestamp NOT NULL DEFAULT (now()),
	`updated_at` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `multi_chain_wallets_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `negotiation_history` (
	`id` int AUTO_INCREMENT NOT NULL,
	`negotiationId` int NOT NULL,
	`activityType` enum('initial_upload','ai_analysis','strategy_generated','counter_offer_sent','counter_offer_received','term_accepted','term_rejected','meeting_scheduled','meeting_completed','document_revised','final_agreement','negotiation_closed') NOT NULL,
	`description` text NOT NULL,
	`actorType` enum('user','counterparty','system','ai') NOT NULL,
	`actorName` varchar(255),
	`metadata` json,
	`documentUrl` varchar(500),
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	CONSTRAINT `negotiation_history_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `negotiation_playbooks` (
	`id` int AUTO_INCREMENT NOT NULL,
	`title` varchar(255) NOT NULL,
	`contractType` varchar(100) NOT NULL,
	`description` text,
	`commonTerms` json,
	`redFlags` json,
	`negotiationStrategies` json,
	`industry` varchar(100),
	`usageCount` int NOT NULL DEFAULT 0,
	`successRate` decimal(5,2),
	`isPublic` boolean NOT NULL DEFAULT true,
	`status` enum('draft','active','archived') NOT NULL DEFAULT 'active',
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `negotiation_playbooks_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `network_houses` (
	`id` int AUTO_INCREMENT NOT NULL,
	`sourceHouseId` int NOT NULL,
	`targetHouseId` int NOT NULL,
	`allocationPercentage` decimal(5,2) NOT NULL,
	`relationship` enum('child','sibling','partner','community') NOT NULL,
	`status` enum('active','inactive') NOT NULL DEFAULT 'active',
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `network_houses_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `nft_mint_queue` (
	`id` int AUTO_INCREMENT NOT NULL,
	`requesterId` int NOT NULL,
	`playerAchievementId` int,
	`nftType` enum('platinum_achievement','leaderboard_champion','tournament_winner','special_event','founder') NOT NULL,
	`name` varchar(255) NOT NULL,
	`description` text,
	`imageUrl` text,
	`attributes` json,
	`queueStatus` enum('pending','processing','minted','failed','cancelled') NOT NULL DEFAULT 'pending',
	`attempts` int NOT NULL DEFAULT 0,
	`lastAttemptAt` timestamp,
	`errorMessage` text,
	`mintedNftId` int,
	`transactionHash` varchar(255),
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `nft_mint_queue_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `notification_preferences` (
	`id` int AUTO_INCREMENT NOT NULL,
	`userId` int NOT NULL,
	`emailEnabled` boolean NOT NULL DEFAULT true,
	`pushEnabled` boolean NOT NULL DEFAULT true,
	`operationAlerts` boolean NOT NULL DEFAULT true,
	`tokenAlerts` boolean NOT NULL DEFAULT true,
	`academyAlerts` boolean NOT NULL DEFAULT true,
	`documentAlerts` boolean NOT NULL DEFAULT true,
	`approvalAlerts` boolean NOT NULL DEFAULT true,
	`systemAlerts` boolean NOT NULL DEFAULT true,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `notification_preferences_id` PRIMARY KEY(`id`),
	CONSTRAINT `notification_preferences_userId_unique` UNIQUE(`userId`)
);
--> statement-breakpoint
CREATE TABLE `notifications` (
	`id` int AUTO_INCREMENT NOT NULL,
	`userId` int NOT NULL,
	`type` enum('system','operation','token','academy','document','approval','alert','success','info') NOT NULL DEFAULT 'info',
	`title` varchar(255) NOT NULL,
	`message` text NOT NULL,
	`entityId` int,
	`referenceType` varchar(50),
	`referenceId` int,
	`actionUrl` varchar(500),
	`isRead` boolean NOT NULL DEFAULT false,
	`isPriority` boolean NOT NULL DEFAULT false,
	`metadata` json,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`readAt` timestamp,
	CONSTRAINT `notifications_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `offer_package_documents` (
	`id` int AUTO_INCREMENT NOT NULL,
	`offerId` int NOT NULL,
	`offerDocType` enum('offer_letter','position_description','compensation_schedule','nda','non_compete','background_check_auth','direct_deposit','w4','w9','i9','token_agreement','policy_acknowledgment','resume') NOT NULL,
	`title` varchar(255) NOT NULL,
	`content` text,
	`s3Key` varchar(1000),
	`s3Url` varchar(2000),
	`requiresSignature` boolean NOT NULL DEFAULT false,
	`signedAt` timestamp,
	`signatureData` text,
	`offerDocStatus` enum('draft','generated','sent','signed','rejected') NOT NULL DEFAULT 'draft',
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `offer_package_documents_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `offer_packages` (
	`id` int AUTO_INCREMENT NOT NULL,
	`resumeId` int NOT NULL,
	`familyMemberId` varchar(100) NOT NULL,
	`fullName` varchar(255) NOT NULL,
	`positionTitle` varchar(255) NOT NULL,
	`department` varchar(255),
	`entityId` varchar(100) NOT NULL,
	`entityName` varchar(255) NOT NULL,
	`reportsTo` varchar(255),
	`employmentType` enum('full_time','part_time','contractor','contingent') NOT NULL DEFAULT 'contingent',
	`baseSalary` decimal(12,2),
	`salaryFrequency` enum('hourly','weekly','biweekly','monthly','annually') DEFAULT 'annually',
	`tokenAllocation` int,
	`revenueSharePercent` decimal(5,2),
	`benefits` json,
	`proposedStartDate` timestamp,
	`contingencyConditions` text,
	`offerLetterGenerated` boolean NOT NULL DEFAULT false,
	`positionDescGenerated` boolean NOT NULL DEFAULT false,
	`compensationScheduleGenerated` boolean NOT NULL DEFAULT false,
	`ndaGenerated` boolean NOT NULL DEFAULT false,
	`nonCompeteGenerated` boolean NOT NULL DEFAULT false,
	`backgroundCheckAuthGenerated` boolean NOT NULL DEFAULT false,
	`taxFormsGenerated` boolean NOT NULL DEFAULT false,
	`tokenAgreementGenerated` boolean NOT NULL DEFAULT false,
	`offerStatus` enum('draft','pending_review','approved','sent','accepted','declined','expired') NOT NULL DEFAULT 'draft',
	`offerSentAt` timestamp,
	`offerAcceptedAt` timestamp,
	`signatureId` varchar(255),
	`createdBy` int,
	`approvedBy` int,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `offer_packages_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `office_document_collaborators` (
	`id` int AUTO_INCREMENT NOT NULL,
	`documentId` int NOT NULL,
	`userId` int NOT NULL,
	`canView` boolean DEFAULT true,
	`canEdit` boolean DEFAULT false,
	`canComment` boolean DEFAULT true,
	`canShare` boolean DEFAULT false,
	`canSign` boolean DEFAULT false,
	`canDelete` boolean DEFAULT false,
	`invitedBy` int,
	`invitedAt` timestamp DEFAULT (now()),
	`acceptedAt` timestamp,
	`lastAccessedAt` timestamp,
	CONSTRAINT `office_document_collaborators_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `office_document_comments` (
	`id` int AUTO_INCREMENT NOT NULL,
	`documentId` int NOT NULL,
	`versionId` int,
	`content` text NOT NULL,
	`anchorType` varchar(50),
	`anchorData` text,
	`parentCommentId` int,
	`isResolved` boolean DEFAULT false,
	`resolvedBy` int,
	`resolvedAt` timestamp,
	`authorId` int NOT NULL,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `office_document_comments_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `office_document_signatures` (
	`id` int AUTO_INCREMENT NOT NULL,
	`documentId` int NOT NULL,
	`signerId` int,
	`signerName` varchar(255) NOT NULL,
	`signerEmail` varchar(255) NOT NULL,
	`signerTitle` varchar(255),
	`signatureType` varchar(50) NOT NULL,
	`signatureData` text,
	`signatureImageUrl` varchar(1000),
	`pageNumber` int,
	`positionX` int,
	`positionY` int,
	`width` int,
	`height` int,
	`ipAddress` varchar(50),
	`userAgent` text,
	`signedAt` timestamp,
	`status` varchar(50) NOT NULL DEFAULT 'pending',
	`declineReason` text,
	`requestedBy` int,
	`requestedAt` timestamp DEFAULT (now()),
	`expiresAt` timestamp,
	`auditTrail` text,
	CONSTRAINT `office_document_signatures_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `office_document_templates` (
	`id` int AUTO_INCREMENT NOT NULL,
	`name` varchar(255) NOT NULL,
	`description` text,
	`templateDocType` enum('document','spreadsheet','presentation','pdf','form','contract','report') NOT NULL,
	`content` text,
	`fileKey` varchar(500),
	`fileUrl` varchar(1000),
	`category` varchar(100),
	`department` varchar(100),
	`entityType` varchar(100),
	`variables` text,
	`usageCount` int DEFAULT 0,
	`isPublic` boolean DEFAULT false,
	`createdBy` int NOT NULL,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `office_document_templates_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `office_document_versions` (
	`id` int AUTO_INCREMENT NOT NULL,
	`documentId` int NOT NULL,
	`versionNumber` int NOT NULL,
	`fileKey` varchar(500),
	`fileUrl` varchar(1000),
	`content` text,
	`changeDescription` text,
	`changedBy` int NOT NULL,
	`title` varchar(500),
	`status` varchar(50),
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	CONSTRAINT `office_document_versions_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `office_documents` (
	`id` int AUTO_INCREMENT NOT NULL,
	`title` varchar(500) NOT NULL,
	`description` text,
	`documentType` enum('document','spreadsheet','presentation','pdf','form','template','contract','report','other') NOT NULL DEFAULT 'document',
	`status` enum('draft','in_review','pending_signature','signed','final','archived','deleted') NOT NULL DEFAULT 'draft',
	`fileKey` varchar(500),
	`fileUrl` varchar(1000),
	`mimeType` varchar(100),
	`fileSize` int,
	`content` text,
	`entityType` varchar(100),
	`entityId` int,
	`department` varchar(100),
	`category` varchar(100),
	`tags` text,
	`isTemplate` boolean DEFAULT false,
	`templateId` int,
	`createdBy` int NOT NULL,
	`ownerId` int NOT NULL,
	`requiresSignature` boolean DEFAULT false,
	`signatureCount` int DEFAULT 0,
	`requiredSignatures` int DEFAULT 0,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	`finalizedAt` timestamp,
	`archivedAt` timestamp,
	CONSTRAINT `office_documents_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `onboarding_assessments` (
	`id` int AUTO_INCREMENT NOT NULL,
	`journeyId` int NOT NULL,
	`realm` enum('self','water','air','land') NOT NULL,
	`totalQuestions` int NOT NULL,
	`correctAnswers` int NOT NULL,
	`score` int NOT NULL,
	`passed` boolean NOT NULL,
	`attemptNumber` int NOT NULL DEFAULT 1,
	`startedAt` timestamp NOT NULL DEFAULT (now()),
	`completedAt` timestamp,
	`durationSeconds` int,
	CONSTRAINT `onboarding_assessments_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `onboarding_checklist_items` (
	`id` int AUTO_INCREMENT NOT NULL,
	`checklistId` int NOT NULL,
	`title` varchar(255) NOT NULL,
	`description` text,
	`category` enum('documentation','equipment','access','training','introduction','compliance','benefits','other') NOT NULL DEFAULT 'other',
	`dueWithinDays` int NOT NULL DEFAULT 7,
	`assignedTo` enum('employee','manager','hr','it','finance') NOT NULL DEFAULT 'employee',
	`isRequired` boolean NOT NULL DEFAULT true,
	`sortOrder` int NOT NULL DEFAULT 0,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	CONSTRAINT `onboarding_checklist_items_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `onboarding_checklists` (
	`id` int AUTO_INCREMENT NOT NULL,
	`name` varchar(255) NOT NULL,
	`description` text,
	`department` varchar(100),
	`positionLevel` enum('executive','manager','lead','coordinator','specialist','intern'),
	`isDefault` boolean NOT NULL DEFAULT false,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `onboarding_checklists_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `onboarding_house_setup` (
	`id` int AUTO_INCREMENT NOT NULL,
	`journeyId` int NOT NULL,
	`userId` int NOT NULL,
	`houseName` varchar(255) NOT NULL,
	`houseType` enum('individual','family','legacy') NOT NULL DEFAULT 'individual',
	`primaryBeneficiaryName` varchar(255),
	`primaryBeneficiaryRelation` varchar(100),
	`valuesAgreed` boolean NOT NULL DEFAULT false,
	`valuesAgreedAt` timestamp,
	`profileComplete` boolean NOT NULL DEFAULT false,
	`createdHouseId` int,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()),
	CONSTRAINT `onboarding_house_setup_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `onboarding_journeys` (
	`id` int AUTO_INCREMENT NOT NULL,
	`userId` int NOT NULL,
	`status` enum('not_started','in_progress','completed','abandoned') NOT NULL DEFAULT 'not_started',
	`currentStep` enum('welcome','self_intro','self_assessment','water_intro','water_assessment','air_intro','air_assessment','land_intro','land_assessment','house_setup','values_agreement','credential_issuance','complete') NOT NULL DEFAULT 'welcome',
	`selfCompleted` boolean NOT NULL DEFAULT false,
	`waterCompleted` boolean NOT NULL DEFAULT false,
	`airCompleted` boolean NOT NULL DEFAULT false,
	`landCompleted` boolean NOT NULL DEFAULT false,
	`selfScore` int,
	`waterScore` int,
	`airScore` int,
	`landScore` int,
	`progressPercent` int NOT NULL DEFAULT 0,
	`startedAt` timestamp NOT NULL DEFAULT (now()),
	`lastActivityAt` timestamp NOT NULL DEFAULT (now()),
	`completedAt` timestamp,
	`credentialId` varchar(50),
	CONSTRAINT `onboarding_journeys_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `onboarding_progress` (
	`id` int AUTO_INCREMENT NOT NULL,
	`companyId` int NOT NULL,
	`stepNumber` int NOT NULL,
	`stepName` varchar(100) NOT NULL,
	`stepCategory` enum('profile','services','integrations','payment','verification','training') NOT NULL,
	`onboardingStepStatus` enum('not_started','in_progress','completed','skipped','blocked') NOT NULL DEFAULT 'not_started',
	`progressData` json,
	`completedFields` json,
	`requiredFields` json,
	`startedAt` timestamp,
	`completedAt` timestamp,
	`estimatedMinutes` int,
	`actualMinutes` int,
	`notes` text,
	`blockedReason` text,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `onboarding_progress_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `onboarding_questions` (
	`id` int AUTO_INCREMENT NOT NULL,
	`questionId` varchar(50) NOT NULL,
	`realm` enum('self','water','air','land') NOT NULL,
	`questionText` text NOT NULL,
	`options` json NOT NULL,
	`correctOptionIndex` int NOT NULL,
	`explanation` text NOT NULL,
	`difficulty` enum('easy','medium','hard') NOT NULL DEFAULT 'medium',
	`orderIndex` int NOT NULL DEFAULT 0,
	`isActive` boolean NOT NULL DEFAULT true,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()),
	CONSTRAINT `onboarding_questions_id` PRIMARY KEY(`id`),
	CONSTRAINT `onboarding_questions_questionId_unique` UNIQUE(`questionId`)
);
--> statement-breakpoint
CREATE TABLE `onboarding_responses` (
	`id` int AUTO_INCREMENT NOT NULL,
	`assessmentId` int NOT NULL,
	`questionId` varchar(50) NOT NULL,
	`questionText` text NOT NULL,
	`selectedOption` varchar(255) NOT NULL,
	`correctOption` varchar(255) NOT NULL,
	`isCorrect` boolean NOT NULL,
	`explanation` text,
	`answeredAt` timestamp NOT NULL DEFAULT (now()),
	CONSTRAINT `onboarding_responses_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `onboarding_task_progress` (
	`id` int AUTO_INCREMENT NOT NULL,
	`onboardingId` int NOT NULL,
	`checklistItemId` int NOT NULL,
	`status` enum('pending','in_progress','completed','skipped','blocked') NOT NULL DEFAULT 'pending',
	`completedAt` timestamp,
	`completedBy` int,
	`notes` text,
	`dueDate` timestamp,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `onboarding_task_progress_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `online_course_details` (
	`id` int AUTO_INCREMENT NOT NULL,
	`courseId` int NOT NULL,
	`code` varchar(20) NOT NULL,
	`category` enum('financial_sovereignty','laws_framework','governance_civics','entrepreneurship','creative_enterprise','core_academic','professional_development','simulator_training') NOT NULL,
	`gradeLevel` enum('k_2','3_5','6_8','9_12','adult','all_ages') NOT NULL DEFAULT 'all_ages',
	`courseType` enum('proprietary','licensed','oer','partnership') NOT NULL DEFAULT 'proprietary',
	`deliveryMethod` enum('asynchronous','synchronous','hybrid','self_paced') NOT NULL DEFAULT 'asynchronous',
	`creditHours` decimal(4,2),
	`duration` varchar(50),
	`syllabus` text,
	`lmsUrl` text,
	`approvedBy` int,
	`approvedAt` timestamp,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `online_course_details_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `operating_procedures` (
	`id` int AUTO_INCREMENT NOT NULL,
	`title` varchar(255) NOT NULL,
	`description` text,
	`documentNumber` varchar(50),
	`category` enum('sop','manual','policy','guide','training','checklist','template','form') NOT NULL,
	`department` varchar(100),
	`entityId` int,
	`positionId` varchar(100),
	`version` varchar(20) NOT NULL DEFAULT '1.0',
	`status` enum('draft','review','approved','archived','superseded') NOT NULL DEFAULT 'draft',
	`content` text,
	`fileUrl` varchar(500),
	`effectiveDate` timestamp,
	`reviewDate` timestamp,
	`expirationDate` timestamp,
	`approvedBy` int,
	`approvedAt` timestamp,
	`createdBy` int NOT NULL,
	`tags` json,
	`relatedProcedures` json,
	`revisionHistory` json,
	`isRequired` boolean DEFAULT false,
	`requiredForDepartments` json,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `operating_procedures_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `parcels` (
	`id` int AUTO_INCREMENT NOT NULL,
	`addressLegalDesc` text NOT NULL,
	`parcelNumber` varchar(100),
	`acquisitionDate` timestamp,
	`acquisitionPrice` decimal(20,2),
	`currentValue` decimal(20,2),
	`useType` enum('hub','academy','community','storage','agricultural','residential','commercial','mixed') NOT NULL,
	`improvements` text,
	`acreage` decimal(10,4),
	`parcelOwnership` enum('trust','subsidiary','house') NOT NULL DEFAULT 'trust',
	`ownershipEntityId` int,
	`ledgerRef` varchar(255),
	`parcelStatus` enum('active','pending_sale','sold','transferred') NOT NULL DEFAULT 'active',
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `parcels_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `payment_fulfillments` (
	`id` int AUTO_INCREMENT NOT NULL,
	`payment_request_id` int NOT NULL,
	`crypto_transaction_id` int NOT NULL,
	`amount_applied` decimal(36,18) NOT NULL,
	`amount_applied_usd` decimal(20,2),
	`created_at` timestamp NOT NULL DEFAULT (now()),
	CONSTRAINT `payment_fulfillments_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `payment_requests` (
	`id` int AUTO_INCREMENT NOT NULL,
	`request_code` varchar(100) NOT NULL,
	`recipient_wallet_id` int NOT NULL,
	`recipient_user_id` int,
	`recipient_house_id` int,
	`requested_amount` decimal(36,18) NOT NULL,
	`requested_token_id` int NOT NULL,
	`requested_amount_usd` decimal(20,2),
	`allow_partial_payment` boolean NOT NULL DEFAULT false,
	`allow_any_token` boolean NOT NULL DEFAULT false,
	`accepted_token_ids` json,
	`description` text,
	`invoice_number` varchar(100),
	`expires_at` timestamp,
	`payment_request_status` enum('pending','partial','completed','expired','cancelled') NOT NULL DEFAULT 'pending',
	`total_received_amount` decimal(36,18) NOT NULL DEFAULT '0',
	`total_received_usd` decimal(20,2) NOT NULL DEFAULT '0',
	`created_at` timestamp NOT NULL DEFAULT (now()),
	`updated_at` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `payment_requests_id` PRIMARY KEY(`id`),
	CONSTRAINT `payment_requests_request_code_unique` UNIQUE(`request_code`)
);
--> statement-breakpoint
CREATE TABLE `payroll_periods` (
	`id` int AUTO_INCREMENT NOT NULL,
	`house_id` int NOT NULL,
	`business_entity_id` int,
	`period_start_date` timestamp NOT NULL,
	`period_end_date` timestamp NOT NULL,
	`pay_date` timestamp NOT NULL,
	`payroll_period_status` enum('open','processing','approved','paid','closed') NOT NULL DEFAULT 'open',
	`created_at` timestamp NOT NULL DEFAULT (now()),
	`updated_at` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `payroll_periods_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `payroll_records` (
	`id` int AUTO_INCREMENT NOT NULL,
	`positionHolderId` int NOT NULL,
	`positionId` int NOT NULL,
	`businessEntityId` int NOT NULL,
	`houseId` int NOT NULL,
	`payPeriodStart` timestamp NOT NULL,
	`payPeriodEnd` timestamp NOT NULL,
	`payDate` timestamp NOT NULL,
	`regularHours` decimal(6,2) DEFAULT '0',
	`overtimeHours` decimal(6,2) DEFAULT '0',
	`grossPay` decimal(12,2) NOT NULL,
	`regularPay` decimal(12,2) DEFAULT '0',
	`overtimePay` decimal(12,2) DEFAULT '0',
	`bonusPay` decimal(12,2) DEFAULT '0',
	`commissionPay` decimal(12,2) DEFAULT '0',
	`federalIncomeTax` decimal(10,2) DEFAULT '0',
	`stateIncomeTax` decimal(10,2) DEFAULT '0',
	`localIncomeTax` decimal(10,2) DEFAULT '0',
	`socialSecurityTax` decimal(10,2) DEFAULT '0',
	`medicareTax` decimal(10,2) DEFAULT '0',
	`healthInsurance` decimal(10,2) DEFAULT '0',
	`retirement401k` decimal(10,2) DEFAULT '0',
	`otherDeductions` decimal(10,2) DEFAULT '0',
	`deductionDetails` json,
	`totalDeductions` decimal(12,2) NOT NULL,
	`netPay` decimal(12,2) NOT NULL,
	`paymentMethod` enum('direct_deposit','check','cash') DEFAULT 'direct_deposit',
	`checkNumber` varchar(20),
	`employerSocialSecurity` decimal(10,2) DEFAULT '0',
	`employerMedicare` decimal(10,2) DEFAULT '0',
	`employerFuta` decimal(10,2) DEFAULT '0',
	`employerSuta` decimal(10,2) DEFAULT '0',
	`status` enum('pending','processed','paid','voided','adjusted') DEFAULT 'pending',
	`processedAt` timestamp,
	`createdBy` int,
	`approvedBy` int,
	`approvedAt` timestamp,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `payroll_records_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `payroll_runs` (
	`id` int AUTO_INCREMENT NOT NULL,
	`payroll_period_id` int NOT NULL,
	`worker_id` int NOT NULL,
	`regular_hours` decimal(10,2) NOT NULL DEFAULT '0',
	`overtime_hours` decimal(10,2) NOT NULL DEFAULT '0',
	`gross_pay` decimal(18,2) NOT NULL,
	`federal_tax` decimal(18,2) NOT NULL DEFAULT '0',
	`state_tax` decimal(18,2) NOT NULL DEFAULT '0',
	`local_tax` decimal(18,2) NOT NULL DEFAULT '0',
	`social_security` decimal(18,2) NOT NULL DEFAULT '0',
	`medicare` decimal(18,2) NOT NULL DEFAULT '0',
	`other_deductions` decimal(18,2) NOT NULL DEFAULT '0',
	`net_pay` decimal(18,2) NOT NULL,
	`luv_ledger_transaction_id` int,
	`payroll_run_status` enum('pending','calculated','approved','paid','voided') NOT NULL DEFAULT 'pending',
	`created_at` timestamp NOT NULL DEFAULT (now()),
	`updated_at` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `payroll_runs_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `pdf_operations` (
	`id` int AUTO_INCREMENT NOT NULL,
	`operationType` varchar(50) NOT NULL,
	`inputDocumentIds` text,
	`inputFileUrls` text,
	`outputDocumentId` int,
	`outputFileKey` varchar(500),
	`outputFileUrl` varchar(1000),
	`operationParams` text,
	`status` varchar(50) NOT NULL DEFAULT 'pending',
	`errorMessage` text,
	`userId` int NOT NULL,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`completedAt` timestamp,
	CONSTRAINT `pdf_operations_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `peer_feedback` (
	`id` int AUTO_INCREMENT NOT NULL,
	`reviewId` int NOT NULL,
	`requesterId` int NOT NULL,
	`responderId` int,
	`responderName` varchar(255),
	`responderEmail` varchar(320),
	`relationship` enum('peer','direct_report','manager','cross_functional','external') NOT NULL,
	`status` enum('pending','submitted','declined') NOT NULL DEFAULT 'pending',
	`communicationRating` int,
	`teamworkRating` int,
	`leadershipRating` int,
	`technicalRating` int,
	`problemSolvingRating` int,
	`strengths` text,
	`areasForImprovement` text,
	`additionalComments` text,
	`isAnonymous` boolean NOT NULL DEFAULT true,
	`requestedAt` timestamp NOT NULL DEFAULT (now()),
	`submittedAt` timestamp,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `peer_feedback_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `pending_heir_placeholders` (
	`id` int AUTO_INCREMENT NOT NULL,
	`parentHouseId` int NOT NULL,
	`parentMemberId` int NOT NULL,
	`parentMemberName` varchar(255),
	`placeholderName` varchar(255) NOT NULL,
	`projectedHouseType` enum('bloodline','mirrored','adaptive') DEFAULT 'bloodline',
	`reservedPercentage` decimal(5,2) NOT NULL,
	`reservedAmount` decimal(18,2) DEFAULT '0',
	`status` enum('active','converted','released') NOT NULL DEFAULT 'active',
	`convertedToHeirId` int,
	`convertedAt` timestamp,
	`expiresAt` timestamp,
	`createdBy` int NOT NULL,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `pending_heir_placeholders_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `pending_house_heirs` (
	`id` int AUTO_INCREMENT NOT NULL,
	`parentHouseId` int NOT NULL,
	`parentHouseName` varchar(255),
	`parentMemberId` int,
	`parentMemberName` varchar(255),
	`otherParentName` varchar(255),
	`fullName` varchar(255) NOT NULL,
	`dateOfBirth` timestamp NOT NULL,
	`relationship` enum('grandchild','great_grandchild','step_grandchild','adopted_grandchild','foster_grandchild','other') NOT NULL,
	`generation` int NOT NULL DEFAULT 3,
	`email` varchar(255),
	`phone` varchar(50),
	`projectedHouseName` varchar(255),
	`projectedHouseType` enum('bloodline','mirrored','adaptive') DEFAULT 'bloodline',
	`activationTrigger` enum('age_18','age_21','age_25','education','marriage','first_business','manual','custom') NOT NULL DEFAULT 'age_18',
	`customActivationCriteria` text,
	`projectedActivationDate` timestamp,
	`accumulatedAmount` decimal(18,2) DEFAULT '0',
	`lastAccumulationDate` timestamp,
	`accumulationAccountId` int,
	`distributionPercentage` decimal(5,2) NOT NULL,
	`percentageLocked` boolean DEFAULT false,
	`status` enum('pending','eligible','activating','activated','deferred','removed') NOT NULL DEFAULT 'pending',
	`activatedHouseId` int,
	`activatedAt` timestamp,
	`activatedBy` int,
	`registeredBy` int NOT NULL,
	`registeredAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	`notes` text,
	CONSTRAINT `pending_house_heirs_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `performance_goals` (
	`id` int AUTO_INCREMENT NOT NULL,
	`performanceReviewId` int NOT NULL,
	`title` varchar(255) NOT NULL,
	`description` text,
	`goalCategory` enum('performance','development','project','behavioral') NOT NULL DEFAULT 'performance',
	`measurableTarget` text,
	`targetDate` timestamp,
	`weight` int DEFAULT 1,
	`goalStatus` enum('not_started','in_progress','completed','exceeded','not_met','cancelled') NOT NULL DEFAULT 'not_started',
	`progressPercent` int DEFAULT 0,
	`progressNotes` text,
	`selfRating` int,
	`managerRating` int,
	`managerComments` text,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `performance_goals_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `performance_reviews` (
	`id` int AUTO_INCREMENT NOT NULL,
	`employeeId` int NOT NULL,
	`employeeName` varchar(255) NOT NULL,
	`employeeRole` varchar(255),
	`department` varchar(100),
	`reviewPeriod` varchar(50) NOT NULL,
	`reviewType` enum('annual','quarterly','probationary','project') NOT NULL DEFAULT 'annual',
	`reviewCycle` enum('mid_year','year_end','quarterly') NOT NULL DEFAULT 'year_end',
	`reviewerId` int,
	`reviewerName` varchar(255),
	`perfReviewStatus` enum('draft','self_assessment','manager_review','calibration','completed','acknowledged') NOT NULL DEFAULT 'draft',
	`selfPerformanceRating` int,
	`selfAccomplishments` text,
	`selfChallenges` text,
	`selfDevelopmentAreas` text,
	`selfGoalsProgress` text,
	`managerPerformanceRating` int,
	`managerFeedback` text,
	`managerStrengths` text,
	`managerImprovementAreas` text,
	`qualityOfWork` int,
	`productivity` int,
	`communication` int,
	`teamwork` int,
	`initiative` int,
	`reliability` int,
	`adaptability` int,
	`leadership` int,
	`overallRating` int,
	`ratingJustification` text,
	`promotionRec` enum('not_ready','developing','ready','highly_recommended'),
	`nextPeriodGoals` text,
	`developmentPlan` text,
	`trainingNeeds` text,
	`employeeAcknowledged` boolean DEFAULT false,
	`employeeAcknowledgedAt` timestamp,
	`employeeComments` text,
	`reviewDueDate` timestamp,
	`selfAssessmentDueDate` timestamp,
	`completedAt` timestamp,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `performance_reviews_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `placement_assessment_questions` (
	`id` int AUTO_INCREMENT NOT NULL,
	`questionId` varchar(50) NOT NULL,
	`domain` enum('land','air','water','self','academic') NOT NULL,
	`subject` varchar(100) NOT NULL,
	`skillArea` varchar(100),
	`difficultyLevel` enum('k_2','3_5','6_8','9_12','adult') NOT NULL,
	`difficultyScore` int DEFAULT 50,
	`questionType` enum('multiple_choice','true_false','matching','short_answer','scenario') NOT NULL,
	`questionText` text NOT NULL,
	`questionMedia` json,
	`options` json,
	`correctAnswer` json,
	`explanation` text,
	`pointValue` int NOT NULL DEFAULT 1,
	`partialCreditAllowed` boolean NOT NULL DEFAULT false,
	`tags` json,
	`isActive` boolean NOT NULL DEFAULT true,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `placement_assessment_questions_id` PRIMARY KEY(`id`),
	CONSTRAINT `placement_assessment_questions_questionId_unique` UNIQUE(`questionId`)
);
--> statement-breakpoint
CREATE TABLE `placement_assessment_responses` (
	`id` int AUTO_INCREMENT NOT NULL,
	`assessmentId` int NOT NULL,
	`questionId` int NOT NULL,
	`selectedAnswer` json,
	`textResponse` text,
	`isCorrect` boolean,
	`pointsEarned` int DEFAULT 0,
	`timeSpentSeconds` int,
	`aiAnalysis` json,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	CONSTRAINT `placement_assessment_responses_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `platform_services_agreements` (
	`id` int AUTO_INCREMENT NOT NULL,
	`parentHouseId` int NOT NULL,
	`subsidiaryEntityType` enum('business','property','trust','nonprofit') NOT NULL,
	`subsidiaryEntityId` int NOT NULL,
	`subsidiaryEntityName` varchar(255) NOT NULL,
	`agreementNumber` varchar(50) NOT NULL,
	`effectiveDate` timestamp NOT NULL,
	`terminationDate` timestamp,
	`platformFeePercentage` decimal(5,2) DEFAULT '30.00',
	`minimumMonthlyFee` decimal(10,2),
	`maximumMonthlyFee` decimal(10,2),
	`servicesIncluded` json,
	`governingLaw` varchar(100),
	`disputeResolution` enum('arbitration','mediation','litigation','internal') DEFAULT 'internal',
	`documentId` int,
	`signedDocumentUrl` varchar(500),
	`parentSignedBy` int,
	`parentSignedAt` timestamp,
	`subsidiarySignedBy` int,
	`subsidiarySignedAt` timestamp,
	`status` enum('draft','pending_signatures','active','amended','terminated','expired') DEFAULT 'draft',
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `platform_services_agreements_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `platform_subscription_plans` (
	`id` int AUTO_INCREMENT NOT NULL,
	`planCode` varchar(50) NOT NULL,
	`name` varchar(100) NOT NULL,
	`description` text,
	`monthlyPrice` decimal(10,2) NOT NULL,
	`annualPrice` decimal(10,2),
	`includedFeatures` json,
	`maxUsers` int,
	`maxStorageGb` int,
	`maxTransactionsPerMonth` int,
	`payrollAccess` boolean NOT NULL DEFAULT false,
	`invoicingAccess` boolean NOT NULL DEFAULT false,
	`contractsAccess` boolean NOT NULL DEFAULT false,
	`taxPrepAccess` boolean NOT NULL DEFAULT false,
	`bankingAccess` boolean NOT NULL DEFAULT false,
	`marketplaceAccess` boolean NOT NULL DEFAULT false,
	`apiAccess` boolean NOT NULL DEFAULT false,
	`whiteLabelAccess` boolean NOT NULL DEFAULT false,
	`payrollFeeRate` decimal(5,2) DEFAULT '2.00',
	`invoiceFeeRate` decimal(5,2) DEFAULT '1.50',
	`marketplaceFeeRate` decimal(5,2) DEFAULT '5.00',
	`isActive` boolean NOT NULL DEFAULT true,
	`sortOrder` int NOT NULL DEFAULT 0,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `platform_subscription_plans_id` PRIMARY KEY(`id`),
	CONSTRAINT `platform_subscription_plans_planCode_unique` UNIQUE(`planCode`)
);
--> statement-breakpoint
CREATE TABLE `platform_usage_fees` (
	`id` int AUTO_INCREMENT NOT NULL,
	`houseId` int NOT NULL,
	`userId` int NOT NULL,
	`feeType` enum('subscription','payroll_processing','invoice_generation','contract_management','document_vault','tax_prep','banking_credit','digital_signatures','training_course','marketplace_listing','marketplace_sale','api_access','white_label','insurance_pool','asset_access','referral_commission') NOT NULL,
	`description` text,
	`baseAmount` decimal(20,2) NOT NULL,
	`feePercentage` decimal(5,2),
	`calculatedFee` decimal(20,2) NOT NULL,
	`splitType` enum('inter_house_60_40','intra_house_70_30') NOT NULL DEFAULT 'inter_house_60_40',
	`houseShare` decimal(20,2) NOT NULL,
	`collectiveShare` decimal(20,2) NOT NULL,
	`sourceType` varchar(50),
	`sourceId` int,
	`status` enum('pending','processed','distributed','refunded') NOT NULL DEFAULT 'pending',
	`processedAt` timestamp,
	`distributedAt` timestamp,
	`blockchainHash` varchar(255),
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `platform_usage_fees_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `position_holders` (
	`id` int AUTO_INCREMENT NOT NULL,
	`positionId` int NOT NULL,
	`userId` int,
	`fullName` varchar(255) NOT NULL,
	`email` varchar(255),
	`phone` varchar(50),
	`address` text,
	`ssn` varchar(11),
	`dateOfBirth` timestamp,
	`relationshipType` enum('family_blood','family_marriage','family_adopted','close_friend','business_partner','employee') NOT NULL,
	`specificRelationship` varchar(100),
	`startDate` timestamp NOT NULL,
	`endDate` timestamp,
	`terminationReason` text,
	`actualSalary` decimal(12,2),
	`actualHourlyRate` decimal(8,2),
	`federalFilingStatus` enum('single','married_filing_jointly','married_filing_separately','head_of_household'),
	`federalAllowances` int DEFAULT 0,
	`additionalWithholding` decimal(8,2) DEFAULT '0',
	`stateFilingStatus` varchar(50),
	`stateAllowances` int DEFAULT 0,
	`bankName` varchar(100),
	`bankRoutingNumber` varchar(9),
	`bankAccountNumber` varchar(20),
	`accountType` enum('checking','savings'),
	`onboardingComplete` boolean DEFAULT false,
	`onboardingCompletedAt` timestamp,
	`employmentAgreementSigned` boolean DEFAULT false,
	`w4Signed` boolean DEFAULT false,
	`i9Signed` boolean DEFAULT false,
	`w9Signed` boolean DEFAULT false,
	`directDepositSigned` boolean DEFAULT false,
	`handbookAcknowledged` boolean DEFAULT false,
	`status` enum('pending_onboarding','active','on_leave','terminated','resigned') DEFAULT 'pending_onboarding',
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `position_holders_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `position_requisitions` (
	`id` int AUTO_INCREMENT NOT NULL,
	`positionId` varchar(100) NOT NULL,
	`positionTitle` varchar(255) NOT NULL,
	`department` varchar(100) NOT NULL,
	`entity` varchar(255) NOT NULL,
	`tier` varchar(50) NOT NULL,
	`requestedBy` int NOT NULL,
	`requestedByName` varchar(255),
	`justification` text NOT NULL,
	`budgetApproved` enum('pending','approved','rejected') NOT NULL DEFAULT 'pending',
	`salaryRange` varchar(100),
	`targetStartDate` timestamp,
	`urgency` enum('low','medium','high','critical') NOT NULL DEFAULT 'medium',
	`status` enum('draft','submitted','under_review','approved','rejected','filled','cancelled') NOT NULL DEFAULT 'draft',
	`approvedBy` int,
	`approvedByName` varchar(255),
	`approvalDate` timestamp,
	`approvalNotes` text,
	`candidateName` varchar(255),
	`candidateEmail` varchar(320),
	`filledDate` timestamp,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `position_requisitions_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `procedure_acknowledgments` (
	`id` int AUTO_INCREMENT NOT NULL,
	`procedureId` int NOT NULL,
	`userId` int NOT NULL,
	`acknowledgedAt` timestamp NOT NULL DEFAULT (now()),
	`version` varchar(20) NOT NULL,
	`signature` varchar(255),
	`notes` text,
	CONSTRAINT `procedure_acknowledgments_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `procedure_categories` (
	`id` int AUTO_INCREMENT NOT NULL,
	`name` varchar(100) NOT NULL,
	`description` text,
	`parentId` int,
	`sortOrder` int NOT NULL DEFAULT 0,
	`icon` varchar(50),
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	CONSTRAINT `procedure_categories_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `production_credits` (
	`id` int AUTO_INCREMENT NOT NULL,
	`productionId` int NOT NULL,
	`artistId` int NOT NULL,
	`role` varchar(255) NOT NULL,
	`creditType` enum('creator','performer','producer','director','writer','designer','animator','editor','composer','featured') NOT NULL,
	`revenueSharePercentage` decimal(5,2),
	`flatFee` decimal(10,2),
	`flatFeePaid` boolean NOT NULL DEFAULT false,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	CONSTRAINT `production_credits_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `progression_events` (
	`id` int AUTO_INCREMENT NOT NULL,
	`progressionId` int NOT NULL,
	`eventType` enum('stage_change','milestone_completed','certification_earned','certification_expired','badge_earned','badge_expired','skill_verified','quality_score_recorded','mentor_assigned','pathway_started','pathway_completed','business_formed','house_joined') NOT NULL,
	`description` text,
	`previousValue` varchar(255),
	`newValue` varchar(255),
	`metadata` json,
	`triggeredBy` int,
	`triggeredBySystem` boolean NOT NULL DEFAULT false,
	`blockchainHash` varchar(255),
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	CONSTRAINT `progression_events_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `progression_milestones` (
	`id` int AUTO_INCREMENT NOT NULL,
	`fromStage` enum('w2_employee','senior_employee','contractor','certified_contractor','business_owner') NOT NULL,
	`toStage` enum('senior_employee','contractor','certified_contractor','business_owner','house_member') NOT NULL,
	`name` varchar(255) NOT NULL,
	`description` text,
	`category` enum('time_based','certification','performance','revenue','training','mentorship','business','approval') NOT NULL,
	`requirementType` varchar(100) NOT NULL,
	`requirementValue` varchar(255) NOT NULL,
	`requirementUnit` varchar(50),
	`weight` int NOT NULL DEFAULT 1,
	`isRequired` boolean NOT NULL DEFAULT true,
	`displayOrder` int NOT NULL DEFAULT 0,
	`iconName` varchar(50),
	`isActive` boolean NOT NULL DEFAULT true,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `progression_milestones_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `project_budget_items` (
	`id` int AUTO_INCREMENT NOT NULL,
	`projectId` int NOT NULL,
	`category` varchar(100) NOT NULL,
	`description` varchar(255) NOT NULL,
	`plannedAmount` decimal(15,2) NOT NULL,
	`actualAmount` decimal(15,2) DEFAULT '0',
	`variance` decimal(15,2) DEFAULT '0',
	`notes` text,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `project_budget_items_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `project_milestones` (
	`id` int AUTO_INCREMENT NOT NULL,
	`projectId` int NOT NULL,
	`name` varchar(255) NOT NULL,
	`description` text,
	`plannedDate` timestamp NOT NULL,
	`actualDate` timestamp,
	`status` varchar(50) NOT NULL DEFAULT 'pending',
	`weight` int DEFAULT 1,
	`deliverables` text,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `project_milestones_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `project_risks` (
	`id` int AUTO_INCREMENT NOT NULL,
	`projectId` int NOT NULL,
	`riskNumber` varchar(50) NOT NULL,
	`title` varchar(255) NOT NULL,
	`description` text NOT NULL,
	`category` varchar(100) NOT NULL,
	`probability` varchar(20) NOT NULL,
	`impact` varchar(20) NOT NULL,
	`riskScore` int,
	`status` varchar(50) NOT NULL DEFAULT 'open',
	`mitigationPlan` text,
	`contingencyPlan` text,
	`ownerId` int,
	`identifiedDate` timestamp DEFAULT (now()),
	`closedDate` timestamp,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `project_risks_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `project_status_reports` (
	`id` int AUTO_INCREMENT NOT NULL,
	`projectId` int NOT NULL,
	`reportDate` timestamp NOT NULL,
	`reportPeriod` varchar(50) NOT NULL,
	`overallStatus` varchar(20) NOT NULL,
	`scheduleStatus` varchar(20) NOT NULL,
	`budgetStatus` varchar(20) NOT NULL,
	`scopeStatus` varchar(20) NOT NULL,
	`accomplishments` text,
	`plannedActivities` text,
	`issues` text,
	`decisions` text,
	`earnedValue` decimal(15,2),
	`plannedValue` decimal(15,2),
	`actualCost` decimal(15,2),
	`cpi` decimal(5,2),
	`spi` decimal(5,2),
	`preparedBy` int,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	CONSTRAINT `project_status_reports_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `project_tasks` (
	`id` int AUTO_INCREMENT NOT NULL,
	`projectId` int NOT NULL,
	`milestoneId` int,
	`parentTaskId` int,
	`name` varchar(255) NOT NULL,
	`description` text,
	`assigneeId` int,
	`plannedStart` timestamp,
	`plannedEnd` timestamp,
	`actualStart` timestamp,
	`actualEnd` timestamp,
	`duration` int,
	`percentComplete` int DEFAULT 0,
	`status` varchar(50) NOT NULL DEFAULT 'not_started',
	`priority` varchar(20) DEFAULT 'medium',
	`dependencies` text,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `project_tasks_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `projects` (
	`id` int AUTO_INCREMENT NOT NULL,
	`name` varchar(255) NOT NULL,
	`code` varchar(50) NOT NULL,
	`description` text,
	`entityId` int,
	`projectType` varchar(50) NOT NULL,
	`status` varchar(50) NOT NULL DEFAULT 'planning',
	`priority` varchar(20) DEFAULT 'medium',
	`startDate` timestamp,
	`endDate` timestamp,
	`actualStartDate` timestamp,
	`actualEndDate` timestamp,
	`budget` decimal(15,2),
	`actualCost` decimal(15,2) DEFAULT '0',
	`percentComplete` int DEFAULT 0,
	`projectManagerId` int,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `projects_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `properties` (
	`id` int AUTO_INCREMENT NOT NULL,
	`houseId` int,
	`entityId` int,
	`propertyName` varchar(255) NOT NULL,
	`propertyCode` varchar(50),
	`streetAddress` varchar(255) NOT NULL,
	`unit` varchar(50),
	`city` varchar(100) NOT NULL,
	`state` varchar(50) NOT NULL,
	`zipCode` varchar(20) NOT NULL,
	`county` varchar(100),
	`country` varchar(100) DEFAULT 'USA',
	`propertyType` enum('single_family','multi_family','condo','townhouse','commercial','industrial','land','mixed_use','other') NOT NULL,
	`propertySubType` varchar(100),
	`yearBuilt` int,
	`squareFootage` int,
	`lotSize` decimal(10,2),
	`bedrooms` int,
	`bathrooms` decimal(3,1),
	`stories` int,
	`garageSpaces` int,
	`status` enum('active','vacant','under_renovation','for_sale','for_rent','pending_acquisition','sold','archived') NOT NULL DEFAULT 'active',
	`occupancyStatus` enum('owner_occupied','tenant_occupied','vacant','under_construction') DEFAULT 'vacant',
	`purchasePrice` decimal(15,2),
	`purchaseDate` timestamp,
	`currentValue` decimal(15,2),
	`lastValuationDate` timestamp,
	`hasMortgage` boolean DEFAULT false,
	`mortgageBalance` decimal(15,2),
	`mortgagePayment` decimal(10,2),
	`mortgageLender` varchar(255),
	`mortgageInterestRate` decimal(5,3),
	`mortgageMaturityDate` timestamp,
	`insuranceProvider` varchar(255),
	`insurancePolicyNumber` varchar(100),
	`insurancePremium` decimal(10,2),
	`insuranceRenewalDate` timestamp,
	`parcelNumber` varchar(100),
	`annualPropertyTax` decimal(10,2),
	`taxAssessedValue` decimal(15,2),
	`deedType` varchar(100),
	`titleCompany` varchar(255),
	`zoningClassification` varchar(100),
	`description` text,
	`notes` text,
	`tags` json,
	`primaryPhotoUrl` text,
	`photoUrls` json,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `properties_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `property_acquisitions` (
	`id` int AUTO_INCREMENT NOT NULL,
	`property_id` int NOT NULL,
	`house_id` int NOT NULL,
	`acquisition_type` enum('purchase','inheritance','gift','restoration_claim','tax_sale','auction') NOT NULL,
	`offer_price` decimal(18,2),
	`accepted_price` decimal(18,2),
	`closing_costs` decimal(18,2),
	`down_payment` decimal(18,2),
	`financed_amount` decimal(18,2),
	`offer_date` timestamp,
	`acceptance_date` timestamp,
	`inspection_date` timestamp,
	`closing_date` timestamp,
	`seller_name` varchar(255),
	`seller_contact` varchar(255),
	`real_estate_agent_id` int,
	`title_company` varchar(255),
	`lender_id` int,
	`acquisition_status` enum('prospecting','offer_submitted','under_contract','inspection','financing','closing','completed','cancelled','withdrawn') NOT NULL DEFAULT 'prospecting',
	`luv_ledger_transaction_id` int,
	`created_at` timestamp NOT NULL DEFAULT (now()),
	`updated_at` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `property_acquisitions_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `property_documents` (
	`id` int AUTO_INCREMENT NOT NULL,
	`propertyId` int NOT NULL,
	`projectId` int,
	`documentName` varchar(255) NOT NULL,
	`documentType` enum('deed','title','survey','appraisal','inspection','insurance','tax','lease','contract','permit','photo','floor_plan','receipt','invoice','other') NOT NULL,
	`description` text,
	`fileUrl` text NOT NULL,
	`fileName` varchar(255),
	`fileSize` int,
	`mimeType` varchar(100),
	`documentDate` timestamp,
	`expirationDate` timestamp,
	`tags` json,
	`isConfidential` boolean DEFAULT false,
	`uploadedBy` int,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `property_documents_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `property_expenses` (
	`id` int AUTO_INCREMENT NOT NULL,
	`property_id` int NOT NULL,
	`house_id` int NOT NULL,
	`property_expense_type` enum('property_tax','insurance','maintenance','repairs','utilities','hoa_fees','management_fees','legal_fees','mortgage_payment','improvements','landscaping','security','other') NOT NULL,
	`description` varchar(500) NOT NULL,
	`amount` decimal(18,2) NOT NULL,
	`expense_date` timestamp NOT NULL,
	`is_recurring` boolean NOT NULL DEFAULT false,
	`recurring_frequency` enum('monthly','quarterly','semi_annual','annual'),
	`vendor_name` varchar(255),
	`vendor_contact` varchar(255),
	`is_tax_deductible` boolean NOT NULL DEFAULT false,
	`tax_category` varchar(100),
	`luv_ledger_transaction_id` int,
	`receipt_url` varchar(500),
	`created_at` timestamp NOT NULL DEFAULT (now()),
	CONSTRAINT `property_expenses_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `property_financials` (
	`id` int AUTO_INCREMENT NOT NULL,
	`propertyId` int NOT NULL,
	`year` int NOT NULL,
	`month` int NOT NULL,
	`rentalIncome` decimal(15,2) DEFAULT '0',
	`otherIncome` decimal(15,2) DEFAULT '0',
	`totalIncome` decimal(15,2) DEFAULT '0',
	`mortgagePayment` decimal(10,2) DEFAULT '0',
	`propertyTax` decimal(10,2) DEFAULT '0',
	`insurance` decimal(10,2) DEFAULT '0',
	`utilities` decimal(10,2) DEFAULT '0',
	`maintenance` decimal(10,2) DEFAULT '0',
	`repairs` decimal(10,2) DEFAULT '0',
	`managementFees` decimal(10,2) DEFAULT '0',
	`hoaFees` decimal(10,2) DEFAULT '0',
	`landscaping` decimal(10,2) DEFAULT '0',
	`otherExpenses` decimal(10,2) DEFAULT '0',
	`totalExpenses` decimal(15,2) DEFAULT '0',
	`netOperatingIncome` decimal(15,2) DEFAULT '0',
	`cashFlow` decimal(15,2) DEFAULT '0',
	`notes` text,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `property_financials_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `property_income` (
	`id` int AUTO_INCREMENT NOT NULL,
	`property_id` int NOT NULL,
	`house_id` int NOT NULL,
	`property_income_type` enum('rent','lease','sale_proceeds','insurance_claim','tax_refund','security_deposit','late_fees','parking','laundry','other') NOT NULL,
	`description` varchar(500) NOT NULL,
	`amount` decimal(18,2) NOT NULL,
	`income_date` timestamp NOT NULL,
	`tenant_name` varchar(255),
	`lease_start_date` timestamp,
	`lease_end_date` timestamp,
	`luv_ledger_transaction_id` int,
	`created_at` timestamp NOT NULL DEFAULT (now()),
	CONSTRAINT `property_income_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `property_inspections` (
	`id` int AUTO_INCREMENT NOT NULL,
	`propertyId` int NOT NULL,
	`projectId` int,
	`inspectionType` enum('move_in','move_out','annual','quarterly','maintenance','safety','code_compliance','appraisal','other') NOT NULL,
	`inspectionDate` timestamp NOT NULL,
	`inspectorName` varchar(255),
	`inspectorCompany` varchar(255),
	`inspectorLicense` varchar(100),
	`overallCondition` enum('excellent','good','fair','poor','critical'),
	`passedInspection` boolean,
	`findings` text,
	`recommendations` text,
	`requiredRepairs` text,
	`estimatedRepairCost` decimal(15,2),
	`followUpRequired` boolean DEFAULT false,
	`followUpDate` timestamp,
	`followUpNotes` text,
	`reportUrl` text,
	`photoUrls` json,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `property_inspections_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `property_maintenance_logs` (
	`id` int AUTO_INCREMENT NOT NULL,
	`propertyId` int NOT NULL,
	`projectId` int,
	`inspectionId` int,
	`maintenanceType` enum('preventive','corrective','emergency','routine','upgrade') NOT NULL,
	`category` enum('plumbing','electrical','hvac','roofing','flooring','appliances','exterior','interior','landscaping','pest_control','cleaning','safety','other') NOT NULL,
	`title` varchar(255) NOT NULL,
	`description` text,
	`reportedDate` timestamp NOT NULL,
	`scheduledDate` timestamp,
	`completedDate` timestamp,
	`status` enum('reported','scheduled','in_progress','completed','cancelled') NOT NULL DEFAULT 'reported',
	`priority` enum('low','medium','high','emergency') DEFAULT 'medium',
	`serviceProviderName` varchar(255),
	`serviceProviderPhone` varchar(50),
	`estimatedCost` decimal(10,2),
	`actualCost` decimal(10,2),
	`resolution` text,
	`partsReplaced` text,
	`warrantyInfo` text,
	`beforePhotoUrls` json,
	`afterPhotoUrls` json,
	`reportedBy` varchar(255),
	`completedBy` varchar(255),
	`notes` text,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `property_maintenance_logs_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `property_project_expenses` (
	`id` int AUTO_INCREMENT NOT NULL,
	`projectId` int NOT NULL,
	`taskId` int,
	`expenseDate` timestamp NOT NULL,
	`category` enum('labor','materials','equipment','permits','inspection','subcontractor','supplies','travel','other') NOT NULL,
	`description` varchar(500) NOT NULL,
	`vendorName` varchar(255),
	`vendorId` int,
	`invoiceNumber` varchar(100),
	`amount` decimal(15,2) NOT NULL,
	`taxAmount` decimal(10,2),
	`totalAmount` decimal(15,2) NOT NULL,
	`paymentStatus` enum('pending','approved','paid','rejected') DEFAULT 'pending',
	`paymentDate` timestamp,
	`paymentMethod` varchar(100),
	`checkNumber` varchar(50),
	`approvedBy` int,
	`approvalDate` timestamp,
	`receiptUrl` text,
	`attachmentUrls` json,
	`notes` text,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `property_project_expenses_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `property_project_milestones` (
	`id` int AUTO_INCREMENT NOT NULL,
	`projectId` int NOT NULL,
	`milestoneName` varchar(255) NOT NULL,
	`description` text,
	`targetDate` timestamp,
	`completedDate` timestamp,
	`status` enum('pending','in_progress','completed','delayed','cancelled') NOT NULL DEFAULT 'pending',
	`dependsOnMilestoneId` int,
	`deliverables` text,
	`sortOrder` int DEFAULT 0,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `property_project_milestones_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `property_project_tasks` (
	`id` int AUTO_INCREMENT NOT NULL,
	`projectId` int NOT NULL,
	`milestoneId` int,
	`parentTaskId` int,
	`taskName` varchar(255) NOT NULL,
	`description` text,
	`assignedToId` int,
	`assignedToName` varchar(255),
	`startDate` timestamp,
	`dueDate` timestamp,
	`completedDate` timestamp,
	`estimatedHours` decimal(8,2),
	`actualHours` decimal(8,2),
	`status` enum('not_started','in_progress','blocked','completed','cancelled') NOT NULL DEFAULT 'not_started',
	`priority` enum('low','medium','high','urgent') DEFAULT 'medium',
	`estimatedCost` decimal(10,2),
	`actualCost` decimal(10,2),
	`sortOrder` int DEFAULT 0,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `property_project_tasks_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `property_projects` (
	`id` int AUTO_INCREMENT NOT NULL,
	`propertyId` int NOT NULL,
	`projectName` varchar(255) NOT NULL,
	`projectCode` varchar(50),
	`projectType` enum('renovation','repair','maintenance','improvement','addition','landscaping','inspection','compliance','other') NOT NULL,
	`description` text,
	`scope` text,
	`objectives` text,
	`status` enum('planning','approved','in_progress','on_hold','completed','cancelled') NOT NULL DEFAULT 'planning',
	`priority` enum('low','medium','high','urgent') DEFAULT 'medium',
	`plannedStartDate` timestamp,
	`plannedEndDate` timestamp,
	`actualStartDate` timestamp,
	`actualEndDate` timestamp,
	`estimatedBudget` decimal(15,2),
	`approvedBudget` decimal(15,2),
	`actualCost` decimal(15,2) DEFAULT '0',
	`contingencyAmount` decimal(15,2),
	`percentComplete` int DEFAULT 0,
	`primaryContractorId` int,
	`contractorName` varchar(255),
	`contractorContact` varchar(255),
	`contractNumber` varchar(100),
	`requiresPermit` boolean DEFAULT false,
	`permitNumber` varchar(100),
	`permitStatus` enum('not_required','pending','approved','denied','expired') DEFAULT 'not_required',
	`permitDate` timestamp,
	`approvedBy` int,
	`approvalDate` timestamp,
	`grantId` int,
	`fundingSourceId` int,
	`chargeCodeId` int,
	`notes` text,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `property_projects_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `property_tenants` (
	`id` int AUTO_INCREMENT NOT NULL,
	`propertyId` int NOT NULL,
	`tenantType` enum('individual','family','business') DEFAULT 'individual',
	`firstName` varchar(100),
	`lastName` varchar(100),
	`businessName` varchar(255),
	`email` varchar(255),
	`phone` varchar(50),
	`leaseStartDate` timestamp,
	`leaseEndDate` timestamp,
	`monthlyRent` decimal(10,2),
	`securityDeposit` decimal(10,2),
	`leaseType` enum('month_to_month','annual','multi_year') DEFAULT 'annual',
	`status` enum('active','pending','past','evicted') NOT NULL DEFAULT 'active',
	`moveInDate` timestamp,
	`moveOutDate` timestamp,
	`emergencyContactName` varchar(255),
	`emergencyContactPhone` varchar(50),
	`notes` text,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `property_tenants_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `property_utilities` (
	`id` int AUTO_INCREMENT NOT NULL,
	`propertyId` int NOT NULL,
	`utilityType` enum('electric','gas','water','sewer','trash','internet','cable','phone','solar','other') NOT NULL,
	`providerName` varchar(255) NOT NULL,
	`accountNumber` varchar(100),
	`providerPhone` varchar(50),
	`providerWebsite` varchar(255),
	`billingDay` int,
	`averageMonthlyBill` decimal(10,2),
	`paidBy` enum('owner','tenant','split') DEFAULT 'owner',
	`status` enum('active','inactive','transferred') DEFAULT 'active',
	`meterNumber` varchar(100),
	`meterLocation` varchar(255),
	`notes` text,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `property_utilities_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `property_valuations` (
	`id` int AUTO_INCREMENT NOT NULL,
	`property_id` int NOT NULL,
	`valuation_type` enum('appraisal','cma','tax_assessment','broker_opinion','self_assessment') NOT NULL,
	`valuation_amount` decimal(18,2) NOT NULL,
	`valuation_date` timestamp NOT NULL,
	`appraiser_name` varchar(255),
	`appraiser_license` varchar(100),
	`appraiser_company` varchar(255),
	`comparable_sales` json,
	`notes` text,
	`document_url` varchar(500),
	`created_at` timestamp NOT NULL DEFAULT (now()),
	CONSTRAINT `property_valuations_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `property_vendors` (
	`id` int AUTO_INCREMENT NOT NULL,
	`vendorName` varchar(255) NOT NULL,
	`vendorType` enum('contractor','plumber','electrician','hvac','landscaper','cleaner','property_manager','inspector','appraiser','insurance','legal','other') NOT NULL,
	`contactName` varchar(255),
	`email` varchar(255),
	`phone` varchar(50),
	`website` varchar(255),
	`address` text,
	`city` varchar(100),
	`state` varchar(50),
	`zipCode` varchar(20),
	`licenseNumber` varchar(100),
	`insuranceInfo` text,
	`taxId` varchar(50),
	`rating` int,
	`status` enum('active','inactive','blacklisted') DEFAULT 'active',
	`notes` text,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `property_vendors_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `protected_lineage` (
	`id` int AUTO_INCREMENT NOT NULL,
	`fullName` varchar(255) NOT NULL,
	`relationship` varchar(100) NOT NULL,
	`role` varchar(100),
	`associatedHouse` varchar(255),
	`lineageOrder` int NOT NULL DEFAULT 0,
	`sealedByScrollId` int,
	`sealHash` varchar(255) NOT NULL,
	`status` enum('active','suspended','transferred') NOT NULL DEFAULT 'active',
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `protected_lineage_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `purchase_request_comments` (
	`id` int AUTO_INCREMENT NOT NULL,
	`purchaseRequestId` int NOT NULL,
	`userId` int NOT NULL,
	`comment` text NOT NULL,
	`isInternal` boolean DEFAULT false,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	CONSTRAINT `purchase_request_comments_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `purchase_requests` (
	`id` int AUTO_INCREMENT NOT NULL,
	`requestNumber` varchar(50) NOT NULL,
	`requesterId` int NOT NULL,
	`departmentId` int NOT NULL,
	`title` varchar(255) NOT NULL,
	`description` text NOT NULL,
	`category` enum('software','equipment','supplies','professional_development','travel','contractor','subscription','other') NOT NULL,
	`vendor` varchar(255),
	`amount` decimal(12,2) NOT NULL,
	`budgetCode` varchar(50),
	`fiscalYear` varchar(10),
	`approvalTier` enum('tier1','tier2','tier3') NOT NULL,
	`status` enum('draft','pending_manager','pending_procurement','pending_finance','pending_ceo','pending_board_notification','approved','rejected','cancelled') NOT NULL DEFAULT 'draft',
	`managerApproval` enum('pending','approved','rejected') DEFAULT 'pending',
	`managerApprovedBy` int,
	`managerApprovedAt` timestamp,
	`managerNotes` text,
	`procurementApproval` enum('pending','approved','rejected') DEFAULT 'pending',
	`procurementApprovedBy` int,
	`procurementApprovedAt` timestamp,
	`procurementNotes` text,
	`financeApproval` enum('pending','approved','rejected') DEFAULT 'pending',
	`financeApprovedBy` int,
	`financeApprovedAt` timestamp,
	`financeNotes` text,
	`ceoApproval` enum('not_required','pending','approved','rejected') DEFAULT 'not_required',
	`ceoApprovedBy` int,
	`ceoApprovedAt` timestamp,
	`ceoNotes` text,
	`boardNotified` boolean DEFAULT false,
	`boardNotifiedAt` timestamp,
	`attachments` json,
	`submittedAt` timestamp,
	`completedAt` timestamp,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `purchase_requests_id` PRIMARY KEY(`id`),
	CONSTRAINT `purchase_requests_requestNumber_unique` UNIQUE(`requestNumber`)
);
--> statement-breakpoint
CREATE TABLE `quality_standards` (
	`id` int AUTO_INCREMENT NOT NULL,
	`departmentId` int,
	`departmentCode` varchar(50),
	`name` varchar(255) NOT NULL,
	`code` varchar(50) NOT NULL,
	`description` text,
	`category` enum('deliverable_quality','timeliness','communication','documentation','compliance','innovation') NOT NULL,
	`metricType` enum('percentage','rating','score','boolean','count') NOT NULL,
	`minimumValue` decimal(10,2),
	`targetValue` decimal(10,2),
	`excellentValue` decimal(10,2),
	`standardTierMinimum` decimal(10,2),
	`premiumTierMinimum` decimal(10,2),
	`eliteTierMinimum` decimal(10,2),
	`weight` int NOT NULL DEFAULT 1,
	`isActive` boolean NOT NULL DEFAULT true,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `quality_standards_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `quiz_attempts` (
	`id` int AUTO_INCREMENT NOT NULL,
	`userId` int NOT NULL,
	`quizId` int NOT NULL,
	`bookId` int NOT NULL,
	`readingSessionId` int,
	`studentProfileId` int,
	`attemptNumber` int DEFAULT 1,
	`answers` json,
	`score` int,
	`correctCount` int DEFAULT 0,
	`totalQuestions` int DEFAULT 0,
	`passed` boolean DEFAULT false,
	`startedAt` timestamp NOT NULL DEFAULT (now()),
	`completedAt` timestamp,
	`durationSeconds` int,
	`feedback` text,
	`areasToImprove` json,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	CONSTRAINT `quiz_attempts_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `reading_acknowledgments` (
	`id` int AUTO_INCREMENT NOT NULL,
	`requiredReadingId` int NOT NULL,
	`resourceLinkId` int NOT NULL,
	`employeeId` int NOT NULL,
	`userId` int,
	`acknowledgedAt` timestamp NOT NULL,
	`signatureType` enum('checkbox','typed_name','drawn','digital_certificate') DEFAULT 'checkbox',
	`signatureData` text,
	`signatureHash` varchar(64),
	`ipAddress` varchar(45),
	`userAgent` text,
	`confirmationText` text,
	`timeSpentSeconds` int,
	`blockchainHash` varchar(128),
	`blockchainTimestamp` timestamp,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	CONSTRAINT `reading_acknowledgments_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `reading_certificates` (
	`id` int AUTO_INCREMENT NOT NULL,
	`userId` int NOT NULL,
	`bookId` int NOT NULL,
	`readingSessionId` int,
	`studentProfileId` int,
	`readingCertType` enum('book_completion','discussion_excellence','vocabulary_mastery','reading_streak') NOT NULL,
	`title` varchar(255) NOT NULL,
	`description` text,
	`achievementData` json,
	`certificateHash` varchar(128),
	`blockchainTimestamp` timestamp,
	`issuedAt` timestamp NOT NULL DEFAULT (now()),
	CONSTRAINT `reading_certificates_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `reading_compliance_reports` (
	`id` int AUTO_INCREMENT NOT NULL,
	`reportType` enum('organization','entity','department','individual') NOT NULL,
	`scopeValue` varchar(255),
	`periodStart` timestamp NOT NULL,
	`periodEnd` timestamp NOT NULL,
	`totalAssignments` int NOT NULL DEFAULT 0,
	`completedAssignments` int NOT NULL DEFAULT 0,
	`overdueAssignments` int NOT NULL DEFAULT 0,
	`complianceRate` decimal(5,2),
	`reportData` json,
	`generatedAt` timestamp NOT NULL DEFAULT (now()),
	`generatedBy` int,
	CONSTRAINT `reading_compliance_reports_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `reading_discussion_requirements` (
	`id` int AUTO_INCREMENT NOT NULL,
	`reqGradeLevel` enum('k_2','3_5','6_8','9_12') NOT NULL,
	`readAloudDefault` boolean DEFAULT true,
	`readAloudCanDisable` boolean DEFAULT true,
	`basicQARequired` boolean DEFAULT true,
	`qaFrequency` enum('after_each_chapter','after_each_section','end_of_book') DEFAULT 'after_each_chapter',
	`deepDiscussionRequired` boolean DEFAULT false,
	`deepDiscussionEncouraged` boolean DEFAULT false,
	`discussionBonusCredits` int DEFAULT 0,
	`minDiscussionMessages` int DEFAULT 0,
	`minQuality` enum('none','surface','developing','proficient') DEFAULT 'none',
	`description` text,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `reading_discussion_requirements_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `reading_sessions` (
	`id` int AUTO_INCREMENT NOT NULL,
	`userId` int NOT NULL,
	`bookId` int NOT NULL,
	`studentProfileId` int,
	`currentPage` int NOT NULL DEFAULT 1,
	`currentChapter` int DEFAULT 1,
	`percentComplete` decimal(5,2) DEFAULT '0',
	`totalReadingMinutes` int DEFAULT 0,
	`lastReadAt` timestamp,
	`sessionsCount` int DEFAULT 0,
	`readAloudEnabled` boolean DEFAULT false,
	`fontSize` varchar(20) DEFAULT 'medium',
	`theme` enum('light','dark','sepia') DEFAULT 'light',
	`readingStatus` enum('not_started','in_progress','completed','abandoned') NOT NULL DEFAULT 'not_started',
	`completedAt` timestamp,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `reading_sessions_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `reading_streaks` (
	`id` int AUTO_INCREMENT NOT NULL,
	`userId` int NOT NULL,
	`studentProfileId` int,
	`currentStreak` int NOT NULL DEFAULT 0,
	`longestStreak` int NOT NULL DEFAULT 0,
	`lastReadDate` date,
	`streakStartDate` date,
	`thisWeekMinutes` int DEFAULT 0,
	`thisMonthMinutes` int DEFAULT 0,
	`totalReadingMinutes` int DEFAULT 0,
	`totalBooksCompleted` int DEFAULT 0,
	`dailyGoalMinutes` int DEFAULT 15,
	`weeklyGoalMinutes` int DEFAULT 60,
	`streakReminderEnabled` boolean DEFAULT true,
	`reminderTime` varchar(10) DEFAULT '18:00',
	`lastReminderSentAt` timestamp,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `reading_streaks_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `real_estate_agents` (
	`id` int AUTO_INCREMENT NOT NULL,
	`house_id` int NOT NULL,
	`agent_name` varchar(255) NOT NULL,
	`agent_license` varchar(100),
	`brokerage_name` varchar(255),
	`email` varchar(255),
	`phone` varchar(50),
	`agent_specialization` enum('residential','commercial','land','investment','luxury','foreclosure'),
	`agent_relationship` enum('buyers_agent','sellers_agent','dual_agent','referral') NOT NULL,
	`transactions_completed` int DEFAULT 0,
	`rating` decimal(3,2),
	`is_active` boolean NOT NULL DEFAULT true,
	`created_at` timestamp NOT NULL DEFAULT (now()),
	`updated_at` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `real_estate_agents_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `real_estate_properties` (
	`id` int AUTO_INCREMENT NOT NULL,
	`house_id` int NOT NULL,
	`user_id` int NOT NULL,
	`business_entity_id` int,
	`property_code` varchar(50) NOT NULL,
	`property_name` varchar(255) NOT NULL,
	`property_type` enum('land','residential','commercial','industrial','agricultural','mixed_use','vacant_lot','ancestral','restoration') NOT NULL,
	`street_address` varchar(500),
	`city` varchar(100),
	`state` varchar(100),
	`zip_code` varchar(20),
	`county` varchar(100),
	`country` varchar(100) NOT NULL DEFAULT 'USA',
	`parcel_number` varchar(100),
	`legal_description` text,
	`deed_reference` varchar(255),
	`acreage` decimal(10,4),
	`square_feet` int,
	`year_built` int,
	`bedrooms` int,
	`bathrooms` decimal(3,1),
	`purchase_price` decimal(18,2),
	`purchase_date` timestamp,
	`current_market_value` decimal(18,2),
	`last_appraisal_date` timestamp,
	`assessed_value` decimal(18,2),
	`is_restoration_property` boolean NOT NULL DEFAULT false,
	`restoration_case_id` int,
	`ancestral_claim_status` enum('none','researching','documented','filed','pending','approved','denied') DEFAULT 'none',
	`ownership_status` enum('owned','under_contract','pending_closing','leased','sold','foreclosed') NOT NULL DEFAULT 'owned',
	`notes` text,
	`created_at` timestamp NOT NULL DEFAULT (now()),
	`updated_at` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `real_estate_properties_id` PRIMARY KEY(`id`),
	CONSTRAINT `real_estate_properties_property_code_unique` UNIQUE(`property_code`)
);
--> statement-breakpoint
CREATE TABLE `requests` (
	`id` int AUTO_INCREMENT NOT NULL,
	`requesterId` int NOT NULL,
	`departmentId` int,
	`category` enum('equipment','software','vehicle','service','facility','training') NOT NULL,
	`itemSpec` varchar(500) NOT NULL,
	`quantity` int NOT NULL DEFAULT 1,
	`justification` text NOT NULL,
	`costEstimate` decimal(20,2),
	`neededBy` timestamp,
	`status` enum('draft','pending_manager','pending_finance','pending_executive','approved','fulfilled','closed','rejected') NOT NULL DEFAULT 'draft',
	`blockchainRef` varchar(255),
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `requests_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `required_readings` (
	`id` int AUTO_INCREMENT NOT NULL,
	`resourceLinkId` int NOT NULL,
	`assignmentType` enum('all_employees','entity','department','role','individual') NOT NULL,
	`scopeValue` varchar(255),
	`scopeEmployeeIds` json,
	`dueDate` timestamp,
	`priority` enum('low','medium','high','critical') DEFAULT 'medium',
	`isRecurring` boolean NOT NULL DEFAULT false,
	`recurrenceInterval` enum('monthly','quarterly','annually'),
	`lastRecurrenceDate` timestamp,
	`isActive` boolean NOT NULL DEFAULT true,
	`createdBy` int NOT NULL,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `required_readings_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `resource_link_categories` (
	`id` int AUTO_INCREMENT NOT NULL,
	`dashboard` varchar(50) NOT NULL,
	`categoryName` varchar(100) NOT NULL,
	`categoryIcon` varchar(50),
	`description` text,
	`orderIndex` int NOT NULL DEFAULT 0,
	`isActive` boolean NOT NULL DEFAULT true,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	CONSTRAINT `resource_link_categories_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `resource_links` (
	`id` int AUTO_INCREMENT NOT NULL,
	`title` varchar(255) NOT NULL,
	`url` varchar(1000) NOT NULL,
	`description` text,
	`dashboard` enum('health','finance','legal','business','education','governance','hr','operations','general') NOT NULL,
	`category` varchar(100),
	`tags` json,
	`priority` int NOT NULL DEFAULT 0,
	`isPinned` boolean NOT NULL DEFAULT false,
	`sourceName` varchar(255),
	`sourceIcon` varchar(500),
	`publishedAt` timestamp,
	`addedBy` int,
	`isAgentIdentified` boolean NOT NULL DEFAULT false,
	`agentId` int,
	`agentConfidence` decimal(5,2),
	`agentReason` text,
	`approvalStatus` enum('pending','approved','rejected') DEFAULT 'approved',
	`approvedBy` int,
	`approvedAt` timestamp,
	`isActive` boolean NOT NULL DEFAULT true,
	`swotRelevance` enum('strength','weakness','opportunity','threat','none') DEFAULT 'none',
	`swotConfidence` decimal(5,2),
	`swotReason` text,
	`industryCategory` enum('competitor_intel','regulatory','market_trends','technology','economic','consumer','talent','general'),
	`impactLevel` enum('low','medium','high','critical'),
	`impactTimeframe` enum('immediate','short_term','medium_term','long_term'),
	`requiresAction` boolean NOT NULL DEFAULT false,
	`actionTaken` text,
	`actionTakenBy` int,
	`actionTakenAt` timestamp,
	`clickCount` int NOT NULL DEFAULT 0,
	`lastClickedAt` timestamp,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `resource_links_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `restoration_cases` (
	`id` int AUTO_INCREMENT NOT NULL,
	`house_id` int NOT NULL,
	`user_id` int NOT NULL,
	`case_number` varchar(100) NOT NULL,
	`case_name` varchar(255) NOT NULL,
	`restoration_claim_type` enum('ancestral_land','property_theft','deed_fraud','tax_sale_reversal','inheritance_dispute','boundary_dispute','title_clearing','other') NOT NULL,
	`property_description` text,
	`original_owner` varchar(255),
	`claimant_relationship` varchar(255),
	`property_address` varchar(500),
	`county` varchar(100),
	`state` varchar(50),
	`parcel_numbers` json,
	`original_ownership_date` timestamp,
	`dispossession_date` timestamp,
	`claim_filed_date` timestamp,
	`attorney_name` varchar(255),
	`attorney_contact` varchar(255),
	`court_case_number` varchar(100),
	`jurisdiction` varchar(255),
	`evidence_document_ids` json,
	`estimated_value` decimal(18,2),
	`legal_fees` decimal(18,2) DEFAULT '0',
	`restoration_case_status` enum('research','documenting','filed','pending_review','hearing_scheduled','in_litigation','settled','won','lost','appealing','closed') NOT NULL DEFAULT 'research',
	`outcome_notes` text,
	`restored_property_id` int,
	`created_at` timestamp NOT NULL DEFAULT (now()),
	`updated_at` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `restoration_cases_id` PRIMARY KEY(`id`),
	CONSTRAINT `restoration_cases_case_number_unique` UNIQUE(`case_number`)
);
--> statement-breakpoint
CREATE TABLE `revenue_sharing_events` (
	`id` int AUTO_INCREMENT NOT NULL,
	`sourceEntityType` enum('business','property','service','grant','investment','other') NOT NULL,
	`sourceEntityId` int NOT NULL,
	`sourceEntityName` varchar(255),
	`sourceHouseId` int NOT NULL,
	`parentHouseId` int NOT NULL,
	`parentHouseName` varchar(255),
	`grossRevenue` decimal(18,2) NOT NULL,
	`revenueType` enum('sales','service_fee','licensing','royalty','rental_income','grant_award','investment_return','dividend','other') NOT NULL,
	`revenueDescription` text,
	`entityRetainedAmount` decimal(18,2) NOT NULL,
	`platformFeeAmount` decimal(18,2) NOT NULL,
	`platformFeePercentage` decimal(5,2) DEFAULT '30.00',
	`feeJustification` enum('platform_services','administrative_services','training_support','technology_infrastructure','compliance_monitoring','combined_services') DEFAULT 'combined_services',
	`servicesProvided` json,
	`reserveAmount` decimal(18,2),
	`communityShareAmount` decimal(18,2),
	`fundAllocations` json,
	`sourceLedgerTransactionId` int,
	`parentLedgerTransactionId` int,
	`blockchainHash` varchar(128),
	`status` enum('pending','processed','failed','reversed') DEFAULT 'pending',
	`processedAt` timestamp,
	`errorMessage` text,
	`revenueDate` timestamp NOT NULL,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	CONSTRAINT `revenue_sharing_events_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `risks` (
	`id` int AUTO_INCREMENT NOT NULL,
	`title` varchar(255) NOT NULL,
	`description` text NOT NULL,
	`riskCategory` enum('financial','operational','legal','compliance','reputational','strategic','technology','security') NOT NULL,
	`likelihood` enum('rare','unlikely','possible','likely','almost_certain') NOT NULL,
	`impact` enum('insignificant','minor','moderate','major','catastrophic') NOT NULL,
	`riskScore` int,
	`mitigationStrategy` text,
	`mitigationStatus` enum('not_started','in_progress','implemented','monitoring') NOT NULL DEFAULT 'not_started',
	`ownerId` int,
	`reviewDate` timestamp,
	`riskStatus` enum('open','mitigated','accepted','closed') NOT NULL DEFAULT 'open',
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `risks_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `sandbox_entities` (
	`id` int AUTO_INCREMENT NOT NULL,
	`sessionId` int NOT NULL,
	`userId` int NOT NULL,
	`entityName` varchar(255) NOT NULL,
	`entityType` enum('trust','llc','corporation','collective','508c1a','asset','division') NOT NULL,
	`status` enum('draft','active','paused','archived') NOT NULL DEFAULT 'active',
	`balance` decimal(15,2) DEFAULT '0.00',
	`interHouseSplit` int DEFAULT 60,
	`intraHouseSplit` int DEFAULT 70,
	`metadata` json,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `sandbox_entities_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `sandbox_operations` (
	`id` int AUTO_INCREMENT NOT NULL,
	`sessionId` int NOT NULL,
	`userId` int NOT NULL,
	`operationType` varchar(100) NOT NULL,
	`operationName` varchar(255) NOT NULL,
	`inputData` json,
	`outputData` json,
	`status` enum('pending','success','failed','rolled_back') NOT NULL DEFAULT 'pending',
	`errorMessage` text,
	`executionTimeMs` int,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	CONSTRAINT `sandbox_operations_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `sandbox_sessions` (
	`id` int AUTO_INCREMENT NOT NULL,
	`userId` int NOT NULL,
	`sessionName` varchar(255) NOT NULL,
	`description` text,
	`status` enum('active','paused','completed','expired') NOT NULL DEFAULT 'active',
	`sandboxType` enum('financial','business','game','curriculum','full') NOT NULL DEFAULT 'full',
	`initialBalance` decimal(15,2) DEFAULT '100000.00',
	`timeMultiplier` decimal(5,2) DEFAULT '1.00',
	`enabledFeatures` json,
	`currentBalance` decimal(15,2) DEFAULT '100000.00',
	`totalTransactions` int DEFAULT 0,
	`totalOperations` int DEFAULT 0,
	`startedAt` timestamp NOT NULL DEFAULT (now()),
	`lastActivityAt` timestamp NOT NULL DEFAULT (now()),
	`expiresAt` timestamp,
	`completedAt` timestamp,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `sandbox_sessions_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `sandbox_snapshots` (
	`id` int AUTO_INCREMENT NOT NULL,
	`sessionId` int NOT NULL,
	`userId` int NOT NULL,
	`snapshotName` varchar(255) NOT NULL,
	`description` text,
	`sessionState` json NOT NULL,
	`entitiesState` json,
	`transactionsCount` int DEFAULT 0,
	`operationsCount` int DEFAULT 0,
	`isAutoSave` boolean DEFAULT false,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	CONSTRAINT `sandbox_snapshots_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `sandbox_templates` (
	`id` int AUTO_INCREMENT NOT NULL,
	`templateName` varchar(255) NOT NULL,
	`description` text,
	`sandboxType` enum('financial','business','game','curriculum','full') NOT NULL DEFAULT 'full',
	`initialBalance` decimal(15,2) DEFAULT '100000.00',
	`timeMultiplier` decimal(5,2) DEFAULT '1.00',
	`enabledFeatures` json,
	`preloadedEntities` json,
	`preloadedScenarios` json,
	`difficulty` enum('beginner','intermediate','advanced','expert') DEFAULT 'beginner',
	`estimatedDuration` int,
	`learningObjectives` json,
	`isPublic` boolean DEFAULT true,
	`createdBy` int,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `sandbox_templates_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `sandbox_transactions` (
	`id` int AUTO_INCREMENT NOT NULL,
	`sessionId` int NOT NULL,
	`userId` int NOT NULL,
	`transactionType` enum('deposit','withdrawal','transfer','split_allocation','token_earn','token_spend','investment','dividend','fee','refund','adjustment') NOT NULL,
	`amount` decimal(15,2) NOT NULL,
	`balanceBefore` decimal(15,2) NOT NULL,
	`balanceAfter` decimal(15,2) NOT NULL,
	`description` text,
	`metadata` json,
	`sourceType` varchar(100),
	`sourceId` varchar(100),
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	CONSTRAINT `sandbox_transactions_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `scheduled_agent_tasks` (
	`id` int AUTO_INCREMENT NOT NULL,
	`agentId` int NOT NULL,
	`name` varchar(100) NOT NULL,
	`description` text,
	`taskType` enum('daily_report','weekly_audit','monthly_analysis','content_schedule','engagement_check','seo_audit','token_report','operation_review','custom') NOT NULL,
	`prompt` text NOT NULL,
	`schedule` varchar(50) NOT NULL,
	`lastRunAt` timestamp,
	`nextRunAt` timestamp,
	`isActive` boolean NOT NULL DEFAULT true,
	`createdBy` int NOT NULL,
	`notifyOnComplete` boolean NOT NULL DEFAULT true,
	`resultHistory` json,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `scheduled_agent_tasks_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `scholarship_applications` (
	`id` int AUTO_INCREMENT NOT NULL,
	`applicantUserId` int,
	`applicantFullName` varchar(255) NOT NULL,
	`applicantEmail` varchar(320),
	`applicantPhone` varchar(20),
	`applicantAddress` text,
	`dateOfBirth` timestamp,
	`scholarshipProgramId` int NOT NULL,
	`currentEducationLevel` enum('middle_school','high_school','ged_program','community_college','university','graduate_school','academy_enrolled','other'),
	`schoolName` varchar(255),
	`gpa` decimal(3,2),
	`expectedGraduation` timestamp,
	`householdIncome` decimal(10,2),
	`householdSize` int,
	`financialNeedStatement` text,
	`communityServiceHours` int,
	`communityServiceDescription` text,
	`personalStatement` text,
	`goalsStatement` text,
	`transcriptUrl` varchar(2000),
	`recommendationLetters` json,
	`additionalDocuments` json,
	`academicScore` int,
	`needScore` int,
	`serviceScore` int,
	`essayScore` int,
	`totalScore` int,
	`reviewStatus` enum('submitted','under_review','committee_review','approved','denied','waitlisted','withdrawn') NOT NULL DEFAULT 'submitted',
	`reviewedBy` varchar(255),
	`reviewedAt` timestamp,
	`reviewNotes` text,
	`awardAmount` decimal(10,2),
	`awardedAt` timestamp,
	`awardLetter` text,
	`acceptedAt` timestamp,
	`declinedAt` timestamp,
	`declineReason` text,
	`submittedAt` timestamp NOT NULL DEFAULT (now()),
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `scholarship_applications_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `scholarship_fund` (
	`id` int AUTO_INCREMENT NOT NULL,
	`name` varchar(255) NOT NULL,
	`description` text,
	`fundType` enum('general','endowment','annual','memorial','corporate','community') NOT NULL,
	`principalBalance` decimal(12,2) NOT NULL DEFAULT '0',
	`availableBalance` decimal(12,2) NOT NULL DEFAULT '0',
	`committedBalance` decimal(12,2) NOT NULL DEFAULT '0',
	`isEndowed` boolean NOT NULL DEFAULT false,
	`endowmentMinimum` decimal(12,2),
	`annualSpendingRate` decimal(5,4),
	`linkedProgramIds` json,
	`fundStatus` enum('active','building','suspended','closed') NOT NULL DEFAULT 'active',
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `scholarship_fund_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `scholarship_fund_transactions` (
	`id` int AUTO_INCREMENT NOT NULL,
	`fundId` int NOT NULL,
	`transactionType` enum('donation','grant_deposit','interest_earned','award_disbursement','transfer_in','transfer_out','fee','adjustment') NOT NULL,
	`amount` decimal(10,2) NOT NULL,
	`donorName` varchar(255),
	`donorId` int,
	`applicationId` int,
	`programId` int,
	`description` text,
	`referenceNumber` varchar(100),
	`balanceAfter` decimal(12,2),
	`processedAt` timestamp NOT NULL DEFAULT (now()),
	`processedBy` varchar(255),
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	CONSTRAINT `scholarship_fund_transactions_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `scholarship_programs` (
	`id` int AUTO_INCREMENT NOT NULL,
	`name` varchar(255) NOT NULL,
	`description` text,
	`scholarshipType` enum('merit_based','need_based','community_service','entrepreneurship','legacy','diversity','stem','arts','full_ride') NOT NULL,
	`coverageType` enum('full_tuition','partial_tuition','stipend','materials','comprehensive') NOT NULL,
	`coverageAmount` decimal(10,2),
	`coveragePercentage` int,
	`minAge` int,
	`maxAge` int,
	`minGPA` decimal(3,2),
	`requiredCommunityHours` int,
	`incomeThreshold` decimal(10,2),
	`eligibilityCriteria` json,
	`applicationStartDate` timestamp,
	`applicationEndDate` timestamp,
	`awardDate` timestamp,
	`totalSlots` int NOT NULL DEFAULT 10,
	`filledSlots` int NOT NULL DEFAULT 0,
	`waitlistEnabled` boolean NOT NULL DEFAULT true,
	`fundingSource` varchar(255),
	`totalBudget` decimal(12,2),
	`remainingBudget` decimal(12,2),
	`renewable` boolean NOT NULL DEFAULT true,
	`maxRenewalYears` int DEFAULT 4,
	`renewalCriteria` text,
	`scholarshipProgramStatus` enum('draft','active','closed','suspended','archived') NOT NULL DEFAULT 'draft',
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `scholarship_programs_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `scroll_activations` (
	`id` int AUTO_INCREMENT NOT NULL,
	`scrollId` int NOT NULL,
	`houseId` int NOT NULL,
	`activatedByUserId` int NOT NULL,
	`activationHash` varchar(255) NOT NULL,
	`status` enum('active','suspended','revoked') NOT NULL DEFAULT 'active',
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `scroll_activations_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `scroll_seal_status` (
	`id` int AUTO_INCREMENT NOT NULL,
	`userId` varchar(255) NOT NULL,
	`houseId` int NOT NULL,
	`scrollNumber` int NOT NULL,
	`scrollTitle` varchar(255),
	`isSealed` boolean NOT NULL DEFAULT false,
	`sealedAt` timestamp,
	`sealedBy` varchar(255),
	`sealHash` varchar(64),
	`verificationMethod` enum('manual','automatic','gpt_audit','course_completion') DEFAULT 'manual',
	`courseId` int,
	`moduleId` int,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `scroll_seal_status_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `secure_documents` (
	`id` int AUTO_INCREMENT NOT NULL,
	`ownerId` int NOT NULL,
	`entityId` int,
	`folderId` int,
	`documentType` enum('business_plan','grant_application','financial_statement','legal_document','contract','certificate','report','template','other') NOT NULL,
	`title` varchar(255) NOT NULL,
	`description` text,
	`fileName` varchar(255),
	`fileUrl` varchar(500),
	`fileSize` int,
	`mimeType` varchar(100),
	`content` text,
	`version` int NOT NULL DEFAULT 1,
	`status` enum('draft','final','archived') NOT NULL DEFAULT 'draft',
	`isTemplate` boolean DEFAULT false,
	`accessLevel` enum('owner_only','entity_members','authorized_users','public') NOT NULL DEFAULT 'owner_only',
	`blockchainHash` varchar(100),
	`metadata` json,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `secure_documents_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `service_availability` (
	`id` int AUTO_INCREMENT NOT NULL,
	`userId` int NOT NULL,
	`serviceIds` json,
	`weeklySchedule` json,
	`blockedDates` json,
	`maxBookingsPerDay` int NOT NULL DEFAULT 4,
	`maxBookingsPerWeek` int NOT NULL DEFAULT 20,
	`isAcceptingBookings` boolean NOT NULL DEFAULT true,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `service_availability_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `service_bookings` (
	`id` int AUTO_INCREMENT NOT NULL,
	`bookingNumber` varchar(50) NOT NULL,
	`serviceId` int NOT NULL,
	`userId` int,
	`clientName` varchar(255) NOT NULL,
	`clientEmail` varchar(320) NOT NULL,
	`clientPhone` varchar(50),
	`clientCompany` varchar(255),
	`requestedDate` timestamp,
	`preferredTimeSlot` varchar(100),
	`scheduledAt` timestamp,
	`serviceDescription` text,
	`additionalNotes` text,
	`quotedPrice` decimal(10,2),
	`finalPrice` decimal(10,2),
	`paymentStatus` enum('pending','partial','paid','refunded') NOT NULL DEFAULT 'pending',
	`status` enum('inquiry','pending_review','quote_sent','confirmed','scheduled','in_progress','completed','cancelled','no_show') NOT NULL DEFAULT 'inquiry',
	`assignedTo` int,
	`assignedAt` timestamp,
	`completedAt` timestamp,
	`completionNotes` text,
	`followUpRequired` boolean NOT NULL DEFAULT false,
	`followUpDate` timestamp,
	`followUpNotes` text,
	`clientRating` int,
	`clientFeedback` text,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `service_bookings_id` PRIMARY KEY(`id`),
	CONSTRAINT `service_bookings_bookingNumber_unique` UNIQUE(`bookingNumber`)
);
--> statement-breakpoint
CREATE TABLE `service_catalog` (
	`id` int AUTO_INCREMENT NOT NULL,
	`serviceCode` varchar(50) NOT NULL,
	`name` varchar(255) NOT NULL,
	`description` text,
	`category` enum('contract_negotiation','business_formation','grant_writing','tax_planning','workforce_transition','mediation','compliance_audit','training','consulting','other') NOT NULL,
	`tier` enum('free','premium','business') NOT NULL,
	`basePrice` decimal(10,2) NOT NULL DEFAULT '0.00',
	`priceUnit` enum('per_hour','per_session','per_project','flat_fee') NOT NULL DEFAULT 'per_session',
	`estimatedDurationMinutes` int DEFAULT 60,
	`isActive` boolean NOT NULL DEFAULT true,
	`requiresApproval` boolean NOT NULL DEFAULT false,
	`owningEntityId` int,
	`owningEntityName` varchar(255),
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `service_catalog_id` PRIMARY KEY(`id`),
	CONSTRAINT `service_catalog_serviceCode_unique` UNIQUE(`serviceCode`)
);
--> statement-breakpoint
CREATE TABLE `service_categories` (
	`id` int AUTO_INCREMENT NOT NULL,
	`name` varchar(100) NOT NULL,
	`slug` varchar(50) NOT NULL,
	`description` text,
	`categoryType` enum('product','service','training') NOT NULL,
	`owningEntityId` int NOT NULL,
	`owningEntityName` varchar(255) NOT NULL,
	`basePrice` decimal(10,2),
	`pricingModel` enum('one_time','subscription','per_use','hourly','project_based','tiered') DEFAULT 'one_time',
	`isActive` boolean NOT NULL DEFAULT true,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `service_categories_id` PRIMARY KEY(`id`),
	CONSTRAINT `service_categories_slug_unique` UNIQUE(`slug`)
);
--> statement-breakpoint
CREATE TABLE `service_integrations` (
	`id` int AUTO_INCREMENT NOT NULL,
	`companyId` int NOT NULL,
	`sourceServiceId` int NOT NULL,
	`targetServiceId` int NOT NULL,
	`integrationType` enum('data_sync','workflow','reporting','notification','payment') NOT NULL,
	`integrationStatus` enum('pending_setup','configuring','active','paused','error','disabled') NOT NULL DEFAULT 'pending_setup',
	`syncDirection` enum('one_way','bidirectional') DEFAULT 'one_way',
	`syncFrequency` enum('realtime','hourly','daily','weekly','manual') DEFAULT 'daily',
	`fieldMappings` json,
	`transformRules` json,
	`lastSyncAt` timestamp,
	`lastSyncStatus` enum('success','partial','failed'),
	`errorCount` int DEFAULT 0,
	`lastError` text,
	`enabledAt` timestamp,
	`enabledBy` int,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `service_integrations_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `service_offerings` (
	`id` int AUTO_INCREMENT NOT NULL,
	`categoryId` int NOT NULL,
	`name` varchar(255) NOT NULL,
	`slug` varchar(100) NOT NULL,
	`description` text,
	`price` decimal(10,2) NOT NULL,
	`pricingUnit` varchar(50),
	`features` json,
	`deliveryMethod` enum('platform_access','document','consultation','workshop','course','custom') NOT NULL,
	`estimatedDeliveryDays` int,
	`isActive` boolean NOT NULL DEFAULT true,
	`isFeatured` boolean DEFAULT false,
	`sortOrder` int DEFAULT 0,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `service_offerings_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `service_orders` (
	`id` int AUTO_INCREMENT NOT NULL,
	`orderNumber` varchar(50) NOT NULL,
	`customerId` int NOT NULL,
	`customerHouseId` int,
	`customerName` varchar(255),
	`customerEmail` varchar(255),
	`serviceOfferingId` int NOT NULL,
	`serviceCategoryId` int NOT NULL,
	`fulfillingEntityId` int NOT NULL,
	`fulfillingEntityName` varchar(255),
	`quantity` int NOT NULL DEFAULT 1,
	`unitPrice` decimal(10,2) NOT NULL,
	`subtotal` decimal(10,2) NOT NULL,
	`discount` decimal(10,2) DEFAULT '0',
	`tax` decimal(10,2) DEFAULT '0',
	`total` decimal(10,2) NOT NULL,
	`resellerId` int,
	`resellerCommission` decimal(10,2) DEFAULT '0',
	`paymentStatus` enum('pending','paid','refunded','failed','cancelled') NOT NULL DEFAULT 'pending',
	`paymentMethod` varchar(50),
	`paidAt` timestamp,
	`fulfillmentStatus` enum('pending','in_progress','completed','cancelled') NOT NULL DEFAULT 'pending',
	`fulfilledAt` timestamp,
	`customerNotes` text,
	`internalNotes` text,
	`status` enum('draft','submitted','processing','completed','cancelled','refunded') NOT NULL DEFAULT 'draft',
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `service_orders_id` PRIMARY KEY(`id`),
	CONSTRAINT `service_orders_orderNumber_unique` UNIQUE(`orderNumber`)
);
--> statement-breakpoint
CREATE TABLE `service_subscriptions` (
	`id` int AUTO_INCREMENT NOT NULL,
	`companyId` int NOT NULL,
	`serviceId` int NOT NULL,
	`subscriptionStatus` enum('pending_setup','trial','active','paused','cancelled','expired') NOT NULL DEFAULT 'pending_setup',
	`monthlyPrice` decimal(10,2),
	`annualPrice` decimal(10,2),
	`billingCycle` enum('monthly','annual') DEFAULT 'monthly',
	`trialStartDate` date,
	`trialEndDate` date,
	`startDate` date,
	`endDate` date,
	`nextBillingDate` date,
	`usageCount` int DEFAULT 0,
	`usageLimit` int,
	`lastUsedAt` timestamp,
	`configuration` json,
	`cancelledAt` timestamp,
	`cancellationReason` text,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `service_subscriptions_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `signature_audit_log` (
	`id` int AUTO_INCREMENT NOT NULL,
	`signature_id` int NOT NULL,
	`action` varchar(50) NOT NULL,
	`action_by` int,
	`action_details` text,
	`ip_address` varchar(45),
	`created_at` timestamp NOT NULL DEFAULT (now()),
	CONSTRAINT `signature_audit_log_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `signature_requests` (
	`id` int AUTO_INCREMENT NOT NULL,
	`documentId` int NOT NULL,
	`documentType` varchar(100) NOT NULL,
	`documentTitle` varchar(255) NOT NULL,
	`requestedBy` int NOT NULL,
	`status` enum('pending','in_progress','completed','expired','cancelled') NOT NULL DEFAULT 'pending',
	`expiresAt` timestamp,
	`completedAt` timestamp,
	`signedDocumentUrl` text,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `signature_requests_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `signatures` (
	`id` int AUTO_INCREMENT NOT NULL,
	`requestId` int NOT NULL,
	`signerId` int NOT NULL,
	`signerName` varchar(255) NOT NULL,
	`signerEmail` varchar(320),
	`signerTitle` varchar(100),
	`signatureType` enum('typed','drawn','uploaded') NOT NULL DEFAULT 'typed',
	`signatureData` text,
	`signedAt` timestamp,
	`ipAddress` varchar(45),
	`userAgent` text,
	`status` enum('pending','signed','declined') NOT NULL DEFAULT 'pending',
	`declineReason` text,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	CONSTRAINT `signatures_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `simulator_certificates` (
	`id` int AUTO_INCREMENT NOT NULL,
	`userId` int NOT NULL,
	`simulatorId` varchar(50) NOT NULL,
	`simulatorName` varchar(255) NOT NULL,
	`totalModulesCompleted` int NOT NULL,
	`totalTokensEarned` int NOT NULL,
	`averageScore` decimal(5,2) NOT NULL,
	`certificateHash` varchar(64) NOT NULL,
	`issuedAt` timestamp NOT NULL DEFAULT (now()),
	`expiresAt` timestamp,
	`verificationUrl` text,
	CONSTRAINT `simulator_certificates_id` PRIMARY KEY(`id`),
	CONSTRAINT `simulator_certificates_certificateHash_unique` UNIQUE(`certificateHash`)
);
--> statement-breakpoint
CREATE TABLE `simulator_completions` (
	`id` int AUTO_INCREMENT NOT NULL,
	`userId` int NOT NULL,
	`houseId` int,
	`simulatorType` enum('business_formation','business_plan','grant_application','trust_formation','nonprofit_formation') NOT NULL,
	`entityType` varchar(50),
	`entityName` varchar(255),
	`entityId` int,
	`businessPlanId` int,
	`completedAt` timestamp NOT NULL DEFAULT (now()),
	`certificateIssued` boolean DEFAULT false,
	`certificateIssuedAt` timestamp,
	`trainingManagerId` int,
	`trainingManagerName` varchar(255),
	`tokensEarned` int DEFAULT 0,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	CONSTRAINT `simulator_completions_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `skill_certifications` (
	`id` int AUTO_INCREMENT NOT NULL,
	`name` varchar(255) NOT NULL,
	`code` varchar(50) NOT NULL,
	`description` text,
	`departmentId` int,
	`departmentCode` varchar(50),
	`level` enum('apprentice','journeyman','master','expert') NOT NULL,
	`prerequisiteCertifications` json,
	`requiredTrainingHours` int DEFAULT 0,
	`requiredPracticeHours` int DEFAULT 0,
	`assessmentRequired` boolean NOT NULL DEFAULT true,
	`validityMonths` int DEFAULT 24,
	`renewalRequirements` json,
	`premiumTier` enum('standard','premium','elite') NOT NULL DEFAULT 'standard',
	`assessmentFee` decimal(10,2) DEFAULT '0.00',
	`renewalFee` decimal(10,2) DEFAULT '0.00',
	`badgeUrl` text,
	`badgeColor` varchar(20),
	`displayOrder` int NOT NULL DEFAULT 0,
	`isActive` boolean NOT NULL DEFAULT true,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `skill_certifications_id` PRIMARY KEY(`id`),
	CONSTRAINT `skill_certifications_code_unique` UNIQUE(`code`)
);
--> statement-breakpoint
CREATE TABLE `skill_taxonomy` (
	`id` int AUTO_INCREMENT NOT NULL,
	`parentId` int,
	`name` varchar(255) NOT NULL,
	`code` varchar(50) NOT NULL,
	`description` text,
	`domain` enum('creative','technical','financial','legal','operational','educational','health','property','leadership','interpersonal') NOT NULL,
	`technologyTrend` enum('emerging','growing','stable','declining','legacy') NOT NULL DEFAULT 'stable',
	`relevanceScore` int NOT NULL DEFAULT 50,
	`lastTrendUpdate` timestamp,
	`externalStandardId` varchar(100),
	`externalStandardName` varchar(255),
	`isActive` boolean NOT NULL DEFAULT true,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `skill_taxonomy_id` PRIMARY KEY(`id`),
	CONSTRAINT `skill_taxonomy_code_unique` UNIQUE(`code`)
);
--> statement-breakpoint
CREATE TABLE `sme_contributors` (
	`id` int AUTO_INCREMENT NOT NULL,
	`familyMemberId` int,
	`name` varchar(200) NOT NULL,
	`expertise` json,
	`entityAffiliation` varchar(100),
	`contributionTypes` json,
	`availability` enum('available','limited','unavailable') NOT NULL DEFAULT 'available',
	`totalContributions` int NOT NULL DEFAULT 0,
	`notes` text,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `sme_contributors_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `social_media_integrations` (
	`id` int AUTO_INCREMENT NOT NULL,
	`userId` int NOT NULL,
	`platform` enum('twitter','facebook','instagram','linkedin','tiktok') NOT NULL,
	`accountName` varchar(100),
	`accountId` varchar(100),
	`accessToken` text,
	`refreshToken` text,
	`tokenExpiresAt` timestamp,
	`isActive` boolean NOT NULL DEFAULT true,
	`lastPostAt` timestamp,
	`metadata` json,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `social_media_integrations_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `social_media_posts` (
	`id` int AUTO_INCREMENT NOT NULL,
	`userId` int NOT NULL,
	`integrationId` int NOT NULL,
	`agentId` int,
	`content` text NOT NULL,
	`mediaUrls` json,
	`hashtags` json,
	`scheduledFor` timestamp,
	`publishedAt` timestamp,
	`status` enum('draft','scheduled','published','failed') NOT NULL DEFAULT 'draft',
	`platformPostId` varchar(100),
	`engagement` json,
	`errorMessage` text,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `social_media_posts_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `software_license_assignments` (
	`id` int AUTO_INCREMENT NOT NULL,
	`licenseId` int NOT NULL,
	`assignedTo` varchar(200) NOT NULL,
	`assignedType` enum('user','department','role') NOT NULL DEFAULT 'user',
	`assignedDate` timestamp NOT NULL DEFAULT (now()),
	`revokedDate` timestamp,
	`status` enum('active','revoked') NOT NULL DEFAULT 'active',
	`notes` text,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	CONSTRAINT `software_license_assignments_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `software_license_categories` (
	`id` int AUTO_INCREMENT NOT NULL,
	`name` varchar(100) NOT NULL,
	`description` text,
	`department` varchar(100),
	`icon` varchar(50),
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `software_license_categories_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `software_licenses` (
	`id` int AUTO_INCREMENT NOT NULL,
	`categoryId` int,
	`name` varchar(200) NOT NULL,
	`vendor` varchar(100) NOT NULL,
	`version` varchar(50),
	`licenseType` enum('subscription','perpetual','floating','site','open_source') NOT NULL DEFAULT 'subscription',
	`licenseKey` text,
	`totalSeats` int NOT NULL DEFAULT 1,
	`usedSeats` int NOT NULL DEFAULT 0,
	`costPerSeat` decimal(10,2),
	`totalCost` decimal(10,2),
	`billingCycle` enum('monthly','annual','one_time') NOT NULL DEFAULT 'annual',
	`purchaseDate` timestamp,
	`renewalDate` timestamp,
	`expirationDate` timestamp,
	`status` enum('active','expired','cancelled','pending') NOT NULL DEFAULT 'active',
	`notes` text,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `software_licenses_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `software_vendor_contracts` (
	`id` int AUTO_INCREMENT NOT NULL,
	`vendorName` varchar(100) NOT NULL,
	`contractType` enum('enterprise','volume','support','maintenance') NOT NULL DEFAULT 'enterprise',
	`contractNumber` varchar(100),
	`startDate` timestamp,
	`endDate` timestamp,
	`annualValue` decimal(10,2),
	`supportLevel` enum('basic','standard','premium','enterprise') NOT NULL DEFAULT 'standard',
	`contactName` varchar(100),
	`contactEmail` varchar(200),
	`contactPhone` varchar(50),
	`notes` text,
	`status` enum('active','expired','pending_renewal') NOT NULL DEFAULT 'active',
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `software_vendor_contracts_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `sovereign_scrolls` (
	`id` int AUTO_INCREMENT NOT NULL,
	`scrollNumber` int NOT NULL,
	`title` varchar(255) NOT NULL,
	`purpose` text NOT NULL,
	`content` text NOT NULL,
	`protectionType` enum('lineage_enforcement','ai_declaration','access_control','inheritance_lock','protected_names') NOT NULL,
	`enforcementRules` json,
	`sealHash` varchar(255) NOT NULL,
	`sealedAt` timestamp,
	`sealedByUserId` int,
	`status` enum('draft','sealed','amended','revoked') NOT NULL DEFAULT 'draft',
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `sovereign_scrolls_id` PRIMARY KEY(`id`),
	CONSTRAINT `sovereign_scrolls_scrollNumber_unique` UNIQUE(`scrollNumber`)
);
--> statement-breakpoint
CREATE TABLE `spark_tokens` (
	`id` int AUTO_INCREMENT NOT NULL,
	`tokenCode` varchar(64) NOT NULL,
	`ownerUserId` int NOT NULL,
	`earnedFrom` enum('course_completion','certification','mentorship','community_contribution','lineage_gift') NOT NULL,
	`sourceReferenceId` int,
	`status` enum('active','redeemed','expired') NOT NULL DEFAULT 'active',
	`redeemedAt` timestamp,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `spark_tokens_id` PRIMARY KEY(`id`),
	CONSTRAINT `spark_tokens_tokenCode_unique` UNIQUE(`tokenCode`)
);
--> statement-breakpoint
CREATE TABLE `specialist_maturity_assessments` (
	`id` int AUTO_INCREMENT NOT NULL,
	`specialistTrackId` int NOT NULL,
	`assessmentPeriod` varchar(20) NOT NULL,
	`assessmentDate` timestamp NOT NULL DEFAULT (now()),
	`assessorId` varchar(100),
	`assessorName` varchar(255),
	`academyModulesCompleted` int NOT NULL DEFAULT 0,
	`academyModulesTarget` int NOT NULL DEFAULT 3,
	`academyScore` int NOT NULL DEFAULT 0,
	`academyNotes` text,
	`simulatorSessionsCompleted` int NOT NULL DEFAULT 0,
	`simulatorAverageScore` int NOT NULL DEFAULT 0,
	`simulatorScore` int NOT NULL DEFAULT 0,
	`simulatorNotes` text,
	`attendanceRating` int NOT NULL DEFAULT 0,
	`punctualityRating` int NOT NULL DEFAULT 0,
	`initiativeRating` int NOT NULL DEFAULT 0,
	`teamworkRating` int NOT NULL DEFAULT 0,
	`qualityOfWorkRating` int NOT NULL DEFAULT 0,
	`supervisorScore` int NOT NULL DEFAULT 0,
	`supervisorComments` text,
	`tokensEarned` int NOT NULL DEFAULT 0,
	`tokensSpentWisely` boolean NOT NULL DEFAULT false,
	`tokenSavingsRate` decimal(5,2),
	`tokenEconomyScore` int NOT NULL DEFAULT 0,
	`tokenEconomyNotes` text,
	`budgetAdherence` boolean NOT NULL DEFAULT false,
	`expenseReportingAccuracy` int NOT NULL DEFAULT 0,
	`financialGoalsMet` int NOT NULL DEFAULT 0,
	`fiscalScore` int NOT NULL DEFAULT 0,
	`fiscalNotes` text,
	`totalScore` int NOT NULL DEFAULT 0,
	`recommendsAdvancement` boolean NOT NULL DEFAULT false,
	`recommendedSpecialistLevel` enum('specialist_i','specialist_ii','specialist_iii','associate'),
	`advancementJustification` text,
	`assessmentStatus` enum('draft','submitted','reviewed','approved','contested') NOT NULL DEFAULT 'draft',
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `specialist_maturity_assessments_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `specialist_milestones` (
	`id` int AUTO_INCREMENT NOT NULL,
	`specialistTrackId` int NOT NULL,
	`milestoneType` enum('level_advancement','hours_milestone','academy_completion','simulator_mastery','first_project_lead','mentorship_start','full_time_transition','graduation_ceremony') NOT NULL,
	`title` varchar(255) NOT NULL,
	`description` text,
	`achievedAt` timestamp NOT NULL DEFAULT (now()),
	`previousLevel` varchar(50),
	`newLevel` varchar(50),
	`certificateIssued` boolean NOT NULL DEFAULT false,
	`certificateId` int,
	`tokenBonus` int NOT NULL DEFAULT 0,
	`ceremonyScheduled` boolean NOT NULL DEFAULT false,
	`ceremonyDate` timestamp,
	`ceremonyNotes` text,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	CONSTRAINT `specialist_milestones_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `specialist_tracks` (
	`id` int AUTO_INCREMENT NOT NULL,
	`familyMemberId` varchar(100) NOT NULL,
	`userId` int,
	`fullName` varchar(255) NOT NULL,
	`dateOfBirth` timestamp,
	`currentAge` int,
	`hasWorkPermit` boolean NOT NULL DEFAULT false,
	`workPermitExpiry` timestamp,
	`educationStatus` enum('in_high_school','high_school_diploma','academy_enrolled','academy_graduate','ged','college_enrolled','college_graduate') NOT NULL DEFAULT 'in_high_school',
	`educationVerifiedAt` timestamp,
	`specialistLevel` enum('specialist_i','specialist_ii','specialist_iii','associate') NOT NULL DEFAULT 'specialist_i',
	`specialistEmploymentType` enum('part_time_20','part_time_25','part_time_30','full_time') NOT NULL DEFAULT 'part_time_20',
	`entityId` varchar(100),
	`entityName` varchar(255),
	`department` varchar(100),
	`positionTitle` varchar(255),
	`supervisorId` varchar(100),
	`supervisorName` varchar(255),
	`hourlyRate` decimal(10,2),
	`tokenAllocationMonthly` int DEFAULT 0,
	`trackStartDate` timestamp,
	`currentLevelStartDate` timestamp,
	`expectedGraduationDate` timestamp,
	`acceleratedTrack` boolean NOT NULL DEFAULT false,
	`maturityScore` int NOT NULL DEFAULT 0,
	`lastMaturityAssessment` timestamp,
	`specialistTrackStatus` enum('pending_eligibility','active','on_hold','graduated','terminated','transferred') NOT NULL DEFAULT 'pending_eligibility',
	`notes` text,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `specialist_tracks_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `spectator_sessions` (
	`id` int AUTO_INCREMENT NOT NULL,
	`matchId` int NOT NULL,
	`matchType` enum('game','tournament') NOT NULL,
	`spectatorId` int NOT NULL,
	`joinedAt` timestamp NOT NULL DEFAULT (now()),
	`leftAt` timestamp,
	`isActive` boolean NOT NULL DEFAULT true,
	CONSTRAINT `spectator_sessions_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `spendthrift_provisions` (
	`id` int AUTO_INCREMENT NOT NULL,
	`heirId` int NOT NULL,
	`houseId` int NOT NULL,
	`protectionLevel` enum('full','partial','none') DEFAULT 'full',
	`allowedExceptions` json,
	`provisionDocumentId` int,
	`governingLaw` varchar(100),
	`status` enum('active','modified','waived') DEFAULT 'active',
	`effectiveDate` timestamp NOT NULL DEFAULT (now()),
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `spendthrift_provisions_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `split_change_request_comments` (
	`id` int AUTO_INCREMENT NOT NULL,
	`requestId` int NOT NULL,
	`userId` int NOT NULL,
	`userName` varchar(255) NOT NULL,
	`comment` text NOT NULL,
	`commentType` enum('question','clarification','approval_note','rejection_reason','revision_request','general') NOT NULL DEFAULT 'general',
	`isInternal` boolean DEFAULT false,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	CONSTRAINT `split_change_request_comments_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `split_change_requests` (
	`id` int AUTO_INCREMENT NOT NULL,
	`houseId` varchar(100) NOT NULL,
	`houseName` varchar(255) NOT NULL,
	`requesterId` int NOT NULL,
	`requesterName` varchar(255) NOT NULL,
	`requesterRole` varchar(100),
	`currentInterHouseSplit` int NOT NULL DEFAULT 60,
	`currentIntraHouseSplit` int NOT NULL DEFAULT 70,
	`proposedInterHouseSplit` int NOT NULL,
	`proposedIntraHouseSplit` int NOT NULL,
	`justification` text NOT NULL,
	`effectiveDate` timestamp NOT NULL,
	`expirationDate` timestamp,
	`estimatedImpact` json,
	`affectedAccounts` json,
	`splitRequestStatus` enum('draft','pending_review','under_review','approved','rejected','implemented','expired','reverted') NOT NULL DEFAULT 'draft',
	`reviewerId` int,
	`reviewerName` varchar(255),
	`reviewedAt` timestamp,
	`reviewNotes` text,
	`implementedAt` timestamp,
	`implementedBy` int,
	`blockchainHash` varchar(255),
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `split_change_requests_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `split_configuration_history` (
	`id` int AUTO_INCREMENT NOT NULL,
	`houseId` varchar(100) NOT NULL,
	`houseName` varchar(255) NOT NULL,
	`interHouseSplit` int NOT NULL,
	`intraHouseSplit` int NOT NULL,
	`changeType` enum('initial','approved_request','admin_override','system_default','reversion') NOT NULL,
	`changeRequestId` int,
	`changedBy` int,
	`changedByName` varchar(255),
	`changeReason` text,
	`effectiveFrom` timestamp NOT NULL,
	`effectiveTo` timestamp,
	`blockchainHash` varchar(255),
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	CONSTRAINT `split_configuration_history_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `statements_of_work` (
	`id` int AUTO_INCREMENT NOT NULL,
	`sowNumber` varchar(50) NOT NULL,
	`contractId` int NOT NULL,
	`title` varchar(500) NOT NULL,
	`description` text,
	`scope` text,
	`deliverables` text,
	`startDate` timestamp,
	`endDate` timestamp,
	`estimatedHours` int,
	`actualHours` int DEFAULT 0,
	`fixedPrice` decimal(15,2),
	`hourlyRate` decimal(10,2),
	`budgetAmount` decimal(15,2),
	`milestones` text,
	`acceptanceCriteria` text,
	`projectManagerId` int,
	`approvedBy` int,
	`approvedAt` timestamp,
	`completedAt` timestamp,
	`status` enum('draft','pending_approval','active','completed','cancelled','on_hold') NOT NULL DEFAULT 'draft',
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `statements_of_work_id` PRIMARY KEY(`id`),
	CONSTRAINT `statements_of_work_sowNumber_unique` UNIQUE(`sowNumber`)
);
--> statement-breakpoint
CREATE TABLE `stock_alerts` (
	`id` int AUTO_INCREMENT NOT NULL,
	`watchlistId` int NOT NULL,
	`symbol` varchar(10) NOT NULL,
	`alertType` enum('price_up','price_down','earnings_upcoming','earnings_released','dividend_announced','dividend_ex_date','sec_filing','analyst_upgrade','analyst_downgrade','insider_buy','insider_sell','news_major','52_week_high','52_week_low') NOT NULL,
	`title` varchar(500) NOT NULL,
	`description` text,
	`sourceUrl` varchar(1000),
	`priceAtAlert` decimal(12,4),
	`changeAmount` decimal(12,4),
	`changePercent` decimal(6,2),
	`priority` enum('critical','high','medium','low') NOT NULL DEFAULT 'medium',
	`isRead` boolean NOT NULL DEFAULT false,
	`isDismissed` boolean NOT NULL DEFAULT false,
	`showInTicker` boolean NOT NULL DEFAULT true,
	`eventDate` timestamp,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	CONSTRAINT `stock_alerts_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `stock_events` (
	`id` int AUTO_INCREMENT NOT NULL,
	`symbol` varchar(10) NOT NULL,
	`eventType` enum('earnings','dividend_ex_date','dividend_pay_date','split','sec_filing','shareholder_meeting','analyst_day') NOT NULL,
	`eventDate` timestamp NOT NULL,
	`title` varchar(500) NOT NULL,
	`description` text,
	`estimatedEps` decimal(8,4),
	`actualEps` decimal(8,4),
	`dividendAmount` decimal(8,4),
	`filingType` varchar(20),
	`filingUrl` varchar(1000),
	`isConfirmed` boolean NOT NULL DEFAULT false,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()),
	CONSTRAINT `stock_events_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `stock_price_history` (
	`id` int AUTO_INCREMENT NOT NULL,
	`symbol` varchar(10) NOT NULL,
	`date` timestamp NOT NULL,
	`open` decimal(12,4),
	`high` decimal(12,4),
	`low` decimal(12,4),
	`close` decimal(12,4) NOT NULL,
	`volume` bigint,
	`adjustedClose` decimal(12,4),
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	CONSTRAINT `stock_price_history_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `stock_watchlist` (
	`id` int AUTO_INCREMENT NOT NULL,
	`symbol` varchar(10) NOT NULL,
	`companyName` varchar(255) NOT NULL,
	`exchange` varchar(50),
	`entityId` int,
	`isHolding` boolean NOT NULL DEFAULT false,
	`shares` decimal(15,4),
	`costBasis` decimal(12,2),
	`purchaseDate` timestamp,
	`lastPrice` decimal(12,4),
	`lastPriceUpdated` timestamp,
	`dayChange` decimal(10,4),
	`dayChangePercent` decimal(6,2),
	`alertOnPriceChange` boolean NOT NULL DEFAULT true,
	`priceChangeThreshold` decimal(5,2) DEFAULT '5.00',
	`alertOnEarnings` boolean NOT NULL DEFAULT true,
	`alertOnDividends` boolean NOT NULL DEFAULT true,
	`alertOnFilings` boolean NOT NULL DEFAULT true,
	`alertOnNews` boolean NOT NULL DEFAULT false,
	`notes` text,
	`sector` varchar(100),
	`industry` varchar(100),
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()),
	`createdBy` int,
	CONSTRAINT `stock_watchlist_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `streak_notifications` (
	`id` int AUTO_INCREMENT NOT NULL,
	`userId` int NOT NULL,
	`streakId` int NOT NULL,
	`streakNotificationType` enum('streak_at_risk','streak_lost','streak_milestone','weekly_summary','encouragement') NOT NULL,
	`title` varchar(255) NOT NULL,
	`message` text NOT NULL,
	`deliveryMethod` enum('in_app','email','push','sms') NOT NULL DEFAULT 'in_app',
	`sentAt` timestamp NOT NULL DEFAULT (now()),
	`readAt` timestamp,
	`streakCount` int,
	`minutesRead` int,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	CONSTRAINT `streak_notifications_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `student_placement_assessments` (
	`id` int AUTO_INCREMENT NOT NULL,
	`studentProfileId` int NOT NULL,
	`guardianUserId` int,
	`assessmentType` enum('initial_placement','progress_checkpoint','level_advancement','subject_specific') NOT NULL,
	`landScore` int,
	`airScore` int,
	`waterScore` int,
	`selfScore` int,
	`readingLevel` int,
	`mathLevel` int,
	`criticalThinkingLevel` int,
	`learningStyle` enum('visual','auditory','reading_writing','kinesthetic','multimodal'),
	`learningStyleScores` json,
	`recommendedHouseId` int,
	`recommendedGradeLevel` varchar(10),
	`recommendedCourses` json,
	`recommendedPace` enum('accelerated','standard','supported','intensive_support') DEFAULT 'standard',
	`aiAnalysis` json,
	`strengthAreas` json,
	`growthAreas` json,
	`parentReviewed` boolean NOT NULL DEFAULT false,
	`parentReviewedAt` timestamp,
	`parentApproved` boolean DEFAULT false,
	`parentNotes` text,
	`parentOverride` boolean NOT NULL DEFAULT false,
	`overriddenCourses` json,
	`overrideReason` text,
	`enrollmentFinalized` boolean NOT NULL DEFAULT false,
	`finalizedAt` timestamp,
	`finalizedCourses` json,
	`placementStatus` enum('in_progress','completed','pending_parent_review','parent_approved','enrolled','expired') NOT NULL DEFAULT 'in_progress',
	`startedAt` timestamp NOT NULL DEFAULT (now()),
	`completedAt` timestamp,
	`expiresAt` timestamp,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `student_placement_assessments_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `student_profiles` (
	`id` int AUTO_INCREMENT NOT NULL,
	`userId` int NOT NULL,
	`guardianUserId` int,
	`houseId` int,
	`displayName` varchar(100),
	`gradeLevel` varchar(10),
	`birthYear` int,
	`primaryLanguageId` int,
	`selectedLanguages` json,
	`ceremonialPath` varchar(100),
	`preferences` json,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `student_profiles_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `student_progress` (
	`id` int AUTO_INCREMENT NOT NULL,
	`studentProfileId` int NOT NULL,
	`courseId` int,
	`lessonId` int,
	`languageLessonId` int,
	`progressType` enum('course','lesson','language') NOT NULL,
	`status` enum('not_started','in_progress','completed','mastered') NOT NULL DEFAULT 'not_started',
	`progressPercentage` int DEFAULT 0,
	`score` int,
	`tokensEarned` int DEFAULT 0,
	`timeSpentMinutes` int DEFAULT 0,
	`startedAt` timestamp,
	`completedAt` timestamp,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `student_progress_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `subscription_plans` (
	`id` int AUTO_INCREMENT NOT NULL,
	`name` varchar(100) NOT NULL,
	`slug` varchar(50) NOT NULL,
	`description` text,
	`owningEntityId` int NOT NULL,
	`tier` enum('starter','professional','enterprise','custom') NOT NULL,
	`monthlyPrice` decimal(10,2) NOT NULL,
	`annualPrice` decimal(10,2),
	`features` json,
	`limits` json,
	`includedServiceIds` json,
	`isActive` boolean NOT NULL DEFAULT true,
	`isFeatured` boolean DEFAULT false,
	`sortOrder` int DEFAULT 0,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `subscription_plans_id` PRIMARY KEY(`id`),
	CONSTRAINT `subscription_plans_slug_unique` UNIQUE(`slug`)
);
--> statement-breakpoint
CREATE TABLE `succession_events` (
	`id` int AUTO_INCREMENT NOT NULL,
	`houseId` int NOT NULL,
	`outgoingCustodianId` int NOT NULL,
	`incomingCustodianId` int NOT NULL,
	`interimCustodianId` int,
	`reason` enum('voluntary_transfer','incapacitation','death','removal_by_majority','generational_transition') NOT NULL,
	`initiatedAt` timestamp NOT NULL DEFAULT (now()),
	`interimStartedAt` timestamp,
	`interimEndsAt` timestamp,
	`alignmentStatementDueAt` timestamp,
	`alignmentStatementReceivedAt` timestamp,
	`alignmentStatementHash` varchar(255),
	`confirmation1At` timestamp,
	`confirmation1ByUserId` int,
	`confirmation2At` timestamp,
	`confirmation2ByUserId` int,
	`confirmation3At` timestamp,
	`confirmation3ByUserId` int,
	`status` enum('initiated','interim_period','awaiting_alignment','awaiting_confirmations','completed','rejected','reverted') NOT NULL DEFAULT 'initiated',
	`completedAt` timestamp,
	`blockchainHash` varchar(255),
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `succession_events_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `supported_locales` (
	`id` int AUTO_INCREMENT NOT NULL,
	`locale_code` varchar(10) NOT NULL,
	`language_code` varchar(5) NOT NULL,
	`country_code` varchar(5),
	`display_name` varchar(100) NOT NULL,
	`native_name` varchar(100) NOT NULL,
	`date_format` varchar(50) NOT NULL DEFAULT 'YYYY-MM-DD',
	`time_format` varchar(50) NOT NULL DEFAULT 'HH:mm:ss',
	`number_format` varchar(50) NOT NULL DEFAULT '1,234.56',
	`default_currency_code` varchar(10) NOT NULL DEFAULT 'USD',
	`currency_symbol` varchar(10) NOT NULL DEFAULT '$',
	`currency_position` enum('before','after') NOT NULL DEFAULT 'before',
	`is_active` boolean NOT NULL DEFAULT true,
	`is_default` boolean NOT NULL DEFAULT false,
	`created_at` timestamp NOT NULL DEFAULT (now()),
	`updated_at` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `supported_locales_id` PRIMARY KEY(`id`),
	CONSTRAINT `supported_locales_locale_code_unique` UNIQUE(`locale_code`)
);
--> statement-breakpoint
CREATE TABLE `supported_tokens` (
	`id` int AUTO_INCREMENT NOT NULL,
	`network_id` int NOT NULL,
	`token_symbol` varchar(50) NOT NULL,
	`token_name` varchar(255) NOT NULL,
	`contract_address` varchar(255),
	`token_type` enum('native','erc20','erc721','erc1155','spl','bep20','trc20','stablecoin','wrapped','luvtoken','custom') NOT NULL,
	`decimals` int NOT NULL DEFAULT 18,
	`logo_url` varchar(500),
	`is_stablecoin` boolean NOT NULL DEFAULT false,
	`pegged_to` varchar(10),
	`current_price_usd` decimal(20,8),
	`price_last_updated_at` timestamp,
	`is_active` boolean NOT NULL DEFAULT true,
	`is_receivable` boolean NOT NULL DEFAULT true,
	`is_sendable` boolean NOT NULL DEFAULT true,
	`created_at` timestamp NOT NULL DEFAULT (now()),
	`updated_at` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `supported_tokens_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `swal_nft_transfers` (
	`id` int AUTO_INCREMENT NOT NULL,
	`nftId` int NOT NULL,
	`fromUserId` int,
	`fromHouseId` int,
	`toUserId` int,
	`toHouseId` int,
	`transferType` enum('mint','gift','sale','house_deposit','house_withdrawal') NOT NULL,
	`salePrice` decimal(10,2),
	`royaltyPaid` decimal(10,2),
	`transactionHash` varchar(255) NOT NULL,
	`transferredAt` timestamp NOT NULL DEFAULT (now()),
	CONSTRAINT `swal_nft_transfers_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `swal_nfts` (
	`id` int AUTO_INCREMENT NOT NULL,
	`nftId` varchar(100) NOT NULL,
	`userId` int NOT NULL,
	`houseId` int,
	`collection` enum('the_awakening','the_healing','the_enlightenment','the_foundation','the_crown') NOT NULL,
	`phase` enum('self','water','air','land','sovereignty') NOT NULL,
	`name` varchar(255) NOT NULL,
	`description` text,
	`imageUrl` varchar(500),
	`rarity` enum('common','uncommon','rare','epic','legendary') NOT NULL,
	`rarityScore` int NOT NULL DEFAULT 0,
	`traits` json NOT NULL,
	`completionLevel` int NOT NULL,
	`completionStats` json NOT NULL,
	`questsCompleted` int NOT NULL,
	`timePlayed` int NOT NULL,
	`sourceTokenId` int NOT NULL,
	`blockchainHash` varchar(255) NOT NULL,
	`isListed` boolean NOT NULL DEFAULT false,
	`listingPrice` decimal(10,2),
	`mintedAt` timestamp NOT NULL DEFAULT (now()),
	`transferredAt` timestamp,
	CONSTRAINT `swal_nfts_id` PRIMARY KEY(`id`),
	CONSTRAINT `swal_nfts_nftId_unique` UNIQUE(`nftId`)
);
--> statement-breakpoint
CREATE TABLE `swal_purchases` (
	`id` int AUTO_INCREMENT NOT NULL,
	`userId` int NOT NULL,
	`tokenId` int NOT NULL,
	`phase` enum('self','water','air','land','sovereignty') NOT NULL,
	`basePrice` decimal(10,2) NOT NULL,
	`discountApplied` decimal(5,2) NOT NULL DEFAULT '0',
	`finalPrice` decimal(10,2) NOT NULL,
	`paymentMethod` enum('stripe','crypto','earned','gifted') NOT NULL,
	`paymentReference` varchar(255),
	`membershipType` enum('public','academy','employee') NOT NULL,
	`transactionHash` varchar(255) NOT NULL,
	`purchasedAt` timestamp NOT NULL DEFAULT (now()),
	CONSTRAINT `swal_purchases_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `swal_royalty_distributions` (
	`id` int AUTO_INCREMENT NOT NULL,
	`transferId` int NOT NULL,
	`totalRoyalty` decimal(10,2) NOT NULL,
	`creatorAmount` decimal(10,2) NOT NULL,
	`communityAmount` decimal(10,2) NOT NULL,
	`platformAmount` decimal(10,2) NOT NULL,
	`distributionHash` varchar(255) NOT NULL,
	`distributedAt` timestamp NOT NULL DEFAULT (now()),
	CONSTRAINT `swal_royalty_distributions_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `swal_token_supply` (
	`id` int AUTO_INCREMENT NOT NULL,
	`phase` enum('self','water','air','land','sovereignty') NOT NULL,
	`tier` enum('genesis','flow','ascend','root','crown') NOT NULL,
	`totalSupply` int NOT NULL,
	`soldCount` int NOT NULL DEFAULT 0,
	`reservedCount` int NOT NULL DEFAULT 0,
	`burnedCount` int NOT NULL DEFAULT 0,
	`basePrice` decimal(10,2) NOT NULL,
	`currentPrice` decimal(10,2) NOT NULL,
	`valueMultiplier` decimal(5,2) NOT NULL,
	`isActive` boolean NOT NULL DEFAULT false,
	`activatedAt` timestamp,
	`totalRevenue` decimal(20,2) NOT NULL DEFAULT '0',
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `swal_token_supply_id` PRIMARY KEY(`id`),
	CONSTRAINT `swal_token_supply_phase_unique` UNIQUE(`phase`)
);
--> statement-breakpoint
CREATE TABLE `swal_tokens` (
	`id` int AUTO_INCREMENT NOT NULL,
	`tokenId` varchar(100) NOT NULL,
	`phase` enum('self','water','air','land','sovereignty') NOT NULL,
	`tier` enum('genesis','flow','ascend','root','crown') NOT NULL,
	`tokenNumber` int NOT NULL,
	`ownerId` int,
	`ownerType` enum('user','house','entity'),
	`purchasePrice` decimal(10,2),
	`currentValue` decimal(10,2) NOT NULL,
	`status` enum('available','owned','burned','reserved') NOT NULL DEFAULT 'available',
	`blockchainHash` varchar(255) NOT NULL,
	`mintedAt` timestamp NOT NULL DEFAULT (now()),
	`purchasedAt` timestamp,
	`burnedAt` timestamp,
	CONSTRAINT `swal_tokens_id` PRIMARY KEY(`id`),
	CONSTRAINT `swal_tokens_tokenId_unique` UNIQUE(`tokenId`)
);
--> statement-breakpoint
CREATE TABLE `swal_unlocks` (
	`id` int AUTO_INCREMENT NOT NULL,
	`userId` int NOT NULL,
	`phase` enum('self','water','air','land','sovereignty') NOT NULL,
	`tokenId` int NOT NULL,
	`nftId` int NOT NULL,
	`blockchainHash` varchar(255) NOT NULL,
	`unlockedAt` timestamp NOT NULL DEFAULT (now()),
	CONSTRAINT `swal_unlocks_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `swal_user_progress` (
	`id` int AUTO_INCREMENT NOT NULL,
	`userId` int NOT NULL,
	`currentPhase` enum('self','water','air','land','sovereignty') NOT NULL DEFAULT 'self',
	`membershipType` enum('public','academy','employee') NOT NULL DEFAULT 'public',
	`tokensOwned` int NOT NULL DEFAULT 0,
	`tokensBurned` int NOT NULL DEFAULT 0,
	`tokensEarned` int NOT NULL DEFAULT 0,
	`nftsOwned` int NOT NULL DEFAULT 0,
	`totalInvested` decimal(10,2) NOT NULL DEFAULT '0',
	`portfolioValue` decimal(10,2) NOT NULL DEFAULT '0',
	`selfCompleted` boolean NOT NULL DEFAULT false,
	`waterCompleted` boolean NOT NULL DEFAULT false,
	`airCompleted` boolean NOT NULL DEFAULT false,
	`landCompleted` boolean NOT NULL DEFAULT false,
	`sovereigntyAchieved` boolean NOT NULL DEFAULT false,
	`journeyStartedAt` timestamp NOT NULL DEFAULT (now()),
	`sovereigntyAchievedAt` timestamp,
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `swal_user_progress_id` PRIMARY KEY(`id`),
	CONSTRAINT `swal_user_progress_userId_unique` UNIQUE(`userId`)
);
--> statement-breakpoint
CREATE TABLE `swot_analyses` (
	`id` int AUTO_INCREMENT NOT NULL,
	`businessEntityId` int,
	`userId` int NOT NULL,
	`title` varchar(255) NOT NULL,
	`description` text,
	`swotStatus` enum('draft','active','archived') NOT NULL DEFAULT 'draft',
	`analysisDate` timestamp NOT NULL DEFAULT (now()),
	`reviewDate` timestamp,
	`strengthScore` int DEFAULT 0,
	`weaknessScore` int DEFAULT 0,
	`opportunityScore` int DEFAULT 0,
	`threatScore` int DEFAULT 0,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `swot_analyses_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `swot_items` (
	`id` int AUTO_INCREMENT NOT NULL,
	`swotAnalysisId` int NOT NULL,
	`swotCategory` enum('strength','weakness','opportunity','threat') NOT NULL,
	`title` varchar(255) NOT NULL,
	`description` text,
	`swotPriority` enum('low','medium','high','critical') NOT NULL DEFAULT 'medium',
	`impact` int DEFAULT 5,
	`actionRequired` boolean DEFAULT false,
	`actionPlan` text,
	`actionStatus` enum('pending','in_progress','completed','cancelled') DEFAULT 'pending',
	`actionDueDate` timestamp,
	`sortOrder` int DEFAULT 0,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `swot_items_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `sync_cycles` (
	`id` int AUTO_INCREMENT NOT NULL,
	`cycleType` enum('hourly','daily','weekly','monthly','quarterly','annual') NOT NULL,
	`houseId` int,
	`scheduledAt` timestamp NOT NULL,
	`startedAt` timestamp,
	`completedAt` timestamp,
	`cycleStatus` enum('scheduled','running','completed','failed','skipped') NOT NULL DEFAULT 'scheduled',
	`transactionsProcessed` int NOT NULL DEFAULT 0,
	`allocationsCreated` int NOT NULL DEFAULT 0,
	`errorsEncountered` int NOT NULL DEFAULT 0,
	`summary` json,
	`errorLog` json,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	CONSTRAINT `sync_cycles_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `system_audit_log` (
	`id` int AUTO_INCREMENT NOT NULL,
	`eventType` enum('income_received','distribution_executed','house_created','member_added','business_linked','rule_triggered','inheritance_vested','manual_override') NOT NULL,
	`entityType` varchar(50) NOT NULL,
	`entityId` int NOT NULL,
	`actorType` enum('system','user','admin') NOT NULL,
	`actorId` int,
	`beforeState` json,
	`afterState` json,
	`description` text,
	`blockchainHash` varchar(255),
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	CONSTRAINT `system_audit_log_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `system_job_configurations` (
	`id` int AUTO_INCREMENT NOT NULL,
	`job_name` varchar(100) NOT NULL,
	`description` text,
	`cron_schedule` varchar(50) NOT NULL,
	`timezone` varchar(50) NOT NULL DEFAULT 'America/Chicago',
	`is_active` boolean NOT NULL DEFAULT true,
	`last_run_at` timestamp,
	`last_run_status` enum('completed','failed'),
	`next_scheduled_run` timestamp,
	`notify_on_success` boolean NOT NULL DEFAULT false,
	`notify_on_failure` boolean NOT NULL DEFAULT true,
	`notification_recipients` json,
	`max_retries` int NOT NULL DEFAULT 3,
	`retry_delay_seconds` int NOT NULL DEFAULT 300,
	`created_at` timestamp NOT NULL DEFAULT (now()),
	`updated_at` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `system_job_configurations_id` PRIMARY KEY(`id`),
	CONSTRAINT `system_job_configurations_job_name_unique` UNIQUE(`job_name`)
);
--> statement-breakpoint
CREATE TABLE `system_job_executions` (
	`id` int AUTO_INCREMENT NOT NULL,
	`job_name` varchar(100) NOT NULL,
	`job_type` enum('signature_expiration_notifications','daily_summary_report','weekly_compliance_audit','monthly_data_cleanup','custom') NOT NULL,
	`started_at` timestamp NOT NULL,
	`completed_at` timestamp,
	`duration_ms` int,
	`status` enum('running','completed','failed') NOT NULL DEFAULT 'running',
	`result` json,
	`error_message` text,
	`error_stack` text,
	`items_processed` int DEFAULT 0,
	`items_succeeded` int DEFAULT 0,
	`items_failed` int DEFAULT 0,
	`triggered_by` enum('scheduled','manual','api') NOT NULL DEFAULT 'scheduled',
	`triggered_by_user_id` int,
	`created_at` timestamp NOT NULL DEFAULT (now()),
	CONSTRAINT `system_job_executions_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `system_versions` (
	`id` int AUTO_INCREMENT NOT NULL,
	`version_number` varchar(50) NOT NULL,
	`version_name` varchar(255),
	`component_type` enum('core','ai_provider','blockchain_network','payment_processor','localization','security','ui','api') NOT NULL,
	`component_name` varchar(255) NOT NULL,
	`release_notes` text,
	`breaking_changes` json,
	`version_status` enum('development','beta','stable','deprecated','retired') NOT NULL DEFAULT 'development',
	`released_at` timestamp,
	`deprecated_at` timestamp,
	`retired_at` timestamp,
	`created_at` timestamp NOT NULL DEFAULT (now()),
	CONSTRAINT `system_versions_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `tax_deductions` (
	`id` int AUTO_INCREMENT NOT NULL,
	`tax_year_id` int NOT NULL,
	`house_id` int NOT NULL,
	`deduction_category` enum('mortgage_interest','property_tax','state_local_tax','charitable','medical','business_expense','home_office','education','retirement_contribution','health_savings','child_care','other') NOT NULL,
	`description` varchar(500) NOT NULL,
	`amount` decimal(18,2) NOT NULL,
	`deduction_date` timestamp NOT NULL,
	`receipt_url` varchar(500),
	`vault_document_id` int,
	`is_verified` boolean NOT NULL DEFAULT false,
	`created_at` timestamp NOT NULL DEFAULT (now()),
	CONSTRAINT `tax_deductions_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `tax_documents` (
	`id` int AUTO_INCREMENT NOT NULL,
	`tax_year_id` int NOT NULL,
	`house_id` int NOT NULL,
	`tax_document_type` enum('w2','1099_misc','1099_nec','1099_int','1099_div','1099_b','1098','receipt','invoice','bank_statement','other') NOT NULL,
	`document_name` varchar(255) NOT NULL,
	`description` text,
	`issuer_name` varchar(255),
	`issuer_ein` varchar(20),
	`reported_amount` decimal(18,2),
	`tax_withheld` decimal(18,2),
	`vault_document_id` int,
	`is_verified` boolean NOT NULL DEFAULT false,
	`verified_at` timestamp,
	`created_at` timestamp NOT NULL DEFAULT (now()),
	CONSTRAINT `tax_documents_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `tax_filings` (
	`id` int AUTO_INCREMENT NOT NULL,
	`tax_year_id` int NOT NULL,
	`house_id` int NOT NULL,
	`tax_filing_type` enum('federal','state','local','amended_federal','amended_state') NOT NULL,
	`form_number` varchar(50) NOT NULL,
	`submitted_at` timestamp,
	`submission_method` enum('e_file','mail','in_person'),
	`confirmation_number` varchar(100),
	`filing_status` enum('draft','submitted','accepted','rejected','processing') NOT NULL DEFAULT 'draft',
	`rejection_reason` text,
	`filing_document_id` int,
	`created_at` timestamp NOT NULL DEFAULT (now()),
	`updated_at` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `tax_filings_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `tax_treaties` (
	`id` int AUTO_INCREMENT NOT NULL,
	`countryCode` varchar(3) NOT NULL,
	`countryName` varchar(255) NOT NULL,
	`treatyYear` int,
	`effectiveDate` timestamp,
	`dividendRate` decimal(5,2),
	`interestRate` decimal(5,2),
	`royaltyRate` decimal(5,2),
	`limitationOnBenefits` boolean DEFAULT false,
	`exchangeOfInformation` boolean DEFAULT true,
	`arbitrationProvision` boolean DEFAULT false,
	`treatyText` text,
	`technicalExplanation` text,
	`isActive` boolean DEFAULT true,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	CONSTRAINT `tax_treaties_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `tax_years` (
	`id` int AUTO_INCREMENT NOT NULL,
	`house_id` int NOT NULL,
	`user_id` int NOT NULL,
	`tax_year` int NOT NULL,
	`tax_filing_status` enum('single','married_filing_jointly','married_filing_separately','head_of_household','qualifying_widow') NOT NULL,
	`total_income` decimal(18,2) NOT NULL DEFAULT '0',
	`adjusted_gross_income` decimal(18,2) NOT NULL DEFAULT '0',
	`taxable_income` decimal(18,2) NOT NULL DEFAULT '0',
	`standard_deduction` decimal(18,2) NOT NULL DEFAULT '0',
	`itemized_deductions` decimal(18,2) NOT NULL DEFAULT '0',
	`use_itemized` boolean NOT NULL DEFAULT false,
	`total_tax_liability` decimal(18,2) NOT NULL DEFAULT '0',
	`total_tax_paid` decimal(18,2) NOT NULL DEFAULT '0',
	`refund_or_owed` decimal(18,2) NOT NULL DEFAULT '0',
	`tax_year_status` enum('in_progress','ready_to_file','filed','accepted','rejected','amended') NOT NULL DEFAULT 'in_progress',
	`filed_at` timestamp,
	`accepted_at` timestamp,
	`created_at` timestamp NOT NULL DEFAULT (now()),
	`updated_at` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `tax_years_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `team_event_participants` (
	`id` int AUTO_INCREMENT NOT NULL,
	`eventId` int NOT NULL,
	`userId` int NOT NULL,
	`rsvpStatus` enum('pending','accepted','declined','tentative') NOT NULL DEFAULT 'pending',
	`rsvpAt` timestamp,
	`attended` boolean DEFAULT false,
	`checkInTime` timestamp,
	`checkOutTime` timestamp,
	`minutesParticipated` int DEFAULT 0,
	`teamName` varchar(100),
	`teamColor` varchar(20),
	`individualScore` int DEFAULT 0,
	`wasOnWinningTeam` boolean DEFAULT false,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	CONSTRAINT `team_event_participants_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `team_game_events` (
	`id` int AUTO_INCREMENT NOT NULL,
	`title` varchar(255) NOT NULL,
	`description` text,
	`gameId` int,
	`gameSlug` varchar(100),
	`scheduledStart` timestamp NOT NULL,
	`scheduledEnd` timestamp NOT NULL,
	`durationMinutes` int NOT NULL,
	`eventType` enum('team_battle','house_championship','laws_tournament','training_session','casual_play') NOT NULL,
	`departmentId` int,
	`houseId` int,
	`entityId` int,
	`maxParticipants` int,
	`minParticipants` int DEFAULT 2,
	`isRequired` boolean DEFAULT false,
	`isRecurring` boolean DEFAULT false,
	`recurrencePattern` varchar(50),
	`recurrenceEndDate` timestamp,
	`parentEventId` int,
	`status` enum('scheduled','in_progress','completed','cancelled') NOT NULL DEFAULT 'scheduled',
	`winningTeam` varchar(255),
	`winningHouse` int,
	`createdBy` int NOT NULL,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `team_game_events_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `time_entries` (
	`id` int AUTO_INCREMENT NOT NULL,
	`workerId` int NOT NULL,
	`chargeCodeId` int NOT NULL,
	`timesheetId` int,
	`entryDate` timestamp NOT NULL,
	`hoursWorked` decimal(5,2) NOT NULL,
	`overtimeHours` decimal(5,2) DEFAULT '0.00',
	`description` text,
	`taskReference` varchar(100),
	`projectId` int,
	`isBillable` boolean NOT NULL DEFAULT true,
	`billingRate` decimal(10,2),
	`status` enum('draft','submitted','approved','rejected','invoiced') NOT NULL DEFAULT 'draft',
	`rejectionReason` text,
	`approvedBy` int,
	`approvedAt` timestamp,
	`clockInTime` timestamp,
	`clockOutTime` timestamp,
	`breakMinutes` int DEFAULT 0,
	`location` varchar(255),
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `time_entries_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `time_off_requests` (
	`id` int AUTO_INCREMENT NOT NULL,
	`workerId` int NOT NULL,
	`requestType` enum('pto','sick','personal','bereavement','jury_duty','military','unpaid','other') NOT NULL,
	`startDate` timestamp NOT NULL,
	`endDate` timestamp NOT NULL,
	`totalHours` decimal(6,2) NOT NULL,
	`reason` text,
	`status` enum('pending','approved','rejected','cancelled') NOT NULL DEFAULT 'pending',
	`approvedBy` int,
	`approvedAt` timestamp,
	`rejectionReason` text,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `time_off_requests_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `timekeeping_workers` (
	`id` int AUTO_INCREMENT NOT NULL,
	`userId` int,
	`employeeId` int,
	`contractorId` int,
	`workerType` enum('employee','contractor','volunteer') NOT NULL,
	`firstName` varchar(100) NOT NULL,
	`lastName` varchar(100) NOT NULL,
	`email` varchar(320),
	`departmentId` int,
	`entityId` int,
	`supervisorId` int,
	`defaultChargeCodeId` int,
	`hourlyRate` decimal(10,2),
	`standardHoursPerWeek` decimal(5,2) DEFAULT '40.00',
	`overtimeEligible` boolean DEFAULT true,
	`status` enum('active','inactive','terminated') NOT NULL DEFAULT 'active',
	`hireDate` timestamp,
	`terminationDate` timestamp,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `timekeeping_workers_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `timesheet_approvals` (
	`id` int AUTO_INCREMENT NOT NULL,
	`timesheetId` int NOT NULL,
	`approverId` int NOT NULL,
	`approverRole` enum('supervisor','department_head','finance','admin') NOT NULL,
	`approvalLevel` int NOT NULL DEFAULT 1,
	`action` enum('approved','rejected','returned_for_revision') NOT NULL,
	`comments` text,
	`approverSignature` text,
	`actionAt` timestamp NOT NULL DEFAULT (now()),
	CONSTRAINT `timesheet_approvals_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `timesheets` (
	`id` int AUTO_INCREMENT NOT NULL,
	`workerId` int NOT NULL,
	`periodStart` timestamp NOT NULL,
	`periodEnd` timestamp NOT NULL,
	`periodType` enum('weekly','biweekly','semimonthly','monthly') NOT NULL DEFAULT 'biweekly',
	`totalRegularHours` decimal(6,2) DEFAULT '0.00',
	`totalOvertimeHours` decimal(6,2) DEFAULT '0.00',
	`totalBillableHours` decimal(6,2) DEFAULT '0.00',
	`totalNonBillableHours` decimal(6,2) DEFAULT '0.00',
	`status` enum('draft','submitted','pending_approval','approved','rejected','processed') NOT NULL DEFAULT 'draft',
	`submittedAt` timestamp,
	`submittedBy` int,
	`workerSignature` text,
	`workerSignedAt` timestamp,
	`notes` text,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `timesheets_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `token_activation_attempts` (
	`id` int AUTO_INCREMENT NOT NULL,
	`houseId` int NOT NULL,
	`userId` varchar(255) NOT NULL,
	`attemptTokenType` enum('mirror','gift','spark','house','crown') NOT NULL,
	`attemptStatus` enum('approved','denied','pending') NOT NULL,
	`expectedToken` varchar(50),
	`scrollsRequired` json,
	`scrollsSealed` json,
	`scrollsMissing` json,
	`resultMessage` text,
	`currentChain` json,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	CONSTRAINT `token_activation_attempts_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `token_chain_states` (
	`id` int AUTO_INCREMENT NOT NULL,
	`houseId` int NOT NULL,
	`userId` varchar(255) NOT NULL,
	`currentTokenIndex` int NOT NULL DEFAULT 0,
	`activatedTokens` json,
	`mirrorActivatedAt` timestamp,
	`giftActivatedAt` timestamp,
	`sparkActivatedAt` timestamp,
	`houseActivatedAt` timestamp,
	`mirrorScrollsSealed` json,
	`giftScrollsSealed` json,
	`sparkScrollsSealed` json,
	`houseScrollsSealed` json,
	`chainStatus` enum('pending','in_progress','completed','blocked') NOT NULL DEFAULT 'pending',
	`blockReason` text,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `token_chain_states_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `token_expansions` (
	`id` int AUTO_INCREMENT NOT NULL,
	`sourceTokenId` int NOT NULL,
	`sourceHouseId` int NOT NULL,
	`sourceUserId` varchar(255) NOT NULL,
	`targetHouseId` int,
	`targetUserId` varchar(255),
	`expansionType` enum('bloodline','mirrored','adaptive') NOT NULL,
	`requiredScrollsComplete` boolean NOT NULL DEFAULT false,
	`requiredTimeElapsed` boolean NOT NULL DEFAULT false,
	`minimumDaysRequired` int DEFAULT 365,
	`expansionStatus` enum('pending','approved','rejected','completed','frozen') NOT NULL DEFAULT 'pending',
	`approvedBy` varchar(255),
	`approvedAt` timestamp,
	`rejectionReason` text,
	`enforcementActive` boolean NOT NULL DEFAULT true,
	`enforcementViolations` int NOT NULL DEFAULT 0,
	`frozenAt` timestamp,
	`freezeReason` text,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `token_expansions_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `token_locks` (
	`id` int AUTO_INCREMENT NOT NULL,
	`tokenId` int NOT NULL,
	`tokenType` enum('mirror','gift','spark','house','crown') NOT NULL,
	`houseId` int NOT NULL,
	`userId` varchar(255) NOT NULL,
	`lockType` enum('time_based','scroll_based','lineage_based','manual') NOT NULL,
	`lockDurationDays` int,
	`lockStartedAt` timestamp NOT NULL DEFAULT (now()),
	`lockExpiresAt` timestamp,
	`requiredScrolls` json,
	`sealedScrolls` json,
	`requiresLineageVerification` boolean DEFAULT false,
	`lineageVerifiedAt` timestamp,
	`lineageVerifiedBy` varchar(255),
	`lockStatus` enum('active','unlocked','expired','violated') NOT NULL DEFAULT 'active',
	`unlockedAt` timestamp,
	`unlockReason` text,
	`violationCount` int NOT NULL DEFAULT 0,
	`lastViolationAt` timestamp,
	`violationDetails` json,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `token_locks_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `token_transaction_log` (
	`id` int AUTO_INCREMENT NOT NULL,
	`userId` int NOT NULL,
	`amount` int NOT NULL,
	`transactionType` enum('earned','spent','bonus','adjustment') NOT NULL,
	`source` varchar(100) NOT NULL,
	`description` text,
	`balanceAfter` int NOT NULL,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	CONSTRAINT `token_transaction_log_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `tournament_matches` (
	`id` int AUTO_INCREMENT NOT NULL,
	`tournamentId` int NOT NULL,
	`roundNumber` int NOT NULL,
	`matchNumber` int NOT NULL,
	`bracketPosition` varchar(50),
	`player1Id` int,
	`player2Id` int,
	`winnerId` int,
	`player1Score` int DEFAULT 0,
	`player2Score` int DEFAULT 0,
	`status` enum('pending','ready','in_progress','completed','bye') NOT NULL DEFAULT 'pending',
	`scheduledAt` timestamp,
	`startedAt` timestamp,
	`completedAt` timestamp,
	`gameMatchId` int,
	`spectatorCount` int DEFAULT 0,
	CONSTRAINT `tournament_matches_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `tournament_participants` (
	`id` int AUTO_INCREMENT NOT NULL,
	`tournamentId` int NOT NULL,
	`playerId` int NOT NULL,
	`seed` int,
	`status` enum('registered','checked_in','playing','eliminated','winner') NOT NULL DEFAULT 'registered',
	`currentRound` int DEFAULT 0,
	`wins` int DEFAULT 0,
	`losses` int DEFAULT 0,
	`pointsScored` int DEFAULT 0,
	`registeredAt` timestamp NOT NULL DEFAULT (now()),
	`checkedInAt` timestamp,
	`eliminatedAt` timestamp,
	`finalPlacement` int,
	`prizesWon` int DEFAULT 0,
	CONSTRAINT `tournament_participants_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `trademark_searches` (
	`id` varchar(36) NOT NULL,
	`userId` int NOT NULL,
	`businessName` varchar(200) NOT NULL,
	`searchDate` timestamp NOT NULL DEFAULT (now()),
	`totalResults` int NOT NULL DEFAULT 0,
	`exactMatch` boolean NOT NULL DEFAULT false,
	`conflictRisk` enum('none','low','medium','high') NOT NULL DEFAULT 'none',
	`resultsJson` text,
	`recommendationsJson` text,
	`entityId` varchar(36),
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	CONSTRAINT `trademark_searches_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `training_answers` (
	`id` int AUTO_INCREMENT NOT NULL,
	`questionId` int NOT NULL,
	`answerText` text NOT NULL,
	`isCorrect` boolean NOT NULL DEFAULT false,
	`feedback` text,
	`orderIndex` int NOT NULL DEFAULT 0,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	CONSTRAINT `training_answers_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `training_hub_assignments` (
	`id` int AUTO_INCREMENT NOT NULL,
	`userId` int NOT NULL,
	`moduleId` int NOT NULL,
	`assignedBy` int NOT NULL,
	`assignmentType` enum('onboarding','compliance','development','remedial','certification') NOT NULL,
	`dueDate` timestamp,
	`priority` enum('low','medium','high','urgent') NOT NULL DEFAULT 'medium',
	`status` enum('assigned','in_progress','completed','overdue','waived') NOT NULL DEFAULT 'assigned',
	`completedAt` timestamp,
	`assignmentNotes` text,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `training_hub_assignments_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `training_hub_certifications` (
	`id` int AUTO_INCREMENT NOT NULL,
	`userId` int NOT NULL,
	`moduleId` int NOT NULL,
	`progressId` int NOT NULL,
	`certificateNumber` varchar(100) NOT NULL,
	`title` varchar(255) NOT NULL,
	`category` varchar(100) NOT NULL,
	`finalScore` decimal(5,2) NOT NULL,
	`tokensEarned` int NOT NULL DEFAULT 0,
	`issuedAt` timestamp NOT NULL DEFAULT (now()),
	`expiresAt` timestamp,
	`isActive` boolean NOT NULL DEFAULT true,
	`blockchainHash` varchar(64),
	`verificationUrl` varchar(500),
	`recertificationRequired` boolean NOT NULL DEFAULT false,
	`recertificationDueAt` timestamp,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	CONSTRAINT `training_hub_certifications_id` PRIMARY KEY(`id`),
	CONSTRAINT `training_hub_certifications_certificateNumber_unique` UNIQUE(`certificateNumber`)
);
--> statement-breakpoint
CREATE TABLE `training_hub_modules` (
	`id` int AUTO_INCREMENT NOT NULL,
	`moduleCode` varchar(50) NOT NULL,
	`title` varchar(255) NOT NULL,
	`description` text,
	`category` enum('company_wide','new_employee','department_specific','position_specific','compliance') NOT NULL,
	`department` varchar(100),
	`positionId` int,
	`scenarioCount` int NOT NULL DEFAULT 5,
	`estimatedMinutes` int NOT NULL DEFAULT 30,
	`passingScore` int NOT NULL DEFAULT 80,
	`issuesCertification` boolean NOT NULL DEFAULT true,
	`certificationValidityMonths` int DEFAULT 12,
	`completionTokens` int NOT NULL DEFAULT 50,
	`prerequisiteModuleIds` json,
	`isRequired` boolean NOT NULL DEFAULT false,
	`status` enum('draft','active','archived') NOT NULL DEFAULT 'draft',
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `training_hub_modules_id` PRIMARY KEY(`id`),
	CONSTRAINT `training_hub_modules_moduleCode_unique` UNIQUE(`moduleCode`)
);
--> statement-breakpoint
CREATE TABLE `training_hub_progress` (
	`id` int AUTO_INCREMENT NOT NULL,
	`userId` int NOT NULL,
	`moduleId` int NOT NULL,
	`scenariosCompleted` int NOT NULL DEFAULT 0,
	`totalScenarios` int NOT NULL,
	`currentScenarioIndex` int NOT NULL DEFAULT 0,
	`correctAnswers` int NOT NULL DEFAULT 0,
	`totalPoints` int NOT NULL DEFAULT 0,
	`maxPoints` int NOT NULL,
	`percentageScore` decimal(5,2),
	`status` enum('not_started','in_progress','completed','failed') NOT NULL DEFAULT 'not_started',
	`passed` boolean NOT NULL DEFAULT false,
	`startedAt` timestamp,
	`completedAt` timestamp,
	`attemptNumber` int NOT NULL DEFAULT 1,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `training_hub_progress_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `training_hub_scenarios` (
	`id` int AUTO_INCREMENT NOT NULL,
	`moduleId` int NOT NULL,
	`title` varchar(255) NOT NULL,
	`description` text,
	`scenarioText` text NOT NULL,
	`orderIndex` int NOT NULL DEFAULT 0,
	`questionType` enum('multiple_choice','true_false','scenario_response','fill_blank') NOT NULL DEFAULT 'multiple_choice',
	`options` json,
	`correctAnswer` text,
	`explanation` text,
	`points` int NOT NULL DEFAULT 10,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	CONSTRAINT `training_hub_scenarios_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `training_modules` (
	`id` int AUTO_INCREMENT NOT NULL,
	`name` varchar(255) NOT NULL,
	`description` text,
	`agentType` varchar(64),
	`simulatorType` varchar(64),
	`difficulty` enum('beginner','intermediate','advanced') NOT NULL DEFAULT 'beginner',
	`estimatedMinutes` int DEFAULT 30,
	`passingScore` int DEFAULT 70,
	`isActive` boolean NOT NULL DEFAULT true,
	`isPublic` boolean NOT NULL DEFAULT true,
	`createdBy` int NOT NULL,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `training_modules_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `training_questions` (
	`id` int AUTO_INCREMENT NOT NULL,
	`topicId` int NOT NULL,
	`questionText` text NOT NULL,
	`questionType` enum('multiple_choice','true_false','open_ended','fill_blank') NOT NULL DEFAULT 'multiple_choice',
	`difficulty` enum('easy','medium','hard') NOT NULL DEFAULT 'medium',
	`points` int NOT NULL DEFAULT 10,
	`orderIndex` int NOT NULL DEFAULT 0,
	`explanation` text,
	`hint` text,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `training_questions_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `training_responses` (
	`id` int AUTO_INCREMENT NOT NULL,
	`sessionId` int NOT NULL,
	`questionId` int NOT NULL,
	`answerId` int,
	`userAnswer` text,
	`isCorrect` boolean NOT NULL DEFAULT false,
	`pointsEarned` int NOT NULL DEFAULT 0,
	`timeSpentSeconds` int,
	`answeredAt` timestamp NOT NULL DEFAULT (now()),
	CONSTRAINT `training_responses_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `training_sessions` (
	`id` int AUTO_INCREMENT NOT NULL,
	`userId` int NOT NULL,
	`moduleId` int NOT NULL,
	`agentConversationId` int,
	`status` enum('in_progress','completed','abandoned') NOT NULL DEFAULT 'in_progress',
	`currentTopicId` int,
	`currentQuestionId` int,
	`totalQuestions` int NOT NULL DEFAULT 0,
	`answeredQuestions` int NOT NULL DEFAULT 0,
	`correctAnswers` int NOT NULL DEFAULT 0,
	`score` int NOT NULL DEFAULT 0,
	`totalPoints` int NOT NULL DEFAULT 0,
	`earnedPoints` int NOT NULL DEFAULT 0,
	`startedAt` timestamp NOT NULL DEFAULT (now()),
	`completedAt` timestamp,
	CONSTRAINT `training_sessions_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `training_topics` (
	`id` int AUTO_INCREMENT NOT NULL,
	`moduleId` int NOT NULL,
	`name` varchar(255) NOT NULL,
	`description` text,
	`orderIndex` int NOT NULL DEFAULT 0,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `training_topics_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `treasury_claims` (
	`id` int AUTO_INCREMENT NOT NULL,
	`sourceType` enum('derivative_logic','scroll_usage','ai_interface','blockchain_deployment') NOT NULL,
	`sourceIdentifier` varchar(255) NOT NULL,
	`grossAmount` decimal(20,2) NOT NULL,
	`claimPercentage` decimal(5,2) NOT NULL DEFAULT '15.00',
	`claimAmount` decimal(20,2) NOT NULL,
	`description` text,
	`claimHash` varchar(255) NOT NULL,
	`status` enum('pending','collected','disputed') NOT NULL DEFAULT 'pending',
	`collectedAt` timestamp,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `treasury_claims_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `trial_exit_surveys` (
	`id` int AUTO_INCREMENT NOT NULL,
	`trialUserId` int NOT NULL,
	`overallRating` int,
	`wouldRecommend` int,
	`easeOfUseRating` int,
	`featureCompletenessRating` int,
	`designRating` int,
	`valuePropositionRating` int,
	`mostUsefulFeature` text,
	`missingFeatures` text,
	`biggestConcerns` text,
	`additionalComments` text,
	`interestedInJoining` enum('yes','no','maybe'),
	`preferredFollowUp` enum('email','call','demo','none'),
	`completedAt` timestamp NOT NULL DEFAULT (now()),
	CONSTRAINT `trial_exit_surveys_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `trial_feature_exploration` (
	`id` int AUTO_INCREMENT NOT NULL,
	`trialUserId` int NOT NULL,
	`featureCategory` varchar(100) NOT NULL,
	`featureName` varchar(100) NOT NULL,
	`firstExploredAt` timestamp NOT NULL DEFAULT (now()),
	`lastExploredAt` timestamp NOT NULL DEFAULT (now()),
	`explorationCount` int NOT NULL DEFAULT 1,
	`totalTimeSeconds` int NOT NULL DEFAULT 0,
	`completedAction` boolean NOT NULL DEFAULT false,
	`completedAt` timestamp,
	CONSTRAINT `trial_feature_exploration_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `trial_feedback` (
	`id` int AUTO_INCREMENT NOT NULL,
	`trialUserId` int NOT NULL,
	`trialSessionId` int,
	`feedbackType` enum('overall_rating','feature_rating','bug_report','suggestion','exit_survey','inline_comment') NOT NULL,
	`rating` int,
	`featureName` varchar(100),
	`pagePath` varchar(500),
	`comment` text,
	`wantsResponse` boolean NOT NULL DEFAULT false,
	`responseStatus` enum('pending','responded','closed') DEFAULT 'pending',
	`respondedAt` timestamp,
	`respondedBy` int,
	`responseNote` text,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	CONSTRAINT `trial_feedback_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `trial_page_views` (
	`id` int AUTO_INCREMENT NOT NULL,
	`trialUserId` int NOT NULL,
	`trialSessionId` int NOT NULL,
	`pagePath` varchar(500) NOT NULL,
	`pageTitle` varchar(255),
	`enteredAt` timestamp NOT NULL DEFAULT (now()),
	`exitedAt` timestamp,
	`timeOnPageSeconds` int,
	`scrollDepthPercent` int,
	`clickCount` int NOT NULL DEFAULT 0,
	`previousPage` varchar(500),
	CONSTRAINT `trial_page_views_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `trial_sample_data` (
	`id` int AUTO_INCREMENT NOT NULL,
	`trialUserId` int NOT NULL,
	`sampleHouseId` int,
	`sampleBusinessId` int,
	`dataGeneratedAt` timestamp,
	`dataTemplate` varchar(50) NOT NULL DEFAULT 'standard',
	`isIsolated` boolean NOT NULL DEFAULT true,
	CONSTRAINT `trial_sample_data_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `trial_sessions` (
	`id` int AUTO_INCREMENT NOT NULL,
	`trialUserId` int NOT NULL,
	`sessionStart` timestamp NOT NULL DEFAULT (now()),
	`sessionEnd` timestamp,
	`durationSeconds` int,
	`ipAddress` varchar(45),
	`userAgent` text,
	`deviceType` varchar(50),
	`browser` varchar(50),
	`pagesVisited` int NOT NULL DEFAULT 0,
	`featuresExplored` json,
	`actionsPerformed` int NOT NULL DEFAULT 0,
	CONSTRAINT `trial_sessions_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `trial_users` (
	`id` int AUTO_INCREMENT NOT NULL,
	`email` varchar(320) NOT NULL,
	`name` varchar(255) NOT NULL,
	`passwordHash` varchar(255) NOT NULL,
	`organization` varchar(255),
	`role` varchar(100),
	`status` enum('pending','active','expired','converted') NOT NULL DEFAULT 'pending',
	`trialStartedAt` timestamp,
	`trialExpiresAt` timestamp,
	`totalSessions` int NOT NULL DEFAULT 0,
	`totalTimeSpentSeconds` int NOT NULL DEFAULT 0,
	`lastLoginAt` timestamp,
	`convertedToUserId` int,
	`convertedAt` timestamp,
	`wantsUpdates` boolean NOT NULL DEFAULT false,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `trial_users_id` PRIMARY KEY(`id`),
	CONSTRAINT `trial_users_email_unique` UNIQUE(`email`)
);
--> statement-breakpoint
CREATE TABLE `trivia_categories` (
	`id` int AUTO_INCREMENT NOT NULL,
	`name` varchar(100) NOT NULL,
	`slug` varchar(50) NOT NULL,
	`description` text,
	`icon` varchar(50),
	`color` varchar(20),
	`isActive` boolean NOT NULL DEFAULT true,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	CONSTRAINT `trivia_categories_id` PRIMARY KEY(`id`),
	CONSTRAINT `trivia_categories_slug_unique` UNIQUE(`slug`)
);
--> statement-breakpoint
CREATE TABLE `trivia_questions` (
	`id` int AUTO_INCREMENT NOT NULL,
	`categoryId` int NOT NULL,
	`question` text NOT NULL,
	`correctAnswer` text NOT NULL,
	`wrongAnswers` json NOT NULL,
	`difficulty` enum('easy','medium','hard') NOT NULL DEFAULT 'medium',
	`ageGroup` enum('k_5','6_8','9_12','adult','all_ages') NOT NULL DEFAULT 'all_ages',
	`explanation` text,
	`source` varchar(200),
	`timesAsked` int DEFAULT 0,
	`timesCorrect` int DEFAULT 0,
	`isActive` boolean NOT NULL DEFAULT true,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `trivia_questions_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `trust_distribution_contracts` (
	`id` int AUTO_INCREMENT NOT NULL,
	`contractId` int NOT NULL,
	`trustName` varchar(255) NOT NULL,
	`trustType` enum('revocable','irrevocable','family','asset_protection','living','testamentary','98_trust','foreign_trust') NOT NULL,
	`grantorWalletId` int NOT NULL,
	`splitType` enum('60_40','70_30','custom') NOT NULL,
	`beneficiaries` json NOT NULL,
	`assets` json,
	`conditions` json,
	`isActive` boolean NOT NULL DEFAULT true,
	`activatedAt` timestamp,
	`lastDistributionAt` timestamp,
	`totalDistributed` decimal(20,8) NOT NULL DEFAULT '0',
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `trust_distribution_contracts_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `user_preferences` (
	`id` int AUTO_INCREMENT NOT NULL,
	`user_id` int NOT NULL,
	`locale_id` int,
	`timezone` varchar(100) NOT NULL DEFAULT 'UTC',
	`preferred_currency` varchar(10) NOT NULL DEFAULT 'USD',
	`crypto_display` enum('symbol','name','both') NOT NULL DEFAULT 'symbol',
	`email_notifications` boolean NOT NULL DEFAULT true,
	`push_notifications` boolean NOT NULL DEFAULT true,
	`sms_notifications` boolean NOT NULL DEFAULT false,
	`show_balances` boolean NOT NULL DEFAULT true,
	`public_profile` boolean NOT NULL DEFAULT false,
	`preferred_ai_provider` varchar(50),
	`ai_response_style` enum('concise','detailed','conversational') NOT NULL DEFAULT 'detailed',
	`created_at` timestamp NOT NULL DEFAULT (now()),
	`updated_at` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `user_preferences_id` PRIMARY KEY(`id`),
	CONSTRAINT `user_preferences_user_id_unique` UNIQUE(`user_id`)
);
--> statement-breakpoint
CREATE TABLE `user_presence` (
	`id` int AUTO_INCREMENT NOT NULL,
	`userId` int NOT NULL,
	`status` enum('online','away','busy','offline') NOT NULL DEFAULT 'offline',
	`statusMessage` varchar(255),
	`lastActiveAt` timestamp NOT NULL DEFAULT (now()),
	`lastSeenAt` timestamp NOT NULL DEFAULT (now()),
	`currentActivity` enum('none','in_meeting','presenting','typing') DEFAULT 'none',
	`currentMeetingId` int,
	`currentChatId` int,
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `user_presence_id` PRIMARY KEY(`id`),
	CONSTRAINT `user_presence_userId_unique` UNIQUE(`userId`)
);
--> statement-breakpoint
CREATE TABLE `user_profiles` (
	`id` int AUTO_INCREMENT NOT NULL,
	`userId` int NOT NULL,
	`firstName` varchar(100) NOT NULL,
	`lastName` varchar(100) NOT NULL,
	`phone` varchar(20),
	`address` varchar(255),
	`city` varchar(100),
	`state` varchar(50),
	`zip` varchar(20),
	`dateOfBirth` varchar(20),
	`highestEducation` enum('high-school','some-college','associates','bachelors','masters','doctorate'),
	`fieldOfStudy` varchar(100),
	`currentOccupation` varchar(100),
	`yearsExperience` enum('0-2','3-5','6-10','11-20','20+'),
	`skills` json,
	`certifications` text,
	`primaryGoal` enum('start-business','employment','education','community','grants','wealth-building'),
	`secondaryGoals` json,
	`timeline` enum('immediate','1-3-months','3-6-months','6-12-months','1-year-plus'),
	`departmentInterests` json,
	`availability` enum('full-time','part-time','volunteer','consulting'),
	`emergencyName` varchar(100),
	`emergencyRelationship` varchar(50),
	`emergencyPhone` varchar(20),
	`idDocumentUrl` varchar(500),
	`resumeUrl` varchar(500),
	`certificationsDocUrl` varchar(500),
	`profileStatus` enum('draft','complete','verified') NOT NULL DEFAULT 'draft',
	`completedAt` timestamp,
	`verifiedAt` timestamp,
	`verifiedBy` int,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `user_profiles_id` PRIMARY KEY(`id`),
	CONSTRAINT `user_profiles_userId_unique` UNIQUE(`userId`)
);
--> statement-breakpoint
CREATE TABLE `user_token_balance` (
	`id` int AUTO_INCREMENT NOT NULL,
	`userId` int NOT NULL,
	`totalTokens` int NOT NULL DEFAULT 0,
	`lifetimeTokensEarned` int NOT NULL DEFAULT 0,
	`tokensSpent` int NOT NULL DEFAULT 0,
	`lastEarnedAt` timestamp,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `user_token_balance_id` PRIMARY KEY(`id`),
	CONSTRAINT `user_token_balance_userId_unique` UNIQUE(`userId`)
);
--> statement-breakpoint
CREATE TABLE `vault_access_logs` (
	`id` int AUTO_INCREMENT NOT NULL,
	`vault_id` int NOT NULL,
	`document_id` int,
	`vault_access_type` enum('view','download','upload','delete','share','print') NOT NULL,
	`accessed_by_user_id` int NOT NULL,
	`ip_address` varchar(45),
	`user_agent` varchar(500),
	`access_granted` boolean NOT NULL DEFAULT true,
	`denial_reason` varchar(255),
	`created_at` timestamp NOT NULL DEFAULT (now()),
	CONSTRAINT `vault_access_logs_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `vault_documents` (
	`id` int AUTO_INCREMENT NOT NULL,
	`vault_id` int NOT NULL,
	`folder_id` int,
	`document_name` varchar(500) NOT NULL,
	`document_type` enum('legal','financial','tax','insurance','property','identity','contract','certificate','receipt','correspondence','other') NOT NULL,
	`file_name` varchar(500) NOT NULL,
	`file_size` int NOT NULL,
	`mime_type` varchar(100) NOT NULL,
	`file_hash` varchar(64) NOT NULL,
	`s3_key` varchar(500) NOT NULL,
	`s3_url` varchar(1000),
	`description` text,
	`tags` json,
	`version` int NOT NULL DEFAULT 1,
	`previous_version_id` int,
	`expiration_date` timestamp,
	`uploaded_by_user_id` int NOT NULL,
	`created_at` timestamp NOT NULL DEFAULT (now()),
	`updated_at` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `vault_documents_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `vault_folders` (
	`id` int AUTO_INCREMENT NOT NULL,
	`vault_id` int NOT NULL,
	`parent_folder_id` int,
	`folder_name` varchar(255) NOT NULL,
	`folder_path` varchar(1000) NOT NULL,
	`document_count` int NOT NULL DEFAULT 0,
	`is_private` boolean NOT NULL DEFAULT false,
	`created_at` timestamp NOT NULL DEFAULT (now()),
	`updated_at` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `vault_folders_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `video_provider_configs` (
	`id` int AUTO_INCREMENT NOT NULL,
	`provider` enum('daily','teams','zoom','custom') NOT NULL,
	`isEnabled` boolean NOT NULL DEFAULT false,
	`isDefault` boolean DEFAULT false,
	`apiKey` text,
	`apiSecret` text,
	`webhookSecret` text,
	`accessToken` text,
	`refreshToken` text,
	`tokenExpiresAt` timestamp,
	`settings` json,
	`tenantId` varchar(255),
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `video_provider_configs_id` PRIMARY KEY(`id`),
	CONSTRAINT `video_provider_configs_provider_unique` UNIQUE(`provider`)
);
--> statement-breakpoint
CREATE TABLE `virtual_spaces` (
	`id` int AUTO_INCREMENT NOT NULL,
	`space_code` varchar(100) NOT NULL,
	`space_name` varchar(255) NOT NULL,
	`metaverse_platform` enum('custom','decentraland','sandbox','spatial','horizon','vr_chat','other') NOT NULL DEFAULT 'custom',
	`coordinates` json,
	`world_id` varchar(255),
	`owner_house_id` int,
	`owner_user_id` int,
	`capacity` int,
	`is_public` boolean NOT NULL DEFAULT true,
	`space_status` enum('active','under_construction','archived') NOT NULL DEFAULT 'under_construction',
	`created_at` timestamp NOT NULL DEFAULT (now()),
	`updated_at` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `virtual_spaces_id` PRIMARY KEY(`id`),
	CONSTRAINT `virtual_spaces_space_code_unique` UNIQUE(`space_code`)
);
--> statement-breakpoint
CREATE TABLE `vocabulary_words` (
	`id` int AUTO_INCREMENT NOT NULL,
	`userId` int NOT NULL,
	`bookId` int,
	`studentProfileId` int,
	`word` varchar(100) NOT NULL,
	`definition` text,
	`partOfSpeech` varchar(50),
	`pronunciation` varchar(100),
	`exampleSentence` text,
	`contextFromBook` text,
	`masteryLevel` enum('new','learning','familiar','mastered') NOT NULL DEFAULT 'new',
	`timesReviewed` int DEFAULT 0,
	`timesCorrect` int DEFAULT 0,
	`lastReviewedAt` timestamp,
	`nextReviewAt` timestamp,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `vocabulary_words_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `w2_workers` (
	`id` int AUTO_INCREMENT NOT NULL,
	`house_id` int NOT NULL,
	`business_entity_id` int,
	`first_name` varchar(100) NOT NULL,
	`last_name` varchar(100) NOT NULL,
	`middle_name` varchar(100),
	`email` varchar(255),
	`phone` varchar(50),
	`address` varchar(500),
	`city` varchar(100),
	`state` varchar(50),
	`zip_code` varchar(20),
	`ssn_encrypted` varchar(255),
	`federal_filing_status` enum('single','married_filing_jointly','married_filing_separately','head_of_household','qualifying_widow') NOT NULL DEFAULT 'single',
	`federal_allowances` int NOT NULL DEFAULT 0,
	`state_filing_status` varchar(50),
	`state_allowances` int DEFAULT 0,
	`employment_type` enum('full_time','part_time','seasonal','temporary') NOT NULL DEFAULT 'full_time',
	`department` varchar(100),
	`job_title` varchar(255),
	`hire_date` timestamp NOT NULL,
	`termination_date` timestamp,
	`pay_type` enum('hourly','salary','commission') NOT NULL DEFAULT 'hourly',
	`pay_rate` decimal(18,2) NOT NULL,
	`pay_frequency` enum('weekly','bi_weekly','semi_monthly','monthly') NOT NULL DEFAULT 'bi_weekly',
	`direct_deposit_enabled` boolean NOT NULL DEFAULT false,
	`bank_routing_encrypted` varchar(255),
	`bank_account_encrypted` varchar(255),
	`worker_status` enum('active','on_leave','terminated') NOT NULL DEFAULT 'active',
	`created_at` timestamp NOT NULL DEFAULT (now()),
	`updated_at` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `w2_workers_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `wallet_balances` (
	`id` int AUTO_INCREMENT NOT NULL,
	`wallet_id` int NOT NULL,
	`token_id` int NOT NULL,
	`balance` decimal(36,18) NOT NULL DEFAULT '0',
	`balance_usd` decimal(20,2),
	`pending_incoming` decimal(36,18) NOT NULL DEFAULT '0',
	`pending_outgoing` decimal(36,18) NOT NULL DEFAULT '0',
	`last_synced_at` timestamp,
	`last_sync_block_number` varchar(100),
	`created_at` timestamp NOT NULL DEFAULT (now()),
	`updated_at` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `wallet_balances_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `weekly_game_requirements` (
	`id` int AUTO_INCREMENT NOT NULL,
	`userId` int NOT NULL,
	`weekNumber` int NOT NULL,
	`weekYear` int NOT NULL,
	`weekStartDate` timestamp NOT NULL,
	`weekEndDate` timestamp NOT NULL,
	`requiredMinutes` int NOT NULL DEFAULT 300,
	`completedMinutes` int NOT NULL DEFAULT 0,
	`complianceStatus` enum('not_started','in_progress','completed','exceeded','excused') NOT NULL DEFAULT 'not_started',
	`bonusTokensAwarded` int DEFAULT 0,
	`streakWeeks` int DEFAULT 0,
	`uniqueGamesPlayed` int DEFAULT 0,
	`managerNotes` text,
	`excuseReason` text,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `weekly_game_requirements_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `worker_badges` (
	`id` int AUTO_INCREMENT NOT NULL,
	`progressionId` int NOT NULL,
	`badgeId` int NOT NULL,
	`status` enum('active','expired','revoked') NOT NULL DEFAULT 'active',
	`earnedAt` timestamp NOT NULL DEFAULT (now()),
	`expiresAt` timestamp,
	`revokedAt` timestamp,
	`achievementValue` varchar(255),
	`achievementPeriodStart` timestamp,
	`achievementPeriodEnd` timestamp,
	`blockchainHash` varchar(255),
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	CONSTRAINT `worker_badges_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `worker_bank_accounts` (
	`id` int AUTO_INCREMENT NOT NULL,
	`worker_id` int NOT NULL,
	`account_name` varchar(100) NOT NULL,
	`bank_name` varchar(100) NOT NULL,
	`routing_number` varchar(9) NOT NULL,
	`account_number` varchar(17) NOT NULL,
	`account_type` enum('checking','savings') NOT NULL DEFAULT 'checking',
	`deposit_type` enum('full','fixed','percentage') NOT NULL DEFAULT 'full',
	`deposit_amount` decimal(18,2),
	`priority` int NOT NULL DEFAULT 1,
	`is_active` boolean NOT NULL DEFAULT true,
	`is_primary` boolean NOT NULL DEFAULT false,
	`verified_at` timestamp,
	`created_at` timestamp NOT NULL DEFAULT (now()),
	`updated_at` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `worker_bank_accounts_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `worker_certifications` (
	`id` int AUTO_INCREMENT NOT NULL,
	`progressionId` int NOT NULL,
	`certificationId` int NOT NULL,
	`status` enum('in_progress','pending_assessment','active','expired','revoked') NOT NULL DEFAULT 'in_progress',
	`startedAt` timestamp NOT NULL DEFAULT (now()),
	`earnedAt` timestamp,
	`expiresAt` timestamp,
	`revokedAt` timestamp,
	`assessmentScore` int,
	`assessmentDate` timestamp,
	`assessorId` int,
	`assessmentNotes` text,
	`trainingHoursCompleted` int NOT NULL DEFAULT 0,
	`practiceHoursCompleted` int NOT NULL DEFAULT 0,
	`certificateNumber` varchar(100),
	`certificateUrl` text,
	`blockchainHash` varchar(255),
	`renewalCount` int NOT NULL DEFAULT 0,
	`lastRenewalAt` timestamp,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `worker_certifications_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `worker_charge_code_assignments` (
	`id` int AUTO_INCREMENT NOT NULL,
	`workerId` int NOT NULL,
	`chargeCodeId` int NOT NULL,
	`assignedAt` timestamp NOT NULL DEFAULT (now()),
	`assignedBy` int,
	`maxHoursPerWeek` decimal(5,2),
	`effectiveFrom` timestamp,
	`effectiveTo` timestamp,
	`isActive` boolean NOT NULL DEFAULT true,
	CONSTRAINT `worker_charge_code_assignments_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `worker_learning_progress` (
	`id` int AUTO_INCREMENT NOT NULL,
	`progressionId` int NOT NULL,
	`pathwayId` int NOT NULL,
	`currentStepIndex` int NOT NULL DEFAULT 0,
	`completedSteps` json,
	`progressPercentage` int NOT NULL DEFAULT 0,
	`status` enum('not_started','in_progress','completed','paused','abandoned') NOT NULL DEFAULT 'not_started',
	`startedAt` timestamp,
	`completedAt` timestamp,
	`lastActivityAt` timestamp,
	`estimatedCompletionDate` timestamp,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `worker_learning_progress_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `worker_milestone_progress` (
	`id` int AUTO_INCREMENT NOT NULL,
	`progressionId` int NOT NULL,
	`milestoneId` int NOT NULL,
	`currentValue` varchar(255),
	`targetValue` varchar(255),
	`progressPercentage` int NOT NULL DEFAULT 0,
	`status` enum('not_started','in_progress','completed','waived') NOT NULL DEFAULT 'not_started',
	`completedAt` timestamp,
	`waivedBy` int,
	`waivedReason` text,
	`evidenceUrl` text,
	`evidenceNotes` text,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `worker_milestone_progress_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `worker_progressions` (
	`id` int AUTO_INCREMENT NOT NULL,
	`userId` int NOT NULL,
	`workerId` int,
	`workerType` enum('w2_employee','contractor') NOT NULL,
	`currentStage` enum('w2_employee','senior_employee','contractor','certified_contractor','business_owner','house_member') NOT NULL DEFAULT 'w2_employee',
	`stageEnteredAt` timestamp NOT NULL DEFAULT (now()),
	`previousStage` enum('w2_employee','senior_employee','contractor','certified_contractor','business_owner','house_member'),
	`previousStageExitedAt` timestamp,
	`readinessScore` int NOT NULL DEFAULT 0,
	`nextStageEligible` boolean NOT NULL DEFAULT false,
	`nextStageBlockers` json,
	`mentorId` int,
	`mentorAssignedAt` timestamp,
	`primaryDepartmentId` int,
	`secondaryDepartments` json,
	`qualityScore` decimal(5,2) DEFAULT '0.00',
	`onTimeDeliveryRate` decimal(5,2) DEFAULT '0.00',
	`clientSatisfactionScore` decimal(5,2) DEFAULT '0.00',
	`totalRevenueGenerated` decimal(15,2) DEFAULT '0.00',
	`targetStage` enum('w2_employee','senior_employee','contractor','certified_contractor','business_owner','house_member'),
	`targetDate` timestamp,
	`careerNotes` text,
	`linkedBusinessId` int,
	`linkedHouseId` int,
	`luvLedgerAccountId` int,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `worker_progressions_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `worker_skills` (
	`id` int AUTO_INCREMENT NOT NULL,
	`progressionId` int NOT NULL,
	`skillId` int NOT NULL,
	`proficiencyLevel` enum('novice','beginner','intermediate','advanced','master') NOT NULL DEFAULT 'novice',
	`selfAssessedLevel` enum('novice','beginner','intermediate','advanced','master'),
	`verifiedLevel` enum('novice','beginner','intermediate','advanced','master'),
	`verifiedBy` int,
	`verifiedAt` timestamp,
	`yearsExperience` decimal(4,1) DEFAULT '0.0',
	`projectCount` int NOT NULL DEFAULT 0,
	`currentlyLearning` boolean NOT NULL DEFAULT false,
	`targetLevel` enum('novice','beginner','intermediate','advanced','master'),
	`aiRecommendedPriority` int,
	`aiRecommendationReason` text,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `worker_skills_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `worker_tax_withholdings` (
	`id` int AUTO_INCREMENT NOT NULL,
	`worker_id` int NOT NULL,
	`tax_year` int NOT NULL,
	`ytd_gross_pay` decimal(18,2) NOT NULL DEFAULT '0',
	`ytd_federal_tax` decimal(18,2) NOT NULL DEFAULT '0',
	`ytd_state_tax` decimal(18,2) NOT NULL DEFAULT '0',
	`ytd_local_tax` decimal(18,2) NOT NULL DEFAULT '0',
	`ytd_social_security` decimal(18,2) NOT NULL DEFAULT '0',
	`ytd_medicare` decimal(18,2) NOT NULL DEFAULT '0',
	`w2_generated` boolean NOT NULL DEFAULT false,
	`w2_generated_at` timestamp,
	`w2_document_id` int,
	`created_at` timestamp NOT NULL DEFAULT (now()),
	`updated_at` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `worker_tax_withholdings_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `ytd_totals` (
	`id` int AUTO_INCREMENT NOT NULL,
	`positionHolderId` int NOT NULL,
	`taxYear` int NOT NULL,
	`ytdGrossPay` decimal(12,2) DEFAULT '0',
	`ytdRegularPay` decimal(12,2) DEFAULT '0',
	`ytdOvertimePay` decimal(12,2) DEFAULT '0',
	`ytdBonusPay` decimal(12,2) DEFAULT '0',
	`ytdFederalTax` decimal(12,2) DEFAULT '0',
	`ytdStateTax` decimal(12,2) DEFAULT '0',
	`ytdSocialSecurity` decimal(12,2) DEFAULT '0',
	`ytdMedicare` decimal(12,2) DEFAULT '0',
	`ytdHealthInsurance` decimal(12,2) DEFAULT '0',
	`ytdRetirement` decimal(12,2) DEFAULT '0',
	`ytdOtherDeductions` decimal(12,2) DEFAULT '0',
	`ytdNetPay` decimal(12,2) DEFAULT '0',
	`ytdRegularHours` decimal(8,2) DEFAULT '0',
	`ytdOvertimeHours` decimal(8,2) DEFAULT '0',
	`lastPayrollRecordId` int,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `ytd_totals_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
ALTER TABLE `business_entities` MODIFY COLUMN `entityType` enum('trust','llc','corporation','collective','508c1a','asset','division') NOT NULL;--> statement-breakpoint
ALTER TABLE `crypto_wallets` MODIFY COLUMN `walletType` enum('luvchain','bitcoin','ethereum','solana','other') NOT NULL;--> statement-breakpoint
ALTER TABLE `users` MODIFY COLUMN `role` enum('user','staff','admin','owner') NOT NULL DEFAULT 'user';--> statement-breakpoint
ALTER TABLE `business_entities` ADD `ein` varchar(20);--> statement-breakpoint
ALTER TABLE `business_entities` ADD `stateOfFormation` varchar(50);--> statement-breakpoint
ALTER TABLE `business_entities` ADD `stateEntityId` varchar(50);--> statement-breakpoint
ALTER TABLE `business_entities` ADD `formationDate` timestamp;--> statement-breakpoint
ALTER TABLE `business_entities` ADD `registeredAddress` text;--> statement-breakpoint
ALTER TABLE `business_entities` ADD `physicalAddress` text;--> statement-breakpoint
ALTER TABLE `business_entities` ADD `houseParticipationStatus` enum('pending','opted_in','opted_out') DEFAULT 'pending' NOT NULL;--> statement-breakpoint
ALTER TABLE `business_entities` ADD `linkedHouseId` int;--> statement-breakpoint
ALTER TABLE `business_entities` ADD `houseActivatedAt` timestamp;--> statement-breakpoint
ALTER TABLE `business_entities` ADD `houseOptOutReason` text;--> statement-breakpoint
ALTER TABLE `business_entities` ADD `parentEntityId` int;--> statement-breakpoint
ALTER TABLE `business_entities` ADD `allocationPercentage` decimal(5,2);--> statement-breakpoint
ALTER TABLE `crypto_wallets` ADD `businessEntityId` int;--> statement-breakpoint
ALTER TABLE `crypto_wallets` ADD `walletName` varchar(255);--> statement-breakpoint
ALTER TABLE `crypto_wallets` ADD `privateKeyHash` varchar(255);